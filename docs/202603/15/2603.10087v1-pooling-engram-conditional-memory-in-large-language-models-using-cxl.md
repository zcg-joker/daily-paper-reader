---
title: Pooling Engram Conditional Memory in Large Language Models using CXL
title_zh: 使用 CXL 在大语言模型中池化 Engram 条件记忆
authors: "Ruiyang Ma, Teng Ma, Zhiyuan Su, Hantian Zha, Xinpeng Zhao, Xuchun Shang, Xingrui Yi, Zheng Liu, Zhu Cao, An Wu, Zhichong Dou, Ziqian Liu, Daikang Kuang, Guojie Luo"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10087v1"
tags: ["query:agimem"]
score: 9.0
evidence: 使用Engram记忆将静态知识查找与动态计算解耦
tldr: 本研究针对大语言模型中 Engram 条件记忆的大规模存储需求，提出利用 CXL（Compute Express Link）内存池进行存储。Engram 通过解耦静态知识检索与动态计算提升性能，但其庞大的嵌入表对内存容量提出挑战。相比 RDMA，CXL 提供了更细粒度和低延迟的访问，适合 Engram 的稀疏访问特性。通过将 CXL 存储集成至 SGLang 框架，该方案在保持接近 DRAM 性能的同时，实现了高扩展性和成本效益。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型中 Engram 条件记忆所需的大规模嵌入表在传统内存中存储成本高且扩展性受限的问题。
method: 提出利用 CXL 内存池作为 Engram 的存储层，并将其集成到 SGLang 框架中，利用 CXL 的低延迟特性支持稀疏访问。
result: 该方案在 SGLang 中实现了接近 DRAM 的端到端推理性能，同时显著降低了存储成本。
conclusion: CXL 内存池是未来集成 Engram 的大语言模型实现高性能、可扩展且低成本存储的理想选择。
---

## 摘要
Engram 条件记忆通过将静态知识查找与动态计算解耦，已成为大语言模型（LLM）中一个极具前景的组件。由于 Engram 表现出稀疏的访问模式并支持预取，其庞大的嵌入表非常适合卸载到低层级内存中。在本文中，我们提出使用 Compute Express Link (CXL) 内存池进行 Engram 存储。与 RDMA 相比，CXL 提供了 Engram 极小且离散的检索模式所需的细粒度、低延迟访问。我们将基于 CXL 的 Engram 池集成到 SGLang 中，实现了接近 DRAM 的端到端性能。这为未来集成 Engram 的大语言模型提供了一种可扩展且具有成本效益的存储解决方案，且不会损害推理性能。

## Abstract
Engram conditional memory has emerged as a promising component for LLMs by decoupling static knowledge lookup from dynamic computation. Since Engram exhibits sparse access patterns and supports prefetching, its massive embedding tables are well-suited for offloading to lower-tier memory. In this paper, we propose using Compute Express Link (CXL) memory pool for Engram storage. Compared to RDMA, CXL provides fine-grained and low-latency access required by minimal and discrete retrieval patterns of Engram. We integrate the CXL-based Engram pool into SGLang, achieving near-DRAM end-to-end performance. This provides a scalable and cost-efficient storage solution for future Engram-integrated LLMs without compromising inference performance.

---

## 论文详细总结（自动生成）

以下是对论文《Pooling Engram Conditional Memory in Large Language Models using CXL》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）在处理词汇知识查找时效率低下。DeepSeek 提出的 **Engram** 架构通过引入“条件记忆”（N-Gram 嵌入表）来解耦静态知识检索与动态推理计算，但 Engram 表体积巨大（未来可达数百 GB），导致 GPU 显存难以承载（Memory Wall 问题）。
*   **研究动机**：Engram 具有稀疏访问、离散检索和延迟容忍（可预取）的特性。传统的 RDMA 内存池在处理此类小包、离散访问时性能较差，因此本文探索利用 **CXL (Compute Express Link)** 技术构建共享内存池，以实现高性能、低成本的 Engram 存储。

### 2. 方法论
*   **核心思想**：利用 CXL 2.0/3.0 的内存池化能力，将 Engram 嵌入表卸载到独立的 CXL 内存设备中，通过 CXL 交换机供多个计算节点共享访问。
*   **关键技术细节**：
    *   **CXL 优势利用**：CXL 提供硬件级的 load/store 语义和缓存行粒度访问，绕过了传统网络协议栈，极大地降低了 Engram 稀疏检索的延迟。
    *   **高效访问例程**：
        *   **CXL → CPU**：采用 OpenMP 多线程并行 `memcpy`，将 CXL 映射的虚拟地址数据拷贝至主机内存。
        *   **CXL → GPU (P2P)**：通过 `cudaHostRegister` 将 CXL 内存注册为 GPU 可访问地址，编写自定义 CUDA Kernel，利用 GPU 的 DMA 引擎直接通过 PCIe 总线从 CXL 设备拉取数据，绕过 CPU 参与。
    *   **框架集成**：在 SGLang 推理框架中实现异步预取机制。在 Transformer 前层计算时，提前从 CXL 池中检索后续 Engram 层所需的嵌入向量，以掩盖传输延迟。

### 3. 实验设计
*   **实验场景**：微观基准测试（检索延迟）和端到端推理测试（吞吐量）。
*   **对比方法**：本地 DRAM、基于 RDMA 的内存池（使用 Mooncake Store）、基于 CXL 的内存池。
*   **测试配置**：
    *   模拟 Engram-27B 和 Engram-40B 的嵌入表参数（词表大小、维度等）。
    *   端到端测试使用 Qwen3-4B 和 Qwen3-8B 作为基础模型（由于 DeepSeek Engram 权重未开源，通过在 Qwen3 中插入 Engram 计算层进行模拟）。

### 4. 资源与算力
*   **服务器**：2 个节点，每个节点配备双路 Intel Xeon 6766E 处理器（共 288 核）。
*   **GPU**：每个节点 2 张 NVIDIA L20 GPU。
*   **CXL 硬件**：XConn XC50256 CXL 交换机芯片，三星 256GB CXL 内存卡（Montage M88MX5851 控制器）。
*   **连接**：PCIe 5.0 x16 接口。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了不同 Batch Size（1 到 1024）下的检索延迟对比。
    *   测试了不同模型规模（4B, 8B）在 DRAM 和 CXL 模式下的吞吐量。
    *   进行了多节点扩展性实验（单节点 vs 双节点，DP=1 vs DP=2）。
    *   提供了详细的硬件成本对比分析。
*   **充分性评价**：实验设计较为全面，从底层延迟到上层应用吞吐量均有覆盖。虽然受限于硬件环境仅使用了 2 个节点，但对于验证 CXL 在 Engram 场景下的可行性是充分且客观的。

### 6. 主要结论与发现
*   **延迟表现**：CXL 的检索延迟远低于 RDMA，且非常接近本地 DRAM。在 Batch Size 增加时，CXL 依然能保持稳定的低延迟。
*   **推理性能**：集成 CXL Engram 池后，端到端吞吐量与纯 DRAM 方案相比损失极小（约 1% 左右），证明了 CXL 卸载方案的透明性。
*   **成本效益**：随着集群规模和 Engram 表容量增加，CXL 共享池方案比为每个节点配置大容量本地 DRAM 节省大量成本（例如 400B 模型在 16 节点下可节省约 16.6 万美元）。

### 7. 优点
*   **特性契合**：精准捕捉了 Engram 稀疏访问模式与 CXL 硬件特性的匹配点。
*   **系统级优化**：实现了 GPU 直接访问 CXL 内存的 P2P 传输优化，减少了数据搬运开销。
*   **实用性强**：在主流推理框架 SGLang 中完成了集成，并给出了详尽的成本分析，具有很强的工程参考价值。

### 8. 不足与局限
*   **模型真实性**：由于缺乏开源的 Engram 模型权重，实验采用 Qwen3 模拟，可能无法完全反映真实模型中 Engram 层与 Attention 层的复杂交互。
*   **集群规模有限**：实验仅在 2 个节点上完成，在大规模 CXL 交换网络（如数十个节点）中的竞争和拥塞情况尚未验证。
*   **硬件门槛**：CXL 2.0/3.0 基础设施目前仍处于早期部署阶段，相关硬件的普及度和兼容性可能限制该方案的即时应用。

（完）
