---
title: "Think While Watching: Online Streaming Segment-Level Memory for Multi-Turn Video Reasoning in Multimodal Large Language Models"
title_zh: 边看边想：多模态大语言模型中用于多轮视频推理的在线流式片段级记忆
authors: "Lu Wang, Zhuoran Jin, Yupu Hao, Yubo Chen, Kang Liu, Yulong Ao, Jun Zhao"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11896v1"
tags: ["query:agimem"]
score: 9.0
evidence: 用于长程依赖建模的在线流式片段级记忆
tldr: 针对多模态大模型在在线流式视频理解中存在的感知与生成无法并行以及长程记忆衰减问题，本文提出了“边看边想”（Think While Watching）框架。该框架通过保留连续的分段级内存，结合三阶段多轮思维链数据集和流式因果掩码，实现了高效的多轮视频推理。实验表明，该方法在StreamingBench等基准测试上显著提升了准确率，并大幅减少了多轮对话中的Token消耗。
selection_source: fresh_fetch
motivation: 现有的流式视频理解方法通常采用交替感知与生成的模式，导致无法并行处理且长程依赖建模能力较弱。
method: 提出一种锚定内存的流式推理框架，利用分段级内存、流式因果掩码及位置编码，并配合三阶段多轮CoT训练策略。
result: "在StreamingBench和OVO-Bench上分别提升了2.6%和3.79%的准确率，且在多轮对话中减少了56%的输出Token。"
conclusion: 该框架有效解决了在线视频流中的实时推理与长程记忆保持问题，为多轮交互式视频理解提供了高效方案。
---

## 摘要
多模态大语言模型（MLLMs）在离线视频理解方面表现出强大的性能，但大多数模型局限于离线推理或在线推理能力较弱，导致难以在持续到达的视频流上进行多轮交互。现有的流式方法通常采用交替的感知-生成范式，这阻碍了感知与生成的并发进行，并随着视频流的增长导致早期记忆衰减，从而损害了长程依赖建模。我们提出了“边看边想”（Think While Watching），这是一种以记忆为锚点的流式视频推理框架，能够在多轮交互过程中保留连续的片段级记忆。我们构建了一个三阶段、多轮的思维链数据集，并采用了阶段匹配的训练策略，同时通过片段级流式因果掩码和流式位置编码来强制执行严格的因果性。在推理过程中，我们引入了一种高效的流水线，使“观看”与“思考”重叠，并自适应地选择最佳的注意力后端。在单轮和多轮流式输入协议下，我们的方法均取得了优异的结果。基于 Qwen3-VL，该方法在 StreamingBench 上的单轮准确率提升了 2.6%，在 OVO-Bench 上提升了 3.79%。在多轮设置下，它在保持性能的同时将输出 token 减少了 56%。代码可在以下地址获取：https://github.com/wl666hhh/Think_While_Watching/

## Abstract
Multimodal large language models (MLLMs) have shown strong performance on offline video understanding, but most are limited to offline inference or have weak online reasoning, making multi-turn interaction over continuously arriving video streams difficult. Existing streaming methods typically use an interleaved perception-generation paradigm, which prevents concurrent perception and generation and leads to early memory decay as streams grow, hurting long-range dependency modeling. We propose Think While Watching, a memory-anchored streaming video reasoning framework that preserves continuous segment-level memory during multi-turn interaction. We build a three-stage, multi-round chain-of-thought dataset and adopt a stage-matched training strategy, while enforcing strict causality through a segment-level streaming causal mask and streaming positional encoding. During inference, we introduce an efficient pipeline that overlaps watching and thinking and adaptively selects the best attention backend. Under both single-round and multi-round streaming input protocols, our method achieves strong results. Built on Qwen3-VL, it improves single-round accuracy by 2.6% on StreamingBench and by 3.79% on OVO-Bench. In the multi-round setting, it maintains performance while reducing output tokens by 56%. Code is available at: https://github.com/wl666hhh/Think_While_Watching/

---

## 论文详细总结（自动生成）

以下是对论文《Think While Watching: Online Streaming Segment-Level Memory for Multi-Turn Video Reasoning in Multimodal Large Language Models》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的多模态大语言模型（MLLM）在离线视频理解上表现优异，但在处理实时流式视频（如直播、监控、机器人助手）时面临挑战。
*   **核心痛点**：
    1.  **记忆侵蚀（Memory Erosion）**：现有的“交替感知-生成”范式在长视频流中会导致早期视觉线索或对话历史被遗忘，尤其在多轮交互中表现极差。
    2.  **串行化瓶颈（Serialization Bottleneck）**：模型在生成文本时必须停止视频摄取，导致处理延迟随轮数增加而爆炸式增长。
*   **研究目标**：提出一种能够“边看边想”的框架，实现感知与生成的并行，并维持长期的片段级记忆。

### 2. 方法论
*   **核心思想**：引入**片段级记忆笔记（Segment-level Memory Notes）**作为持久状态，并解耦视频摄取与文本解码过程。
*   **关键技术细节**：
    *   **片段级记忆笔记**：模型为每个视频片段生成简短的文本笔记并存入记忆库。回答问题时，通过注意力机制隐式检索相关笔记。
    *   **流式因果掩码（Streaming Causal Mask）**：在片段级别强制执行严格的因果性，确保生成的笔记或答案只能访问当前及过去的视频片段，防止信息泄露。
    *   **流式位置编码**：基于 MRoPE 改进，为输入流和输出流分配独立的位置索引，使得新到达的视频片段无需等待生成结束即可确定位置。
    *   **双 KV 缓存（Dual KV Cache）推理**：借鉴操作系统进程调度思想，通过解耦 KV 缓存实现视频摄取（Watching）与文本解码（Thinking）的并行处理。
*   **三阶段训练策略**：
    1.  **阶段 1**：学习编写片段笔记并回答单轮问题。
    2.  **阶段 2**：扩展到多轮对话，训练跨轮次的一致性。
    3.  **阶段 3**：针对长视频进行训练，增强长程记忆、不确定性处理和抗干扰能力。

### 3. 实验设计
*   **数据集与场景**：
    *   **流式基准**：StreamingBench（评估实时、全源、主动理解）、OVO-Bench（评估真实世界在线理解）。
    *   **离线迁移评估**：Video-MME、LV-Bench（验证流式训练对离线长视频理解的增益）。
*   **对比方法**：
    *   **闭源模型**：Gemini 1.5 Pro, GPT-4o。
    *   **开源流式模型**：Flash-VStream, VideoLLM-online, Dispider, StreamAgent 等。
    *   **基座模型**：Qwen3-VL (2B, 4B, 8B) 的 Instruct 和 Thinking 版本。
*   **评估协议**：分为单轮流式（Single-round）和多轮流式（Multi-round）两种协议。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA RTX A6000 (48GB)** GPU。
*   **训练细节**：采用全参数监督微调（Full-parameter SFT），使用 DeepSpeed ZeRO-3 优化器，精度为 bf16。
*   **训练时长**：文中未明确给出总训练时长，但详细列出了学习率（1.0e-5）、Batch Size（128）等超参数。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2B、4B、8B 三种参数规模的基座模型，并在 4 个主流视频理解基准上进行了测试。
*   **消融实验**：针对记忆笔记的有效性、视频帧掩码的影响、片段分割粒度等进行了详细消融。
*   **客观性与公平性**：
    *   对比了多种最新的流式 MLLM 方案。
    *   引入了 **TTFT（首字延迟）** 和 **Token 减少率** 等效率指标，不仅关注准确率，也关注工业部署的实用性。
    *   实验设计涵盖了从短视频到极长视频（3600秒以上）的跨度，具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：在 Qwen3-VL-4B 上，单轮流式准确率在 StreamingBench 提升 2.6%，OVO-Bench 提升 3.79%。
*   **效率飞跃**：在多轮交互中，该方法在不损失准确率的前提下，**减少了 56% 的输出 Token 消耗**。
*   **延迟降低**：相比离线 Batch 模式，流式流水线将 **TTFT 降低了 92.6%**。
*   **长程能力**：通过阶段 3 训练，模型能够有效将注意力从近期片段转移到更远的历史记忆中，缓解了记忆侵蚀问题。

### 7. 优点
*   **架构创新**：通过解耦位置编码和双 KV 缓存，真正实现了“边看边想”的并行化，解决了流式推理的实时性瓶颈。
*   **数据贡献**：构建并开源了首个三阶段、多轮流式思维链（CoT）数据集，填补了该领域的空白。
*   **泛化性**：流式训练不仅提升了在线表现，还意外增强了模型在离线长视频基准（如 Video-MME）上的性能。

### 8. 不足与局限
*   **分割粒度依赖**：实验表明片段分割的长度（如 30s vs 60s）会显著影响准确率与效率的平衡，目前仍需手动设定，缺乏自适应分割机制。
*   **模态覆盖**：目前主要关注视觉流，尚未充分整合音频信号（如语音转录），这在复杂的真实场景中可能限制理解深度。
*   **极端长流挑战**：尽管有记忆笔记，但在极长视频中仍存在细粒度属性丢失和干扰信息导致记忆污染的风险。

（完）
