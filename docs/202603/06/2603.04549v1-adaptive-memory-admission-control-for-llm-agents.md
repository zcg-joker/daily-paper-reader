---
title: Adaptive Memory Admission Control for LLM Agents
title_zh: LLM 智能体的自适应记忆准入控制
authors: "Guilin Zhang, Wei Jiang, Xiejiashan Wang, Aisha Behr, Kai Zhao, Jeffrey Friedman, Xu Chu, Amine Anoun"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04549v1"
tags: ["query:agimem"]
score: 10.0
evidence: 用于长期保留的自适应记忆准入控制
tldr: 针对LLM智能体长期记忆管理中信息冗余、成本高且难以审计的问题，本文提出A-MAC框架。该框架将记忆准入视为结构化决策问题，通过整合未来效用、事实置信度、语义新颖性、时间近因和内容类型先验五个可解释维度，结合轻量级规则与LLM评估，实现了高效且透明的记忆筛选。实验证明，A-MAC在提升记忆质量的同时显著降低了延迟。
selection_source: fresh_fetch
motivation: 现有的LLM智能体记忆系统缺乏对存储内容的有效控制，导致冗余信息堆积或记忆管理成本过高且不可控。
method: 提出A-MAC框架，通过分解记忆价值为五个可解释因子，并结合轻量级特征提取与LLM辅助评估来优化准入策略。
result: "在LoCoMo基准测试中，A-MAC将F1分数提升至0.583，并比现有LLM原生记忆系统降低了31%的延迟。"
conclusion: 显式且可解释的准入控制是构建可扩展且可靠的LLM智能体长期记忆系统的关键设计原则。
---

## 摘要
基于大语言模型（LLM）的智能体日益依赖长期记忆来支持多会话推理和交互，但目前的系统对保留哪些信息几乎没有控制。在实践中，智能体要么积累大量的对话内容（包括幻觉或过时的事实），要么依赖于不透明、完全由 LLM 驱动的记忆策略，而这些策略成本高昂且难以审计。因此，记忆准入在智能体架构中仍然是一个定义模糊且控制薄弱的组件。为了填补这一空白，我们提出了自适应记忆准入控制（A-MAC），这是一个将记忆准入视为结构化决策问题的框架。A-MAC 将记忆价值分解为五个互补且可解释的因素：未来效用、事实置信度、语义新颖性、时间近时性和内容类型先验。该框架将轻量级的基于规则的特征提取与单次 LLM 辅助的效用评估相结合，并通过交叉验证优化来学习领域自适应的准入策略。这种设计实现了对长期记忆透明且高效的控制。在 LoCoMo 基准测试上的实验表明，与最先进的 LLM 原生记忆系统相比，A-MAC 实现了更优的精确率-召回率权衡，将 F1 分数提高到 0.583，同时将延迟降低了 31%。消融实验结果表明，内容类型先验是可靠记忆准入中影响最大的因素。这些发现表明，显式且可解释的准入控制是构建可扩展且可靠的 LLM 智能体记忆的关键设计原则。

## Abstract
LLM-based agents increasingly rely on long-term memory to support multi-session reasoning and interaction, yet current systems provide little control over what information is retained. In practice, agents either accumulate large volumes of conversational content, including hallucinated or obsolete facts, or depend on opaque, fully LLM-driven memory policies that are costly and difficult to audit. As a result, memory admission remains a poorly specified and weakly controlled component in agent architectures. To address this gap, we propose Adaptive Memory Admission Control (A-MAC), a framework that treats memory admission as a structured decision problem. A-MAC decomposes memory value into five complementary and interpretable factors: future utility, factual confidence, semantic novelty, temporal recency, and content type prior. The framework combines lightweight rule-based feature extraction with a single LLM-assisted utility assessment, and learns domain-adaptive admission policies through cross-validated optimization. This design enables transparent and efficient control over long-term memory. Experiments on the LoCoMo benchmark show that A-MAC achieves a superior precision-recall tradeoff, improving F1 to 0.583 while reducing latency by 31% compared to state-of-the-art LLM-native memory systems. Ablation results identify content type prior as the most influential factor for reliable memory admission. These findings demonstrate that explicit and interpretable admission control is a critical design principle for scalable and reliable memory in LLM-based agents.

---

## 论文详细总结（自动生成）

以下是对论文《Adaptive Memory Admission Control for LLM Agents》（LLM 智能体的自适应记忆准入控制）的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：LLM 智能体在长期交互中需要记忆，但现有的记忆管理缺乏有效的“准入控制”（Admission Control）。
*   **研究动机**：
    *   **存储冗余与幻觉**：无差别存储会导致记忆库臃肿、检索延迟增加，并可能引入幻觉或过时信息。
    *   **现有方法局限**：启发式方法（如 MemGPT）依赖手动规则，难以处理复杂决策且无法识别幻觉；LLM 原生方法（如 A-mem）虽然效果好，但计算成本极高、决策过程不透明且难以审计。
    *   **缺乏标准**：记忆准入在当前的智能体架构中仍是一个定义模糊且控制薄弱的环节。

### 2. 方法论：A-MAC 框架
*   **核心思想**：将记忆准入视为一个结构化的决策问题，通过五个可解释的维度对候选记忆进行评分，并利用学习到的策略进行筛选。
*   **关键技术细节**：
    *   **五个评估维度**：
        1.  **未来效用 (Utility, U)**：评估信息对未来任务的潜在价值（通过单次 LLM 调用实现）。
        2.  **事实置信度 (Confidence, C)**：通过 ROUGE-L 衡量候选记忆与原始对话内容的对齐程度，防止幻觉。
        3.  **语义新颖性 (Novelty, N)**：利用 Sentence-BERT 嵌入计算余弦相似度，避免存储重复信息。
        4.  **时间近因 (Recency, R)**：应用指数衰减函数，优先考虑近期信息。
        5.  **内容类型先验 (Type Prior, T)**：基于规则识别信息类型（如用户偏好 vs. 临时状态），赋予不同权重。
    *   **决策公式**：$S(m) = \sum_{i=1}^{5} w_i \cdot F_i(m)$。当总分 $S(m)$ 超过阈值 $\theta$ 时，记忆被准入。
    *   **混合架构**：仅在“效用评估”中使用一次 LLM，其余四个维度均采用轻量级规则或小模型计算，兼顾性能与效率。
    *   **策略学习**：通过 5 折交叉验证和网格搜索，在标注数据上自动优化权重 $w$ 和阈值 $\theta$。

### 3. 实验设计
*   **数据集/基准测试**：使用 **LoCoMo** 基准测试，包含 30 个长对话（涵盖个人助手、技术支持、研究协作等场景），涉及约 1500 条带标注的候选记忆。
*   **对比方法 (Baselines)**：
    *   **Random**：随机准入。
    *   **MemGPT**：基于近因和 LLM 判断重要性的启发式方法。
    *   **MemoryBank**：结合近因、相关性和重要性的线性评分方法。
    *   **A-mem**：当前最先进的（SOTA）LLM 原生记忆系统，生成结构化属性进行匹配。
    *   **Equal Weights**：A-MAC 的等权重版本。

### 4. 资源与算力
*   **模型使用**：使用了 **Qwen 2.5** 作为本地 LLM 进行效用评分，使用 **Sentence-BERT** 生成嵌入。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或总训练时长。但提供了详细的延迟数据：A-MAC 处理单个候选记忆的平均延迟为 2644ms（其中 LLM 推理占 97.6%），比 A-mem 降低了 31%。

### 5. 实验数量与充分性
*   **实验规模**：
    *   主实验对比了 6 种方法在精确率、召回率、F1 和延迟上的表现。
    *   进行了**消融实验**，逐一移除五个维度以验证各自贡献。
    *   进行了**阈值敏感性分析**和**跨领域泛化实验**（个人 vs. 专业领域）。
*   **充分性评价**：实验设计较为全面，通过交叉验证确保了结果的稳健性。消融实验清晰地揭示了“内容类型先验”是影响最大的因子。虽然数据集规模（30 个对话）不算巨大，但对于记忆准入这一细分任务而言，标注密度足以支持其结论。

### 6. 主要结论与发现
*   **性能领先**：A-MAC 的 F1 分数达到 0.583，显著优于 A-mem (0.541) 和 MemoryBank (0.452)。
*   **效率提升**：在保持高召回率的同时，精确率大幅提升，且延迟比 LLM 原生系统降低了 31%。
*   **关键因子**：消融实验证明 **内容类型先验 (Type Prior)** 是最关键的特征，这说明区分信息的本质属性（如长期偏好 vs. 短期对话）对记忆管理至关重要。
*   **可解释性**：线性加权模型允许开发者审计每一条记忆被准入或拒绝的具体原因。

### 7. 优点
*   **平衡性好**：在计算效率（轻量级规则）和语义理解（单次 LLM 调用）之间找到了良好的平衡点。
*   **可解释与可审计**：不同于黑盒 LLM 决策，A-MAC 的五个维度提供了清晰的决策依据。
*   **抗幻觉能力**：显式引入“事实置信度”维度，直接针对智能体记忆中的幻觉传播问题。
*   **自适应性**：通过学习权重，框架可以根据不同应用场景（如个人助手 vs. 技术支持）自动调整准入策略。

### 8. 不足与局限
*   **领域表现差异**：实验显示在“专业领域”的 F1 分数（0.338）明显低于“个人领域”（0.482），说明框架对复杂专业术语和隐含上下文的处理仍有提升空间。
*   **仍依赖 LLM**：虽然减少了调用次数，但效用评估仍依赖 LLM，这在极大规模实时交互中可能仍是瓶颈。
*   **数据集局限**：LoCoMo 虽然是标准基准，但样本量相对较小，未来需在更大规模、更多样化的真实生产环境中验证。

（完）
