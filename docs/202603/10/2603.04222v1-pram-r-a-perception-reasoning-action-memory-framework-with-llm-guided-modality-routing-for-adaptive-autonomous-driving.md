---
title: "PRAM-R: A Perception-Reasoning-Action-Memory Framework with LLM-Guided Modality Routing for Adaptive Autonomous Driving"
title_zh: PRAM-R：一种用于自适应自动驾驶的、具有 LLM 引导模态路由的感知-推理-动作-记忆框架
authors: "Yi Zhang, Xian Zhang, Saisi Zhao, Yinglei Song, Chengdong Wu, Nenad Petrovic, Alois Knoll"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04222v1"
tags: ["query:agimem"]
score: 9.0
evidence: 用于自动驾驶长期适应的分层记忆模块
tldr: 本研究针对自动驾驶中多模态感知计算开销大的问题，提出了PRAM-R框架。该框架采用异步双环设计，包含快速反应环和慢速决策环，利用大语言模型（LLM）根据环境上下文和传感器状态动态引导模态路由。通过引入分层存储模块确保时间一致性，PRAM-R在nuScenes等数据集上实现了在保持高精度轨迹预测的同时，显著降低传感器模态的使用频率和系统振荡，为高效自适应自动驾驶提供了新方案。
selection_source: fresh_fetch
motivation: 旨在解决多模态感知在自动驾驶中因所有传感器持续开启而导致的冗余计算开销问题。
method: 提出一种包含感知-推理-动作-存储的异步双环框架，利用LLM进行模态路由选择并结合分层存储实现长期自适应。
result: "实验表明该方法在减少6.22%模态使用的前提下保持了与全模态基准相当的精度，并减少了87.2%的路由振荡。"
conclusion: 证明了结合LLM引导的模态路由与分层存储架构能有效提升自动驾驶系统的感知效率与环境适应性。
---

## 摘要
多模态感知实现了鲁棒的自动驾驶，但当所有传感器都保持激活状态时，会产生不必要的计算开销。本文提出了 PRAM-R，这是一个统一的感知-推理-动作-记忆（Perception-Reasoning-Action-Memory）框架，具有用于自适应自动驾驶的 LLM 引导模态路由。PRAM-R 采用异步双环设计：一个用于感知和控制的快速反应环，以及一个用于推理驱动的模态选择和记忆更新的慢速审议环。LLM 路由器利用环境上下文和传感器诊断来选择模态并分配权重，而分层记忆模块则保持时间一致性并支持长期自适应。我们进行了两个阶段的评估：(1) 用于稳定性分析的合成压力测试，以及 (2) 在 nuScenes 数据集上的真实世界验证。合成压力测试证实，通过基于滞后的稳定化方法，路由振荡减少了 87.2%。在 nuScenes 上的真实世界验证显示，在复杂城市场景中，在保持与全模态基准相当的轨迹精度的同时，模态减少了 6.22%，记忆召回率为 20%。我们的工作表明，具有分层记忆的 LLM 增强架构在自动驾驶中实现了高效、自适应的多模态感知。

## Abstract
Multimodal perception enables robust autonomous driving but incurs unnecessary computational cost when all sensors remain active. This paper presents PRAM-R, a unified Perception-Reasoning-Action-Memory framework with LLM-Guided Modality Routing for adaptive autonomous driving. PRAM-R adopts an asynchronous dual-loop design: a fast reactive loop for perception and control, and a slow deliberative loop for reasoning-driven modality selection and memory updates. An LLM router selects and weights modalities using environmental context and sensor diagnostics, while a hierarchical memory module preserves temporal consistency and supports long-term adaptation. We conduct a two-stage evaluation: (1) synthetic stress tests for stability analysis and (2) real-world validation on the nuScenes dataset. Synthetic stress tests confirm 87.2% reduction in routing oscillations via hysteresis-based stabilization. Real-world validation on nuScenes shows 6.22% modality reduction with 20% memory recall while maintaining comparable trajectory accuracy to full-modality baselines in complex urban scenarios. Our work demonstrates that LLM-augmented architectures with hierarchical memory achieve efficient, adaptive multimodal perception in autonomous driving.

---

## 论文详细总结（自动生成）

以下是对论文《PRAM-R: A Perception-Reasoning-Action-Memory Framework with LLM-Guided Modality Routing for Adaptive Autonomous Driving》的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：自动驾驶依赖多模态感知（摄像头、激光雷达、雷达）来确保鲁棒性，但始终开启所有传感器会导致巨大的计算开销、延迟以及能源浪费。
*   **核心问题**：现有的多模态融合方法多为静态或基于启发式规则，缺乏高层推理能力，难以根据环境上下文（如天气、光照、传感器故障）动态调整传感器组合。
*   **整体含义**：论文提出了 PRAM-R 框架，旨在利用大语言模型（LLM）的推理能力，实现“按需分配”的模态路由（Modality Routing），在保证感知精度的同时优化计算效率。

### 2. 方法论
*   **核心思想**：构建一个包含感知、推理、动作、记忆四个模块的异步双环架构，通过 LLM 引导的路由机制动态选择和加权传感器模态。
*   **关键技术细节**：
    *   **异步双环设计**：
        *   **快速反应环（Fast Reactive Loop）**：高频运行，负责实时感知映射和车辆控制。
        *   **慢速审议环（Slow Deliberative Loop）**：低频运行，利用 LLM 进行复杂的模态路由决策、语义抽象和记忆更新。
    *   **LLM 引导的模态路由**：使用 **Qwen3-VL-8B** 作为路由器。它接收传感器诊断指标（如图像亮度、点云密度、雷达信噪比）和环境上下文，输出各模态的激活掩码和可靠性权重。
    *   **稳定性机制**：引入**滞后（Hysteresis）处理**和**指数移动平均（EMA）**，防止因环境微小波动导致的传感器频繁开关（路由振荡）。
    *   **分层记忆模块**：分为感知（短期）、推理（中期）、动作（执行记录）和知识库（长期经验）四层，用于缓存路由决策、保持时间一致性并支持长期自适应。

### 3. 实验设计
*   **数据集与场景**：
    1.  **合成压力测试**：模拟极端情况（如传感器逐渐退化、突发故障、高频噪声），验证路由的稳定性。
    2.  **真实世界验证**：使用 **nuScenes** 数据集，选取 10 个具有代表性的复杂城市驾驶场景。
*   **Benchmark 与对比方法**：
    *   **基准方法**：**PLA**（感知-语言-动作框架），其特点是全模态激活且无记忆机制。
    *   **对比维度**：路由效率（RE）、路由一致性（RC）、稳定性指数（RSI）、记忆召回率（MRR）以及轨迹预测精度（ADE/FDE）。

### 4. 资源与算力
*   **模型规模**：使用了 Qwen3-VL-8B 模型。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练/推理的总时长。但作者在结论中提到，8B 规模的模型在嵌入式硬件上仍存在延迟和内存开销挑战，暗示了对算力有一定要求。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了针对稳定性的消融实验（有无滞后机制）。
    *   在 nuScenes 的 10 个典型场景上进行了聚合分析。
    *   进行了关于记忆模块和双环架构的消融实验。
*   **充分性评价**：实验设计较为科学，涵盖了从合成压力到真实场景的过渡，且通过消融实验验证了各组件的有效性。但由于 nuScenes 场景样本量较小（10个场景），其在更大规模数据集上的泛化性仍有待进一步验证。

### 6. 主要结论与发现
*   **稳定性提升**：通过滞后稳定化机制，路由振荡减少了 **87.2%**。
*   **效率优化**：在 nuScenes 验证中，实现了 **6.22%** 的模态使用减少，同时保持了约 **20%** 的记忆召回率。
*   **性能保持**：在减少传感器负载的情况下，轨迹预测精度（ADE/FDE）与全模态激活的基准方法相当，证明了路由决策的有效性。
*   **自适应性**：系统能根据场景复杂度自动调整，例如在简单场景关闭激光雷达，在复杂场景激活全传感器。

### 7. 优点
*   **架构创新**：异步双环设计巧妙解决了 LLM 推理慢与自动驾驶实时性要求高之间的矛盾。
*   **稳定性保障**：引入滞后和 EMA 机制，解决了基于 LLM 决策时常见的输出波动问题。
*   **分层记忆**：记忆模块的设计不仅提高了推理效率（通过缓存），还增强了系统在长时序列中的一致性。

### 8. 不足与局限
*   **实时性挑战**：尽管采用了双环设计，但 8B 参数量的模型在车载嵌入式设备上的实时推理延迟仍是实际部署的瓶颈。
*   **实验覆盖面**：实验主要集中在 10 个 nuScenes 场景，缺乏更大规模、更多样化（如不同城市、极端天气）的广泛测试。
*   **闭环验证缺失**：目前的评估主要基于离线数据集（ADE/FDE），缺乏在真实车辆或高保真模拟器中的闭环控制验证。
*   **记忆模块深度分析不足**：文中对记忆模块如何具体影响下游控制的量化分析相对较少。

（完）
