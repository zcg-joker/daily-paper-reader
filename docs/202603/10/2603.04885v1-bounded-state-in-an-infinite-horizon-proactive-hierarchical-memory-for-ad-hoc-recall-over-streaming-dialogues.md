---
title: "Bounded State in an Infinite Horizon: Proactive Hierarchical Memory for Ad-Hoc Recall over Streaming Dialogues"
title_zh: 无限时界中的有界状态：面向流式对话即时召回的主动分层记忆
authors: "Bingbing Wang, Jing Li, Ruifeng Xu"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04885v1"
tags: ["query:agimem"]
score: 9.0
evidence: 流式记忆评估基准
tldr: 针对无限流式对话中的记忆召回挑战，本文提出了STEM-Bench基准测试，揭示了现有方法在保真度与效率间的矛盾。为此，作者开发了ProStream框架，通过主动式分层记忆和自适应时空优化，在保持有限状态的同时实现了高效、准确的即时记忆召回，显著提升了流式对话系统的性能与响应速度。
selection_source: fresh_fetch
motivation: 现有的记忆机制难以在无限流式对话中平衡即时召回的保真度与推理效率，导致长程对话处理能力受限。
method: 提出了ProStream框架，通过多粒度蒸馏的主动分层记忆和自适应时空优化技术，动态优化记忆保留并维持有界知识状态。
result: 实验结果显示，ProStream在STEM-Bench基准测试中的准确率和推理效率均显著优于现有的检索式和全上下文基准方法。
conclusion: 该研究通过主动式分层记忆管理，成功解决了流式对话中的保真度-效率困境，为无限时域下的智能对话系统提供了高效方案。
---

## 摘要
现实世界的对话通常以无限流的形式展开。因此，它需要有界状态记忆机制在无限时界内运行。然而，现有的“先读后思”记忆模式与该场景根本不匹配，因为它无法在流展开过程中支持即时（ad-hoc）记忆召回。为了探索这一挑战，我们推出了 STEM-Bench，这是首个用于记忆流式评估（STreaming Evaluation of Memory）的基准测试。它包含对话流中的 1.4 万余个问答对，旨在评估无限时界约束下的感知保真度、时间推理和全局意识。在 STEM-Bench 上的初步分析表明存在一个关键的“保真度-效率困境”：基于检索的方法使用片段上下文，而全上下文模型则会产生无限的延迟。为解决这一问题，我们提出了 ProStream，一种面向流式对话的主动分层记忆框架。它通过多粒度蒸馏对连续流进行推理，从而实现按需的即时记忆召回。此外，它采用自适应时空优化，根据预期效用动态优化记忆保留。它在不牺牲推理保真度的前提下，实现了有界知识状态，从而降低了推理延迟。实验表明，ProStream 在准确性和效率方面均优于基准模型。

## Abstract
Real-world dialogue usually unfolds as an infinite stream. It thus requires bounded-state memory mechanisms to operate within an infinite horizon. However, existing read-then-think memory is fundamentally misaligned with this setting, as it cannot support ad-hoc memory recall while streams unfold. To explore this challenge, we introduce \textbf{STEM-Bench}, the first benchmark for \textbf{ST}reaming \textbf{E}valuation of \textbf{M}emory. It comprises over 14K QA pairs in dialogue streams that assess perception fidelity, temporal reasoning, and global awareness under infinite-horizon constraints. The preliminary analysis on STEM-Bench indicates a critical \textit{fidelity-efficiency dilemma}: retrieval-based methods use fragment context, while full-context models incur unbounded latency. To resolve this, we propose \textbf{ProStream}, a proactive hierarchical memory framework for streaming dialogues. It enables ad-hoc memory recall on demand by reasoning over continuous streams with multi-granular distillation. Moreover, it employs Adaptive Spatiotemporal Optimization to dynamically optimize retention based on expected utility. It enables a bounded knowledge state for lower inference latency without sacrificing reasoning fidelity. Experiments show that ProStream outperforms baselines in both accuracy and efficiency.

---

## 论文详细总结（自动生成）

这篇论文由香港理工大学和哈尔滨工业大学（深圳）的研究团队合作完成，旨在解决大语言模型（LLM）在处理长程、无限流式对话时的记忆挑战。

以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：现实世界的对话通常是持续不断的“无限流”，而现有的长短期记忆机制大多遵循“先读后思”（read-then-think）模式，假设上下文是静态且完全可访问的。
*   **核心挑战**：在流式对话中，系统需要支持“即时召回”（Ad-Hoc Recall），即在对话进行的任何时刻根据需求检索历史。
*   **保真度-效率困境**：
    *   **检索式方法（如RAG）**：虽然延迟低，但会将上下文碎片化，导致逻辑推理能力下降。
    *   **全上下文模型**：虽然保真度高，但随着对话增长，推理延迟呈指数级增加，且冗余信息会干扰推理。
*   **研究目标**：在无限的时界（Infinite Horizon）中维持一个有界的状态（Bounded State），实现高保真且低延迟的记忆召回。

### 2. 方法论：ProStream 框架
ProStream 将记忆维护建模为一个有界状态演化过程，核心包含四个阶段：

1.  **主动语义流感知（Proactive Semantic Stream Perception）**：
    *   使用 Whisper ASR 将语音转为文本，并结合说话人身份。
    *   通过计算语义漂移阈值（$\tau_{drift}$）将连续流切分为离散的“语义块”，避免上下文碎片化。
2.  **分层多粒度蒸馏（Hierarchical Multi-Granular Distillation）**：
    *   将语义块蒸馏为三层树状拓扑：**场景（Scene）**、**事件（Event）**和**原子记忆单元（AMU）**。
    *   利用 LLM 进行摘要生成，并使用 GLiNER 提取实体关系三元组作为原子单元。
3.  **自适应时空优化（Adaptive Spatiotemporal Optimization）**：
    *   **效用函数**：基于认知心理学的记忆理性分析，结合**访问频率**和**时间衰减（近因）**计算节点效用值。
    *   **在线背包问题优化**：在严格的 Token 预算（$T_{max}$）下，通过贪婪策略进行“最小遗憾剪枝”、语义合并和级联抽象，确保记忆状态不随时间无限膨胀。
4.  **概率证据增强生成（Probabilistic Evidence-Grounded Reasoning）**：
    *   采用自顶向下的检索策略，结合短期感知缓存（STSB）和分层记忆树，为查询提供结构化的证据支持。

### 3. 实验设计
*   **基准测试（STEM-Bench）**：
    *   **数据集**：基于 LongDialQA 改造，涵盖《生活大爆炸》、《老友记》和《办公室》三个领域的对话。
    *   **规模**：包含 1.4 万个 QA 对、5.5 万条话语和 6000 多个语义主题。
    *   **评估维度**：高保真感知（HFP）、结构化逻辑推理（SLR）、动态全局意识（DGA）。
*   **对比方法**：
    *   **传统基准**：标准 RAG、全上下文（Full Context）、滚动摘要。
    *   **先进结构化方法**：GraphRAG、HippoRAG、RAPTOR。
    *   **智能体记忆系统**：MemoRAG、A-Mem、MemGAS。
*   **评估指标**：BLEU-4、ROUGE-L、BERTScore、关键实体匹配（KEM）、证据相似度，以及使用 Gemini-2.5-Pro 作为专家评委。

### 4. 资源与算力
*   **硬件环境**：文中提到实验在 NVIDIA GPU 上运行，并使用了 PyTorch 内存优化技术。
*   **模型细节**：
    *   **转录**：Whisper large-v3。
    *   **嵌入**：ms-marco-MiniLM-L6-v2 等。
    *   **蒸馏/生成**：LaMini-T5-738M 用于摘要，Qwen 系列（3B, 7B, 14B）作为推理骨干。
*   **说明**：论文未明确给出具体的 GPU 数量和总训练/推理时长，但强调了其方法具有 $O(1)$ 的恒定时间复杂度。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同领域的对话数据集上进行了大规模测试，样本量超过 1.4 万。
*   **实验类型**：
    *   **主实验**：对比了 10 种主流记忆机制。
    *   **消融实验**：验证了短期缓存、分层树和挂起缓冲区的作用。
    *   **定量分析**：探讨了模型规模（3B/7B/14B）和上下文复杂度（熵）对性能的影响。
*   **充分性评价**：实验设计非常全面，不仅对比了准确率，还重点分析了推理延迟随对话轮数的变化曲线，客观地展示了该方法在长程对话中的稳定性。

### 6. 主要结论与发现
*   **性能领先**：ProStream 在所有维度上均显著优于 RAG 和其他结构化记忆方法。
*   **超越全上下文**：在某些任务中，ProStream 的表现甚至超过了全上下文模型，证明了主动蒸馏能有效过滤噪声。
*   **效率优势**：ProStream 成功将推理延迟与流长度解耦，实现了恒定的推理时间，解决了延迟无限增长的问题。
*   **规模效应**：模型规模越大，处理结构化记忆的能力越强，ProStream 带来的增益越明显。

### 7. 优点与亮点
*   **首创性**：提出了首个针对流式对话记忆评估的基准测试 STEM-Bench。
*   **理论支撑**：将记忆管理形式化为“带衰减值的在线背包问题”，并给出了数学证明。
*   **主动性**：从被动检索转向主动的记忆组织和动态优化，更符合人类的记忆机制。
*   **实用性**：通过有界状态设计，使 LLM 能够真正应用于无限时长的实时对话场景。

### 8. 不足与局限
*   **指令-事实冲突**：在错误分析中发现，如果用户提问中包含错误的前提，模型有时会为了顺应指令而忽略记忆中的事实（幻觉）。
*   **语义噪声干扰**：在极高密度的对话中，细微的语义线索（如填字游戏的小细节）可能会被主要的语义流淹没。
*   **计算开销**：虽然推理延迟低，但在流处理过程中进行实时蒸馏和拓扑维护需要额外的后台计算资源。
*   **应用限制**：目前主要针对文本/语音对话，尚未扩展到多模态流（如视频流）场景。

（完）
