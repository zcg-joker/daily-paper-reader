---
title: "Memex(RL): Scaling Long-Horizon LLM Agents via Indexed Experience Memory"
title_zh: Memex(RL)：通过索引经验记忆扩展长程 LLM 智能体
authors: "Zhenting Wang, Huancheng Chen, Jiayun Wang, Wei Wei"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04257v1"
tags: ["query:agimem"]
score: 9.0
evidence: 索引经验记忆机制，使长程 LLM 智能体突破上下文窗口限制
tldr: 针对大语言模型智能体在长程任务中受限于上下文窗口的问题，本文提出 Memex 机制。该机制通过索引化经验记忆，将简洁摘要与稳定索引保留在工作上下文中，而将完整交互存入外部数据库。配合 MemexRL 强化学习框架，智能体能自主学习总结、归档与检索策略。实验证明，Memex 在显著减少上下文占用的同时，提升了长程任务的成功率。
selection_source: fresh_fetch
motivation: 现有的上下文压缩方法（如截断或摘要）会导致原始证据丢失，限制了智能体在长程任务中的表现。
method: 提出 Memex 索引记忆机制，结合外部数据库存储完整细节，并利用 MemexRL 框架优化智能体的读写与检索决策。
result: 在挑战性的长程任务中，Memex 智能体在保持较小工作上下文的同时，显著提高了任务成功率。
conclusion: 索引化经验记忆是一种比纯摘要更高效、低损耗的长程记忆方案，能有效扩展智能体的处理能力。
---

## 摘要
大语言模型 (LLM) 智能体在处理长程任务时，从根本上受到有限上下文窗口的限制。随着轨迹的增长，在上下文中保留工具输出和中间推理过程很快变得不可行：工作上下文变得过长，最终超出上下文预算，并且即使远端证据仍然存在，也变得难以利用。现有的解决方案通常通过截断或滚动摘要来缩短上下文，但这些方法从根本上是有损的，因为它们压缩或丢弃了过去的证据本身。我们引入了 Memex，这是一种索引经验记忆机制，它在压缩上下文的同时不丢弃证据。Memex 维护一个由简洁结构化摘要和稳定索引组成的紧凑工作上下文，同时将高保真的底层交互存储在这些索引下的外部经验数据库中。智能体随后可以决定何时解引用索引，并恢复当前子目标所需的精确历史证据。我们利用强化学习框架 MemexRL 优化读写行为，采用针对上下文预算下索引记忆使用情况定制的奖励塑造，使智能体学会总结什么、归档什么、如何索引以及何时检索。与仅使用摘要的方法相比，这产生了一种损耗显著更低的长程记忆形式。我们进一步提供了理论分析，展示了 Memex 循环在保持有效上下文计算随历史增长而有界的同时，通过有界解引用来维持决策质量的潜力。实验表明，在具有挑战性的长程任务中，使用 MemexRL 训练的 Memex 智能体在显著减小工作上下文的同时，提高了任务成功率。

## Abstract
Large language model (LLM) agents are fundamentally bottlenecked by finite context windows on long-horizon tasks. As trajectories grow, retaining tool outputs and intermediate reasoning in-context quickly becomes infeasible: the working context becomes prohibitively long, eventually exceeds the context budget, and makes distant evidence harder to use even when it is still present. Existing solutions typically shorten context through truncation or running summaries, but these methods are fundamentally lossy because they compress or discard past evidence itself. We introduce Memex, an indexed experience memory mechanism that instead compresses context without discarding evidence. Memex maintains a compact working context consisting of concise structured summaries and stable indices, while storing full-fidelity underlying interactions in an external experience database under those indices. The agent can then decide when to dereference an index and recover the exact past evidence needed for the current subgoal. We optimize both write and read behaviors with our reinforcement learning framework MemexRL, using reward shaping tailored to indexed memory usage under a context budget, so the agent learns what to summarize, what to archive, how to index it, and when to retrieve it. This yields a substantially less lossy form of long-horizon memory than summary-only approaches. We further provide a theoretical analysis showing the potential of the Memex loop to preserve decision quality with bounded dereferencing while keeping effective in-context computation bounded as history grows. Empirically, on challenging long-horizon tasks, Memex agent trained with MemexRL improves task success while using a significantly smaller working context.

---

## 论文详细总结（自动生成）

这是一份关于论文《Memex(RL): Scaling Long-Horizon LLM Agents via Indexed Experience Memory》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对的核心问题是 **LLM 智能体在长程任务（Long-horizon tasks）中的上下文瓶颈**。
*   **背景**：随着任务步骤增加，轨迹中积累的工具输出、观察结果和推理过程会迅速填满上下文窗口。
*   **动机**：现有的上下文管理方法（如截断或滚动摘要）是“有损”的，会永久丢失原始证据。而基于语义相似度的检索（RAG）在处理高度相似或结构化的工具调用历史时往往不够精确。
*   **核心含义**：提出一种类似人类“笔记+索引”的机制，将上下文分为**紧凑的工作状态**和**高保真的外部存档**，通过强化学习让智能体自主管理记忆的写入与读取。

### 2. 方法论：核心思想与关键技术
Memex 的核心是**索引经验记忆（Indexed Experience Memory）**和配套的强化学习框架 **MemexRL**。

*   **核心思想**：
    *   **工作上下文（Working Context）**：仅保留系统提示、任务指令、当前的“索引摘要”（包含进度描述和指向外部存储的稳定索引）。
    *   **外部经验存储（External Store）**：以键值对（Key-Value）形式存储完整的工具输出、代码片段或日志。
*   **关键操作**：
    *   `CompressExperience`：将当前冗长的上下文重写为“索引摘要”，并将细节归档。支持“显式编写”和“锚点提取”两种模式。
    *   `ReadExperience(index)`：通过解引用（Dereferencing）精确恢复特定索引下的原始证据。
*   **MemexRL 强化学习框架**：
    *   **算法**：基于 GRPO（群体相对策略优化）算法。
    *   **奖励函数**：$R = R_{task} - P_{context} - P_{redundancy} - P_{format}$。其中包含对上下文溢出、冗余工具调用（鼓励检索而非重复执行）和格式错误的惩罚。
    *   **分段轨迹处理**：将压缩点作为边界分割轨迹，使模型能学习到“当前的压缩决策如何影响几十步后的任务成败”。
    *   **软触发机制**：不强制压缩，而是通过在观察中加入 `ContextStatus` 提示，让智能体自主决定压缩时机。

### 3. 实验设计
*   **实验场景**：使用了修改后的 **ALFWorld**（具身智能体基准测试）。
*   **难度增强（Benchmark 改进）**：
    *   隐藏了所有可用的命令列表，要求智能体通过探索发现。
    *   限制“查看（Look）”动作每集只能使用一次，强制智能体必须依赖记忆检索来获取环境布局。
    *   强制对摘要进行 300 标记（Tokens）的截断，迫使模型必须将细节存入外部索引。
*   **对比方法**：主要对比了 **Memex 智能体在 RL 训练前后的表现**，验证了学习机制的有效性。

### 4. 资源与算力
*   **模型**：使用 Qwen3-30B-A3B-Thinking-2507（MoE 架构，约 30B 参数，激活参数约 3B）。
*   **训练框架**：基于开源的 LLM RL 框架 **Slime**。
*   **算力细节**：
    *   采用了 **INT4 量化推理**和**量化感知训练（QAT）**。
    *   训练参数：学习率 5e-6，Batch Size 32，Group Size 8（每条提示生成 8 个样本）。
    *   **未明确说明**具体的 GPU 型号（如 H100/A100）数量及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：训练集包含 3553 个任务。
*   **充分性评价**：
    *   **理论支撑**：论文提供了两个命题的证明，从理论上论证了 Memex 循环可以在保持上下文有界的同时匹配全上下文最优策略。
    *   **动态分析**：展示了训练过程中成功率、惩罚项以及工具使用频率（压缩次数减少、检索次数增加）的变化曲线。
    *   **局限性**：实验主要集中在修改版的 ALFWorld 上，虽然难度较高，但缺乏在其他领域（如 Web 浏览、软件工程）的横向对比实验。

### 6 主要结论与发现
*   **性能飞跃**：经过 MemexRL 训练后，任务成功率从 **24.2% 提升至 85.6%**。
*   **效率提升**：峰值工作上下文长度从约 1.7 万 Tokens 降至约 0.96 万 Tokens（降幅约 **43%**），接近设定的 8000 Tokens 惩罚阈值。
*   **行为转变**：RL 成功教会了智能体“少压缩、多检索”。智能体学会了在关键节点进行高质量归档，并在需要精确数据（如长字符串 ID）时通过索引取回。

### 7. 优点
*   **无损性**：相比传统的摘要方法，Memex 通过外部存储保留了证据的完整性。
*   **自主性**：将记忆管理视为一种“工具调用”，使智能体能根据任务语义而非硬性字符数来决定管理策略。
*   **可解释性**：索引解引用的过程是显式的，开发者可以清晰地看到智能体在决策时参考了哪一段历史。

### 8. 不足与局限
*   **场景单一**：目前仅在 ALFWorld 这一类具身模拟环境中验证，真实世界复杂任务（如多 API 协作、长文档分析）的泛化能力有待观察。
*   **依赖基础模型**：该方案高度依赖于具有强推理能力（Thinking 模型）的基础模型，对于较弱的模型，可能难以掌握复杂的索引管理逻辑。
*   **检索开销**：虽然节省了上下文，但频繁的 `ReadExperience` 会增加交互轮数，可能在某些实时性要求高的场景下产生延迟。

（完）
