---
title: "MemPO: Self-Memory Policy Optimization for Long-Horizon Agents"
title_zh: MemPO：面向长时程智能体的自记忆策略优化
authors: "Ruoran Li, Xinghua Zhang, Haiyang Yu, Shitong Duan, Xiang Li, Wenxin Xiang, Chonghua Liao, Xudong Guo, Yongbin Li, Jinli Suo"
date: 2026-02-28
pdf: "https://arxiv.org/pdf/2603.00680v1"
tags: ["query:agimem"]
score: 10.0
evidence: 用于记忆管理的自记忆策略优化
tldr: 针对长程智能体在交互中上下文增长导致的性能下降问题，本文提出 MemPO 算法。该方法通过自记忆策略优化，使智能体能自主总结和管理记忆。通过改进基于记忆有效性的信用分配机制，智能体可选择性保留关键信息，在显著降低 Token 消耗的同时提升任务表现，在 F1 分数和效率上均优于现有 SOTA 模型。
selection_source: fresh_fetch
motivation: 现有的外部记忆模块缺乏主动管理能力，且不断增长的上下文会损害长程智能体的性能与稳定性。
method: 提出 MemPO 算法，通过改进基于记忆有效性的信用分配机制，使策略模型能够自主总结并选择性保留关键记忆信息。
result: "实验显示 MemPO 相比基准模型 F1 分数提升 25.98%，相比 SOTA 提升 7.1%，同时减少了约 70% 的 Token 使用量。"
conclusion: MemPO 有效解决了长程任务中的上下文冗余问题，实现了记忆管理与任务目标的对齐，显著提升了智能体的效率与性能。
---

## 摘要
长时程智能体在与环境交互过程中面临上下文规模不断增长的挑战，这会导致性能和稳定性下降。现有方法通常引入外部记忆模块并从存储的记忆中检索相关信息，这阻碍了模型自身主动管理其记忆内容并使其与智能体的总体任务目标保持一致。为了解决这些局限性，我们提出了自记忆策略优化算法（MemPO），该算法使智能体（策略模型）能够在与环境交互期间自主地总结和管理其记忆。通过改进基于记忆有效性的信用分配机制，策略模型可以有选择地保留关键信息，在保持任务性能的同时显著降低 Token 消耗。大量的实验和分析证实，MemPO 相比基础模型实现了 25.98% 的 F1 分数绝对增益，相比之前的 SOTA 基准实现了 7.1% 的增益，同时将 Token 使用量分别减少了 67.58% 和 73.12%。

## Abstract
Long-horizon agents face the challenge of growing context size during interaction with environment, which degrades the performance and stability. Existing methods typically introduce the external memory module and look up the relevant information from the stored memory, which prevents the model itself from proactively managing its memory content and aligning with the agent's overarching task objectives. To address these limitations, we propose the self-memory policy optimization algorithm (MemPO), which enables the agent (policy model) to autonomously summarize and manage their memory during interaction with environment. By improving the credit assignment mechanism based on memory effectiveness, the policy model can selectively retain crucial information, significantly reducing token consumption while preserving task performance. Extensive experiments and analyses confirm that MemPO achieves absolute F1 score gains of 25.98% over the base model and 7.1% over the previous SOTA baseline, while reducing token usage by 67.58% and 73.12%.

---

## 论文详细总结（自动生成）

这是一份关于论文《MemPO: Self-Memory Policy Optimization for Long-Horizon Agents》的结构化深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
长时程（Long-horizon）智能体在解决复杂任务时，需要与环境进行多轮交互。传统的 **ReAct 模式**会导致上下文随交互轮数线性增长，带来三大问题：
*   **性能下降**：长上下文引发“迷失在中间（Lost in the Middle）”现象，降低推理准确性。
*   **资源消耗**：Token 成本随轮数激增，限制了大规模应用。
*   **窗口限制**：LLM 的上下文窗口上限限制了交互的总轮数。

现有的外部记忆（如 RAG）通常是**被动检索**，记忆管理与智能体的最终任务目标脱节。本文旨在将记忆管理转化为智能体的**主动能力**，通过强化学习让模型学会自主总结、保留关键信息并压缩上下文。

### 2. 方法论：核心思想与关键技术
论文提出了 **MemPO（自记忆策略优化）** 算法，其核心思想是将记忆管理内化为智能体的一种动作，并利用强化学习进行端到端优化。

*   **动作范式**：智能体在每一步输出包含四个部分：`<mem>`（自主总结历史）、`<think>`（思维链推理）、`<tool_call>`（工具调用）和 `<information>`（环境反馈）。
*   **推理优化**：在推理时，智能体不再输入全部历史，而是仅保留上一步生成的 `<mem>` 内容作为上下文，实现极高的压缩率。
*   **双重奖励信用分配机制**：
    1.  **轨迹级奖励（Trajectory-level Advantage, $A^T$）**：基于最终答案的正确性和格式规范性给予稀疏奖励。
    2.  **记忆级奖励（Informative Memory Advantage, $A^M$）**：这是本文的核心创新。利用**条件概率**衡量记忆的质量。具体而言，计算“给定当前记忆内容时，生成正确答案的后验概率”，并减去一个偏置项（前序步骤的概率）。如果生成的记忆能显著提高模型对正确答案的“信心”，则给予更高奖励。
*   **策略优化**：基于 **GRPO**（群组相对策略优化）框架，通过组内比较来估计优势函数，避免了复杂的价值网络建模，同时结合 KL 散度约束防止策略坍塌。

### 3. 实验设计
*   **数据集与场景**：
    *   **多目标任务**：基于 HotpotQA 和 NQ 合成，涵盖 2 到 10 个目标的复杂查询，分为本地 Wiki 搜索和在线 Web 搜索环境。
    *   **单目标 QA**：包括 NQ、TriviaQA、PopQA、Musique 等 7 个标准数据集。
    *   **深度研究任务**：GAIA、Frames、WebWalkerQA（用于测试 OOD 泛化能力）。
*   **对比方法（Baselines）**：
    *   基础模型：Qwen2.5-7B (ReAct)。
    *   强化学习基准：ReSearch、DeepResearcher、GRPO (无记忆版)。
    *   记忆相关基准：MEM1（RL 记忆模型）、A-MEM（基于 RAG 的记忆模型）。

### 4. 资源与算力
*   **模型基座**：统一使用 Qwen2.5-7B。
*   **训练细节**：
    *   **SFT 阶段**：使用 GPT-4 生成的 10k 条轨迹进行 1 epoch 的微调，以对齐格式。
    *   **RL 阶段**：Rollout 组大小（N）为 16，Batch Size 为 128，学习率为 1e-6。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号（如 A100 或 H100）及总训练时长，但从参数量和 Batch Size 来看，属于中等规模的学术训练量。

### 5. 实验数量与充分性
*   **实验规模**：论文在 15 个不同的数据集/子任务上进行了测试，涵盖了从简单 QA 到极长程的 Web 搜索任务。
*   **消融实验**：对比了有无记忆奖励设计的影响，以及不同上下文保留策略（保留 1 步、3 步或全上下文）的效果。
*   **分析深度**：通过统计条件概率分布图，定量证明了 MemPO 确实提升了记忆中的信息密度。
*   **客观性**：实验同时在本地静态库和动态 Web 环境下进行，证明了方法的鲁棒性。

### 6. 主要结论与发现
*   **性能领先**：在多目标长程任务中，MemPO 的 F1 分数比基座模型提升 25.98%，比之前的 SOTA（MEM1）提升 7.1%。
*   **极致压缩**：在 10 目标任务中，Token 消耗仅为 ReSearch 的约 1/3，峰值 Token 占用仅为 1/5。
*   **解决注意力稀释**：实验发现，在长程任务中，保留过多的上下文反而会导致性能下降，而 MemPO 的单步记忆模式在长程场景下表现最稳健。
*   **泛化能力**：在未见过的 OOD 任务（如 GAIA）上，MemPO 表现出了与专门针对 Web 训练的模型相当的竞争力。

### 7. 优点（亮点）
*   **内生性**：将记忆管理从“外部插件”变为“内在技能”，实现了记忆与任务目标的端到端对齐。
*   **奖励设计巧妙**：利用条件概率差值作为记忆奖励，解决了长程任务中信用分配（Credit Assignment）难的问题。
*   **高效率**：显著降低了推理成本，解决了长程智能体落地的核心痛点。

### 8. 不足与局限
*   **状态偏差风险**：由于不同步骤的工具调用反馈差异巨大，计算组内优势时可能存在状态不均等导致的偏差（尽管论文引入了偏置项 $\epsilon$ 来缓解）。
*   **任务复杂度限制**：目前的实验主要集中在信息检索和 QA 类任务，对于需要极高逻辑严密性（如超长代码工程）的场景，单步记忆是否会丢失细节尚待验证。
*   **算力细节缺失**：未公布具体训练资源消耗，不便于其他研究者评估复现成本。

（完）
