---
title: "ParamMem: Augmenting Language Agents with Parametric Reflective Memory"
title_zh: ParamMem：利用参数化反思记忆增强语言智能体
authors: "Tianjun Yao, Yongqiang Chen, Yujia Zheng, Pan Li, Zhiqiang Shen, Kun Zhang"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.23320v1"
tags: ["query:agimem"]
score: 9.0
evidence: 将反思模式编码进模型参数的参数化记忆模块
tldr: 针对语言智能体在自我反思中容易产生重复输出、限制推理性能的问题，本文提出ParamMem。这是一种参数化反射记忆模块，通过将跨样本反射模式编码进模型参数，实现多样化的反射生成。基于此构建的ParamAgent框架结合了情节和跨样本记忆，在代码、数学和问答任务上显著超越现有基准，并展现出强大的自改进和跨模型迁移能力。
selection_source: fresh_fetch
motivation: 现有的自我反思机制常因输出重复而限制性能，且研究发现反射多样性与任务成功率呈正相关。
method: 提出ParamMem模块，将跨样本反射模式参数化，并构建集成多种记忆类型的ParamAgent框架。
result: 在代码生成、数学推理和多跳问答等任务上取得SOTA性能，并证明了样本效率和弱到强的迁移能力。
conclusion: ParamMem是增强语言智能体性能的有效组件，支持无需外部强模型的自我改进。
---

## 摘要
自我反思使语言智能体能够迭代地优化解决方案，但往往会产生重复的输出，从而限制了推理性能。最近的研究尝试通过各种方法解决这一局限性，其中增加反思多样性已显示出良好的前景。我们的实证分析揭示了反思多样性与任务成功率之间存在强正相关，这进一步激发了对多样化反思信号的需求。我们引入了 ParamMem，这是一种参数化记忆模块，它将跨样本的反思模式编码到模型参数中，通过温度控制采样实现多样化的反思生成。基于该模块，我们提出了 ParamAgent，这是一个基于反思的智能体框架，它将参数化记忆与情境记忆和跨样本记忆相结合。在代码生成、数学推理和多跳问答方面的广泛实验表明，该方法相较于最先进的基准模型取得了持续的改进。进一步的分析表明，ParamMem 具有样本效率，能够实现跨模型规模的从弱到强的迁移，并支持在不依赖更强外部模型的情况下实现自我提升，凸显了 ParamMem 作为增强语言智能体有效组件的潜力。

## Abstract
Self-reflection enables language agents to iteratively refine solutions, yet often produces repetitive outputs that limit reasoning performance. Recent studies have attempted to address this limitation through various approaches, among which increasing reflective diversity has shown promise. Our empirical analysis reveals a strong positive correlation between reflective diversity and task success, further motivating the need for diverse reflection signals. We introduce ParamMem, a parametric memory module that encodes cross-sample reflection patterns into model parameters, enabling diverse reflection generation through temperature-controlled sampling. Building on this module, we propose ParamAgent, a reflection-based agent framework that integrates parametric memory with episodic and cross-sample memory. Extensive experiments on code generation, mathematical reasoning, and multi-hop question answering demonstrate consistent improvements over state-of-the-art baselines. Further analysis reveals that ParamMem is sample-efficient, enables weak-to-strong transfer across model scales, and supports self-improvement without reliance on stronger external model, highlighting the potential of ParamMem as an effective component for enhancing language agents.

---

## 论文详细总结（自动生成）

### ParamMem：利用参数化反思记忆增强语言智能体 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题在于解决语言智能体（Language Agents）在迭代推理过程中**自我反思（Self-reflection）的局限性**。
*   **研究背景**：虽然反思机制（如 Reflexion）能让智能体通过诊断错误来优化方案，但现有的反思往往会陷入**重复性输出**和**准确性不足**的困境，导致推理性能遇到瓶颈。
*   **核心动机**：作者通过实证分析发现，**反思的多样性（Reflective Diversity）与任务成功率之间存在极强的正相关关系**（平均皮尔逊相关系数达 0.76）。现有的基于提示词（Prompt-based）或基于检索（RAG-based）的方法在捕捉复杂模式和维持多样性方面能力有限。因此，论文旨在引入一种新的记忆机制来提升反思信号的多样性和有效性。

#### 2. 方法论：核心思想与关键技术
论文提出了 **ParamMem** 模块及基于此的 **ParamAgent** 框架。
*   **核心思想**：将跨样本的反思模式（Cross-sample reflection patterns）通过微调手段**编码进模型参数**中，而不是依赖外部检索。在推理时，通过温度控制采样生成多样化的反思信号。
*   **关键技术细节**：
    *   **ParamMem 模块**：使用轻量级的 LoRA（Low-Rank Adaptation）对预训练模型进行微调。训练数据是通过强模型（如 GPT-4o-mini）或自身生成的“输入-反思”对。
    *   **多样化采样**：在推理的第一轮使用低温度（T=0.2）保证稳定性，后续轮次使用高温度（T=1.0）以诱导多样化的诊断假设。
    *   **ParamAgent 框架**：整合了三种记忆：
        1.  **情节记忆（Episodic Memory）**：存储当前任务之前的尝试记录。
        2.  **参数化记忆（Parametric Memory）**：即 ParamMem，提供全局的反思模式。
        3.  **跨样本记忆（Cross-sample Memory）**：在增强版 ParamAgent-plus 中，通过检索相似任务的成功轨迹来补充信息。
*   **算法流程**：在第 $k$ 次迭代中，智能体根据输入 $x$、历史反思 $r_{1:k-1}$ 和从 ParamMem 采样的全局反思 $r^g_k$ 生成新的解决方案 $y_k$。

#### 3. 实验设计
*   **数据集与场景**：
    *   **代码生成**：HumanEval, MBPP, LiveCodeBench。
    *   **数学推理**：MATH（涵盖代数、几何等 7 个学科）。
    *   **多跳问答（QA）**：HotpotQA, 2WikiMultiHopQA。
*   **对比基准（Baselines）**：
    *   **基础模型**（Base）、**Reflexion**（经典反思框架）。
    *   **Retroformer**（基于策略梯度的参数化反思模型）。
    *   **DoT / DoT-bank**（目前最先进的基于多样化提示和检索的反思方法）。
*   **骨干模型**：Llama-3.1-8B/70B, Mistral-7B, Qwen2-1.5B, Qwen3-Next-30B/80B。

#### 4. 资源与算力
*   **训练细节**：论文明确提到使用 LoRA 进行微调，参数设置包括：秩 $r=128$，缩放因子 $\alpha=32$，学习率 $2e-5$，训练 3 个 epoch。
*   **算力设备**：文中**未明确说明**具体的 GPU 型号（如 A100 或 H100）及具体数量。
*   **成本分析**：作者提供了一份基于 TogetherAI API 价格的推理成本分析，指出 ParamAgent 虽然提升了性能，但 Token 消耗和经济成本高于基础反思方法。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个领域、6 个主流数据集上进行了广泛测试，并跨越了从 1.5B 到 80B 不同规模的骨干模型。
*   **消融与深度分析**：
    *   **多样性验证**：使用余弦距离、K-means 聚类和轮廓系数（Silhouette Score）量化证明了 ParamMem 确实提升了反思的多样性。
    *   **自改进实验**：验证了在没有外部强模型引导下，模型通过自身数据微调也能提升。
    *   **样本效率实验**：证明仅需 500 个样本即可达到良好效果。
*   **客观性**：实验对比了多种 SOTA 方法，并使用了 Pass@1 和准确率等标准指标，实验设计较为全面、客观。

#### 6. 主要结论与发现
1.  **性能领先**：ParamAgent 在所有任务上均显著优于 Reflexion 和 DoT-bank，尤其在代码和问答任务中表现突出。
2.  **弱到强迁移（Weak-to-Strong）**：即使使用较小的模型（如 8B）作为 ParamMem 模块，也能显著增强大模型（如 80B）的推理能力。
3.  **样本效率高**：ParamMem 具有极高的样本利用率，少量多样化数据即可诱导模型学习到通用的反思模式。
4.  **自改进潜力**：模型可以利用自身生成的反思数据进行迭代微调，实现性能的持续增长。

#### 7. 优点
*   **机制创新**：打破了传统“检索式记忆”的局限，通过“参数化记忆”解决了反思同质化的问题。
*   **即插即用**：ParamMem 作为一个轻量级模块，可以无缝集成到现有的反思框架中。
*   **多样性驱动**：通过实证研究确立了多样性与成功率的联系，为后续研究提供了理论支撑。
*   **不依赖强模型**：证明了自监督/自改进的可行性，降低了对 GPT-4 等闭源高价模型的依赖。

#### 8. 不足与局限
*   **Token 消耗**：为了获得多样性，ParamAgent 需要生成更多的反思文本，导致推理过程中的 Token 成本显著增加。
*   **推理延迟**：由于引入了额外的参数化模块采样步骤，端到端的响应时间会变长。
*   **数学任务的依赖性**：实验显示在数学任务中，ParamMem 虽有提升，但跨样本检索（RAG）的作用依然非常关键，参数化记忆在纯逻辑推导上的独立贡献略逊于代码任务。
*   **训练成本**：虽然是轻量级 LoRA，但相比于纯 Prompt 方法，仍增加了额外的微调阶段。

（完）
