---
title: "The Missing Memory Hierarchy: Demand Paging for LLM Context Windows"
title_zh: 缺失的内存层级：大语言模型上下文窗口的请求分页
authors: Tony Mason
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.09023v1"
tags: ["query:agimem"]
score: 9.0
evidence: 将大模型上下文作为记忆层级的请求分页系统
tldr: "本研究指出当前大语言模型将上下文窗口视为唯一的内存资源，缺乏类似计算机系统的多级存储架构，导致严重的结构性浪费。为此，作者提出了Pichay，一个针对LLM上下文的请求分页系统。该系统作为透明代理，通过驱逐陈旧内容、检测缺页中断并固定工作集页面，实现了上下文的高效管理。实验表明，Pichay在生产环境中可减少高达93%的上下文消耗，证明了将虚拟内存理论应用于LLM上下文管理的有效性。"
selection_source: fresh_fetch
motivation: "现有的LLM上下文管理缺乏内存层次结构，导致大量陈旧信息占据昂贵的上下文空间，造成约21.8%的结构性浪费。"
method: 提出Pichay系统，通过透明代理拦截消息流，实现陈旧内容驱逐、缺页检测及基于故障历史的工作集页面固定。
result: "在生产部署中，该系统将上下文消耗降低了高达93%，且在离线回放中的缺页率仅为0.0254%。"
conclusion: LLM上下文限制和成本问题本质上是虚拟内存问题，可以通过引入请求分页和多级存储层次结构来有效解决。
---

## 摘要
大语言模型的上下文窗口并非内存，而是 L1 缓存：一种小容量、高速且昂贵的资源，而目前该领域却将其视为整个内存系统。这里没有 L2 缓存，没有虚拟内存，也没有分页机制。每一个工具定义、每一条系统提示词以及每一个过时的工具结果，在整个会话生命周期内都会占用上下文。其结果是可衡量的：在 857 个生产会话和 445 万个有效输入 token 中，有 21.8% 属于结构性浪费。我们提出了 Pichay，一个针对 LLM 上下文窗口的请求分页系统。Pichay 作为客户端与推理 API 之间的透明代理实现，它介入消息流以驱逐陈旧内容，在模型重新请求已驱逐材料时检测缺页异常，并根据缺页历史记录固定工作集页面。在 140 万次模拟驱逐的离线回放中，缺页率为 0.0254%。在超过 681 轮的生产环境部署中，该系统将上下文消耗降低了高达 93%（从 5,038KB 降至 339KB）；在极端持续压力下，系统仍能运行，但会出现预期的抖动病态现象，即已驱逐内容被反复调入。核心观察是，该领域面临的问题（如上下文限制、注意力退化、成本扩展、跨会话状态丢失）本质上都是披着不同外衣的虚拟内存问题。解决方案已经存在：工作集理论（Denning, 1968）、请求分页、缺页驱动的替换策略，以及具有多个驱逐管理层级的内存层级结构。我们描述了 LLM 系统完整内存层级（从 L1 到持久化存储）的架构，报告了在生产中部署的前三个层级（L1 驱逐、L2 缺页驱动固定、L3 模型发起的对话压缩），并将跨会话内存确定为最后的待开发领域。

## Abstract
The context window of a large language model is not memory. It is L1 cache: a small, fast, expensive resource that the field treats as the entire memory system. There is no L2, no virtual memory, no paging. Every tool definition, every system prompt, and every stale tool result occupies context for the lifetime of the session. The result is measurable: across 857 production sessions and 4.45 million effective input tokens, 21.8% is structural waste.   We present Pichay, a demand paging system for LLM context windows. Implemented as a transparent proxy between client and inference API, Pichay interposes on the message stream to evict stale content, detect page faults when the model re-requests evicted material, and pin working-set pages identified by fault history. In offline replay across 1.4 million simulated evictions, the fault rate is 0.0254%. In live production deployment over 681turns, the system reduces context consumption by up to 93% (5,038KB to 339KB); under extreme sustained pressure, the system remains operational but exhibits the expected thrashing pathology, with repeated fault-in of evicted content.   The key observation is that the problems the field faces, such as context limits, attention degradation, cost scaling, lost state across sessions, are virtual memory problems wearing different clothes. The solutions exist: working set theory (Denning, 1968), demand paging, fault-driven replacement policies, and memory hierarchies with multiple eviction-managed levels. We describe the architecture of a full memory hierarchy for LLM systems (L1 through persistent storage), report on the first three levels deployed in production use (L1 eviction, L2 fault-driven pinning, L3 model-initiated conversation compaction), and identify cross-session memory as the remaining frontier.

---

## 论文详细总结（自动生成）

### 论文总结：《缺失的内存层级：大语言模型上下文窗口的请求分页》

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前大语言模型（LLM）领域将“上下文窗口”误视为内存，而本质上它更接近计算机架构中的 **L1 缓存**——昂贵、高速且容量有限。
*   **研究动机**：现有的 Agent 系统（如 Claude Code, Cursor）在每次 API 调用时都会重复发送完整的工具定义、系统提示词和全部历史记录。这种“全量堆积”模式导致了严重的**结构性浪费**。
*   **背景**：作者通过对 857 个生产会话的分析发现，约 **21.8%** 的 token 消耗属于无效浪费（包括未使用的工具 Schema、重复内容和陈旧的工具结果）。这不仅增加了成本，还因注意力稀释导致模型性能下降。

#### 2. 论文提出的方法论
*   **核心思想**：引入操作系统中的**虚拟内存（Virtual Memory）**和**请求分页（Demand Paging）**机制，构建一个四层内存层级（L1-L4）。
*   **关键技术细节 (Pichay 系统)**：
    *   **透明代理 (Transparent Proxy)**：部署在客户端与推理 API 之间，无需修改模型或客户端代码。
    *   **垃圾回收 vs. 分页**：区分不可重请求的临时输出（GC）和可根据路径重新读取的地址化内容（Paging）。
    *   **驱逐策略 (Eviction Policy)**：采用基于用户轮次的 FIFO 策略，将陈旧内容替换为微小的“检索句柄”（Retrieval Handles）。
    *   **故障驱动固定 (Fault-Driven Pinning)**：若被驱逐内容被模型重新请求（发生“缺页中断”），系统会将其识别为“工作集”并永久固定在 L2 层级，防止二次驱逐。
    *   **协作式管理 (Cooperative Management)**：
        *   **Phantom Tools**：注入虚拟工具，允许模型主动释放内存或请求故障恢复。
        *   **Cleanup Tags**：模型可在输出中嵌入标签，指示代理进行块删除、摘要压缩或对话合并（L3 压缩）。
*   **成本模型**：提出了“反转成本模型”，指出在 LLM 中“保留内容”比“产生缺页（重新读取）”更昂贵，因此激进的驱逐策略在经济上是合理的。

#### 3. 实验设计
*   **数据集/场景**：
    *   **生产语料库**：来自 Claude Code 编码助手的 857 个真实会话，包含 54,170 次 API 调用和 44.5 亿有效 token。
    *   **场景**：复杂的软件工程任务（多文件编辑、子代理协作）。
*   **Benchmark 与对比方法**：
    *   **Baseline**：原始全量上下文模式。
    *   **Trimmed**：仅应用工具定义修剪和技能去重。
    *   **Compact+Trim**：在修剪基础上增加陈旧结果驱逐（分页）。
*   **验证方式**：
    *   **离线回放**：模拟 140 万次驱逐以测量缺页率。
    *   **生产部署**：在作者日常开发中实测，观察长达 681 轮的极端会话。
    *   **非劣性评估**：使用 3 个 LLM 裁判（Sonnet 4, Haiku 4.5）对 18 个会话的输出进行盲测评分。

#### 4. 资源与算力
*   **模型使用**：主要基于 Anthropic 的 Claude 3.5 Opus 和 Sonnet 4。
*   **算力说明**：论文未提及具体的 GPU 训练算力，因为 Pichay 是一个**推理侧的代理系统**，不涉及模型训练。实验主要消耗的是 API Token 额度。
*   **处理开销**：代理层的处理是 I/O 密集型的，对延迟的影响极小。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   分析了 857 个生产会话，样本量在同类研究中属于较大规模。
    *   离线回放涵盖了 139 万次模拟决策，数据具有统计学意义。
    *   LLM 裁判盲测包含 54 个判定结果。
*   **充分性与客观性**：实验设计较为全面，包含了浪费分类、策略安全性验证（缺页率）、端到端性能对比以及质量评估。通过引入“缺页率”这一硬指标，客观地评价了驱逐策略的合理性。

#### 6. 论文的主要结论与发现
*   **浪费严重**：Agent 系统中 21.8% 的 token 是结构性浪费，且随着会话增长，成本呈二次方爆炸。
*   **分页有效**：Pichay 在生产中可将上下文占用从 5MB 压缩至 339KB（减少 93%），且离线缺页率仅为 **0.0254%**。
*   **注意力集中效应**：移除无关的陈旧内容不仅省钱，还能提高模型输出的质量（裁判更倾向于选择处理后的输出）。
*   **病态抖动**：当“工作集”超过物理上下文限制时，系统会出现类似 OS 的“抖动（Thrashing）”现象，导致频繁的重复读取。

#### 7. 优点
*   **理论创新**：首次将经典的操作系统内存管理理论（工作集、请求分页、Belady 算法）完整且结构化地映射到 LLM 上下文管理中。
*   **工程实用性**：透明代理设计使其具有极强的通用性，无需厂商配合即可部署。
*   **协作机制**：提出的“协作式内存管理”利用了 LLM 的理解能力，让模型参与自身的缓存清理，这是传统硬件内存管理无法实现的。

#### 8. 不足与局限
*   **样本偏差**：语料库来自单一的高级用户，可能无法完全代表所有用户的交互模式。
*   **应用限制**：主要针对 Agentic（代理式）工作流，对于简单的单轮对话或非工具类任务效果有限。
*   **依赖模型理解**：分页句柄的有效性依赖于模型对“内容已被分页，请重新读取”这一提示的理解能力，较弱的模型可能无法正确触发故障恢复。
*   **缓存失效成本**：频繁的结构性修改（如 L3 压缩）会导致推理侧的 Prefix Caching 失效，产生额外的一次性计算开销。

（完）
