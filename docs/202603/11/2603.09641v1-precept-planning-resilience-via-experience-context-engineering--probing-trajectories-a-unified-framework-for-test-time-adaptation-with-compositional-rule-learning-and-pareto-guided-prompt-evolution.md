---
title: "PRECEPT: Planning Resilience via Experience, Context Engineering & Probing Trajectories A Unified Framework for Test-Time Adaptation with Compositional Rule Learning and Pareto-Guided Prompt Evolution"
title_zh: PRECEPT：通过经验、上下文工程与探测轨迹实现规划韧性——一个结合组合规则学习与帕累托引导提示演化的统一测试时自适应框架
authors: Arash Shahmansoori
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09641v1"
tags: ["query:agimem"]
score: 9.0
evidence: 冲突感知记忆与规则失效策略
tldr: 针对LLM智能体在处理多条件规则检索、规则组合及应对陈旧知识时的局限性，本文提出PRECEPT框架。该框架结合了确定性精确匹配检索、基于贝叶斯可靠性的冲突感知记忆以及Pareto引导的提示词演化算法（COMPASS）。实验表明，PRECEPT在组合泛化、持续学习和对抗鲁棒性方面表现优异，显著提升了智能体在复杂动态环境下的规划韧性。
selection_source: fresh_fetch
motivation: 解决LLM智能体在规则检索退化、规则组合不可靠以及缺乏对陈旧或对抗性知识检测机制的问题。
method: 采用确定性精确匹配检索、冲突感知记忆机制及COMPASS提示词演化外环，构建统一的测试时自适应框架。
result: 在首试成功率、组合泛化（+33.3pp）和持续学习（+40-55pp）等方面取得显著提升，并大幅减少了执行步骤。
conclusion: PRECEPT通过结构化规则管理与动态演化，有效增强了LLM智能体在复杂任务中的适应性与鲁棒性。
---

## 摘要
以自然语言存储知识的大语言模型（LLM）智能体在条件数量增加时面临严重的检索性能下降，通常难以可靠地组合已学习的规则，且往往缺乏检测陈旧或对抗性知识的显式机制。我们提出了 PRECEPT，这是一个用于测试时自适应的统一框架，包含三个紧密耦合的组件：(1) 基于结构化条件键的确定性精确匹配规则检索；(2) 具有贝叶斯来源可靠性和基于阈值的规则失效机制的冲突感知记忆；(3) COMPASS，一个帕累托引导的提示演化外环。精确检索消除了确定性路径上的部分匹配解释错误（根据构造为 0%，而在定理 B.6 的独立性模型下 N=10 时为 94.4%），并通过语义层级结构支持组合堆叠；冲突感知记忆解决了静态与动态之间的不一致，并支持漂移自适应；COMPASS 通过相同的端到端执行流水线评估提示。实验结果（9-10 个随机种子）显示：PRECEPT 相比 Full Reflexion 在首次尝试上取得了 +41.1pp 的优势 (d>1.9)，组合泛化能力提升了 +33.3pp (d=1.55)，在双向物流组合上达到 100% 的 $P_1$ (d=2.64)，持续学习增益达 +40-55pp，在对抗性静态知识下表现出强大的最终鲁棒性（在对抗性静态知识激活时物流任务达到 100%；集成任务部分恢复），漂移恢复提升了 +55.0pp (d=0.95, p=0.031)，且步数减少了 61%。核心对比具有统计学显著性，通常 p < 0.001。

## Abstract
LLM agents that store knowledge as natural language suffer steep retrieval degradation as condition count grows, often struggle to compose learned rules reliably, and typically lack explicit mechanisms to detect stale or adversarial knowledge. We introduce PRECEPT, a unified framework for test-time adaptation with three tightly coupled components: (1) deterministic exact-match rule retrieval over structured condition keys, (2) conflict-aware memory with Bayesian source reliability and threshold-based rule invalidation, and (3) COMPASS, a Pareto-guided prompt-evolution outer loop. Exact retrieval eliminates partial-match interpretation errors on the deterministic path (0% by construction, vs 94.4% under Theorem~B.6's independence model at N=10) and supports compositional stacking through a semantic tier hierarchy; conflict-aware memory resolves static--dynamic disagreements and supports drift adaptation; COMPASS evaluates prompts through the same end-to-end execution pipeline.   Results (9--10 seeds): PRECEPT achieves a +41.1pp first-try advantage over Full Reflexion (d>1.9), +33.3pp compositional generalization (d=1.55), 100% $P_1$ on 2-way logistics compositions (d=2.64), +40--55pp continuous learning gains, strong eventual robustness under adversarial static knowledge (100% logistics with adversarial SK active; partial recovery on integration), +55.0pp drift recovery (d=0.95, p=0.031), and 61% fewer steps. Core comparisons are statistically significant, often at p<0.001.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **PRECEPT** 的统一框架，旨在提升大语言模型（LLM）智能体在复杂、动态环境下的规划韧性。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的 LLM 智能体在处理复杂规则时存在四大局限性：
    1.  **组合爆炸 (L1)**：随着条件增加，规则组合呈指数级增长，难以通过有限训练覆盖。
    2.  **解释退化 (L2)**：基于自然语言的检索在多条件匹配时准确率随条件数增加而指数级下降。
    3.  **漂移盲区 (L3)**：智能体难以识别陈旧或过时的规则，且无法有效处理静态知识与动态经验之间的冲突。
    4.  **样本低效 (L4)**：强化学习（RL）需要大量样本，而部署环境通常要求极低样本下的快速自适应。
*   **研究背景**：现有的“反思型”（Reflexion）智能体依赖自然语言存储经验，在检索和应用规则时存在严重的解释偏差。论文旨在通过结构化、确定性的机制替代纯概率性的语言推理。

### 2. 论文提出的方法论
PRECEPT 框架由三个紧密耦合的组件组成：
*   **确定性精确匹配检索 (Deterministic Retrieval)**：
    *   引入 $O(1)$ 的结构化条件键检索，消除检索时的解释错误。
    *   **原子约束堆叠 (Atomic Constraint Stacking)**：通过语义层级结构（安全 > 合规 > 偏好）将学习到的原子规则在测试时进行组合，实现从 $N$ 个原子规则到 $2^N-1$ 个组合场景的覆盖。
*   **冲突感知记忆与进化存储 (Evo-Memory)**：
    *   **Type I 冲突（静态 vs 动态）**：使用贝叶斯来源可靠性模型和汤普森采样（Thompson Sampling）来解决预设文档（静态）与实际执行结果（动态）之间的矛盾。
    *   **Type II 冲突（环境漂移）**：引入基于阈值的规则失效机制。当规则连续失败（默认阈值 $\theta=2$）时，自动将其从记忆中抹除并重新学习。
*   **COMPASS 提示演化外环**：
    *   **双频控制**：高频监控层（每步执行）进行约束检查；低频架构层（事件触发）进行提示词演化。
    *   **帕累托引导 (Pareto-Guided)**：在“成功率”和“步骤效率”两个维度上寻找最优提示词，并利用 MAP-Elites 保持策略的多样性。

### 3. 实验设计
*   **实验场景**：设计了三个具有挑战性的“黑盒”领域：
    1.  **物流 (Logistics)**：处理港口关闭、海关延迟等组合条件。
    2.  **预订 (Booking)**：处理复杂的预订失败和超卖情况。
    3.  **集成 (Integration)**：模拟 API 编排中的 OAuth 失败和特定品牌偏见（最难领域）。
*   **Benchmark 与对比方法**：
    *   **对比方法**：增强版的 **Full Reflexion**（带向量库和结构化提示）和 **ExpeL**（经验学习智能体）。
    *   **实验设置**：每个实验运行 9-10 个独立随机种子，提供 95% 置信区间。
*   **测试维度**：包括首次尝试成功率 ($P_1$)、最终成功率 ($P_t$)、步骤效率、组合泛化能力、持续学习曲线、规则持久性以及对环境漂移的恢复速度。

### 4. 资源与算力
*   **模型使用**：所有实验均使用 **GPT-4o-mini** 作为核心 LLM，嵌入模型使用 `text-embedding-3-small`。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或训练时长。由于该研究侧重于基于 API 的智能体框架而非模型本身的预训练，其算力消耗主要集中在推理调用和 COMPASS 演化过程中的多次 Rollout 评估上。

### 5. 实验数量与充分性
*   **实验规模**：论文共报告了 **9 组核心实验**，涵盖了从基础性能对比到复杂的消融实验（如静态知识消融、训练规模消融、COMPASS 压力测试等）。
*   **充分性与客观性**：
    *   实验设计非常详尽，不仅有实证数据，还附带了闭式解的理论证明（如定理 B.6 证明了精确检索的优势）。
    *   使用了 Cohen's d 效应量和 p 值进行统计显著性检验，大多数核心对比的 $p < 0.001$，显示出结果的稳健性。
    *   通过“黑盒”设计（如使用 MD5 哈希映射解空间）刻意消除了 LLM 的语义猜测可能，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能飞跃**：PRECEPT 在首次尝试成功率上比 Full Reflexion 高出 **41.1pp**，步骤数减少了 **61%**。
*   **组合泛化**：仅通过学习原子规则，PRECEPT 在复杂组合任务上的表现优于基线 **33.3pp**。
*   **漂移适应**：在环境规则发生突变时，PRECEPT 的恢复速度极快（+55.0pp 的恢复增益），而基线方法往往会陷入旧规则的死循环。
*   **解释偏差**：实验证实，即使 LLM 在思维链（CoT）中找到了正确答案，在最终输出时仍可能受参数先验影响而犯错，而 PRECEPT 的确定性路径有效规避了这一点。

### 7. 优点（亮点）
*   **理论与实证结合**：不仅有实验，还提供了严密的数学证明，为智能体架构提供了理论支撑。
*   **确定性与概率性的平衡**：巧妙地将 $O(1)$ 的确定性检索与 LLM 的概率性推理结合，既保证了已知规则的可靠性，又保留了处理未知情况的灵活性。
*   **冲突处理机制**：引入贝叶斯可靠性模型处理知识冲突，这在现有的 RAG 或智能体框架中较为罕见且非常实用。
*   **自净化能力**：规则失效机制确保了提示词和记忆库的实时更新，防止了“知识中毒”。

### 8. 不足与局限
*   **领域覆盖有限**：虽然在三个领域表现优异，但这些领域多为结构化、规则导向的任务，在开放式创意任务或极度非结构化的任务中表现尚待验证。
*   **条件表示依赖**：目前的框架假设条件可以被离散化和规范化（如 A+B+C），对于连续型特征（如温度、重量）需要预处理。
*   **计算成本**：COMPASS 的低频演化层涉及多次 Rollout 评估，虽然有“智能分配”策略，但在大规模部署时仍可能产生较高的 API 调用成本。
*   **单智能体限制**：目前的研究集中在单智能体自适应，尚未探讨多智能体协作下的约束传播问题。

（完）
