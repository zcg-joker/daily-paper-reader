---
title: Empowering Locally Deployable Medical Agent via State Enhanced Logical Skills for FHIR-based Clinical Tasks
title_zh: 通过状态增强逻辑技能赋能基于 FHIR 临床任务的本地可部署医疗智能体
authors: "Wanrong Yang, Zhengliang Liu, Yuan Li, Bingjie Yan, Lingfang Li, Mingguang He, Dominik Wojtczak, Yalin Zheng, Danli Shi"
date: 2026-03-06
pdf: "https://arxiv.org/pdf/2603.06902v1"
tags: ["query:agimem"]
score: 9.0
evidence: 用于提取临床轨迹的状态增强逻辑技能记忆
tldr: "针对医疗智能体在本地部署时面临的隐私和数据稀缺挑战，本文提出SELSM框架。该框架无需训练，通过将模拟临床轨迹提炼为抽象的逻辑技能，并利用两阶段检索机制引导模型推理，解决了状态歧义问题。在MedAgentBench基准测试中，该方法显著提升了30B规模本地模型的性能，实现了100%的任务完成率，为临床信息系统的AI适配提供了高效且保护隐私的方案。"
selection_source: fresh_fetch
motivation: 医疗智能体在实际部署中受限于隐私保护导致的数据稀缺，且现有模型在处理复杂临床任务时存在状态歧义和推理中断问题。
method: 提出SELSM框架，通过提炼实体无关的逻辑规则并结合查询锚定的两阶段检索机制，为模型提供动态更新的认知支架。
result: "在MedAgentBench上，该方法使Qwen3-30B-A3B模型达到100%的任务完成率，成功率绝对提升22.67%，远超现有记忆增强基准。"
conclusion: 该研究证明了通过状态增强的逻辑技能记忆，可以在保护隐私的前提下有效提升本地部署医疗智能体的临床任务处理能力。
---

## 摘要
尽管大语言模型作为主动式医疗智能体展现出巨大潜力，但在隐私约束下的数据稀缺严重瓶颈了其在现实世界中的部署。为克服这一问题，我们提出了状态增强逻辑技能记忆（SELSM），这是一种无需训练的框架，可将模拟的临床轨迹提取为抽象技能空间中与实体无关的操作规则。在推理过程中，一种基于查询锚点的两阶段检索机制动态获取这些与实体无关的逻辑先验，以引导智能体的逐步推理，从而有效解决状态多义性问题。在 MedAgentBench（唯一使用真实临床数据进行基准测试的权威高保真虚拟电子健康档案（EHR）沙箱）上的评估结果显示，SELSM 显著提升了本地可部署基础模型（30B-32B 参数）的零样本能力。值得注意的是，在 Qwen3-30B-A3B 骨干模型上，我们的框架完全消除了任务链中断，实现了 100% 的完成率，使整体成功率绝对提升了 22.67%，并显著优于现有的记忆增强基线模型。本研究表明，为模型配备动态可更新的状态增强认知支架，是 AI 智能体在临床信息系统中进行本地适配的一种隐私保护且计算高效的途径。虽然目前作为初步尝试在基于 FHIR 的 EHR 交互中进行了验证，但 SELSM 的实体无关设计为更广泛的临床部署奠定了原则性基础。

## Abstract
While Large Language Models demonstrate immense potential as proactive Medical Agents, their real-world deployment is severely bottlenecked by data scarcity under privacy constraints. To overcome this, we propose State-Enhanced Logical-Skill Memory (SELSM), a training-free framework that distills simulated clinical trajectories into entity-agnostic operational rules within an abstract skill space. During inference, a Query-Anchored Two-Stage Retrieval mechanism dynamically fetches these entity-agnostic logical priors to guide the agent's step-by-step reasoning, effectively resolving the state polysemy problem. Evaluated on MedAgentBench -- the only authoritative high-fidelity virtual EHR sandbox benchmarked with real clinical data -- SELSM substantially elevates the zero-shot capabilities of locally deployable foundation models (30B--32B parameters). Notably, on the Qwen3-30B-A3B backbone, our framework completely eliminates task chain breakdowns to achieve a 100\% completion rate, boosting the overall success rate by an absolute 22.67\% and significantly outperforming existing memory-augmented baselines. This study demonstrates that equipping models with a dynamically updatable, state-enhanced cognitive scaffold is a privacy-preserving and computationally efficient pathway for local adaptation of AI agents to clinical information systems. While currently validated on FHIR-based EHR interactions as an initial step, the entity-agnostic design of SELSM provides a principled foundation toward broader clinical deployment.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **SELSM（State-Enhanced Logical-Skill Memory）** 的框架，旨在提升本地部署的医疗智能体在处理复杂临床任务时的性能。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）作为医疗智能体在实际临床部署中面临三大瓶颈：
    1.  **隐私约束与数据稀缺**：临床交互轨迹高度敏感，无法上传云端训练，且高质量的多轮强化学习（RL）标注数据极难获取。
    2.  **环境异构性**：不同医院的 EHR 系统（如 FHIR 接口、操作规程）各异，导致模型在未见过的环境中容易产生幻觉或任务链中断。
    3.  **状态歧义（State Polysemy）**：在复杂的临床工作流中，不同的任务可能出现相似的中间状态，导致模型检索到错误的操作逻辑。
*   **研究背景**：现有的参数微调方法成本高且难以泛化，而传统的记忆增强方法在处理实体密集的医疗数据时效果不佳。

### 2. 核心方法论：SELSM 框架
SELSM 是一种**无需训练（Training-free）**、基于记忆增强的框架，其核心思想是将临床交互轨迹提炼为“与实体无关”的逻辑技能。
*   **逻辑技能提炼（Logical Skill Distillation）**：
    *   利用“LLM-as-Judge”机制，将原始的交互轨迹（State-Action-Observation）转化为抽象的逻辑规则。
    *   **实体无关协议**：剥离具体的患者 ID、API 链接等实例信息，仅保留通用的推理模式（如：验证身份 -> 检查过敏 -> 查询药表 -> 提交处方）。
*   **分层记忆索引（Hierarchical Memory Indexing）**：
    *   构建两级索引：**任务级（Task-level）**索引临床查询，**转换级（Transition-level）**索引具体的操作步骤和状态。
*   **查询锚定的两阶段检索（Query-Anchored Two-Stage Retrieval）**：
    *   **第一阶段（任务过滤）**：根据当前任务查询，在语义空间中筛选出最相关的 Top-Q 个任务记录。
    *   **第二阶段（状态排序）**：在筛选出的记录中，根据当前详细状态进行精细匹配，获取具体的逻辑技能。
    *   **注入推理**：将检索到的逻辑技能作为 Prompt 注入模型，引导其进行下一步决策。

### 3. 实验设计
*   **数据集/场景**：使用了 **MedAgentBench**，这是目前唯一基于真实临床数据构建的高保真虚拟 EHR 沙箱，涵盖了读取（GET）和写入（POST）操作。
*   **基准测试（Benchmark）**：包含 300 个临床任务，分为查询类（信息检索）和动作类（状态修改，如开处方）。
*   **对比方法**：
    *   **Vanilla Baseline**：无增强的基础模型。
    *   **ExpeL**：一种基于经验学习的智能体框架。
    *   **A-Mem**：一种通用的智能体记忆增强方法。
*   **骨干模型**：选用本地可部署的 30B-32B 规模模型（Qwen3-30B-A3B, Qwen3-32B, GLM4-32B）。

### 4. 资源与算力
*   **算力说明**：论文强调了该框架的“本地部署”属性，适用于医院常见的商品化硬件。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。但由于该方法是“无需训练”的（Training-free），其主要计算开销在于推理时的向量检索和 Prompt 处理，而非大规模参数更新。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同的骨干模型上进行了 300 个任务的测试，总计进行了多组对比实验。
*   **消融实验**：针对“操作逻辑模块”、“推理轨迹模块”和“两阶段检索机制”进行了消融测试，验证了各组件的必要性。
*   **充分性评价**：实验设计较为充分，涵盖了成功率（SR）、完成率（TC）、Token 效率、错误鲁棒性等多个维度。对比实验选择了同类 SOTA 方法，且在不同能力的模型上验证了泛化性，表现出较好的客观性和公平性。

### 6. 主要结论与发现
*   **性能显著提升**：在 Qwen3-30B-A3B 上，SELSM 将任务完成率提升至 **100%**，成功率绝对提升了 **22.67%**。
*   **解决状态歧义**：两阶段检索机制被证明是防止模型在复杂流程中迷失方向的关键。
*   **补偿模型能力**：实验发现，SELSM 对基础能力稍弱的模型（如 GLM4-32B）提升最为显著（成功率提升 27.3%），起到了“认知支架”的作用。
*   **高效性**：相比其他记忆增强方法，SELSM 在提升准确率的同时，降低了完成任务所需的平均 Token 消耗。

### 7. 优点与亮点
*   **隐私友好**：无需将敏感数据上传云端微调，通过本地模拟器生成逻辑技能即可适配。
*   **实体无关设计**：通过抽象化处理，使技能具有极强的可重用性，解决了医疗数据中实体干扰检索的问题。
*   **两阶段检索**：巧妙地利用全局查询（Query）作为锚点，解决了局部状态相似导致的检索偏离。

### 8. 不足与局限
*   **单一基准测试**：虽然 MedAgentBench 很权威，但仅在一个基准上测试可能存在过拟合特定环境的风险。
*   **缺乏真实跨院验证**：论文虽然在理论上支持跨机构部署，但尚未在具有不同 IT 架构的真实医院环境中进行多中心验证。
*   **依赖种子查询**：逻辑技能库的质量高度依赖于初始采集轨迹时设计的种子查询，这仍需要一定的医学专家知识。
*   **文本局限性**：目前仅支持基于文本和 API 的交互，无法处理放射影像（DICOM）或心电图信号等跨模态临床数据。

（完）
