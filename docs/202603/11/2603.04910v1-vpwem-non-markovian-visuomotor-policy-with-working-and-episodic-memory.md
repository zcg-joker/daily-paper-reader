---
title: "VPWEM: Non-Markovian Visuomotor Policy with Working and Episodic Memory"
title_zh: VPWEM：具有工作记忆和情节记忆的非马尔可夫视觉运动策略
authors: "Yuheng Lei, Zhixuan Liang, Hongyuan Zhang, Ping Luo"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04910v1"
tags: ["query:agimem"]
score: 9.0
evidence: 具备工作记忆和情境记忆的视觉运动策略
tldr: 针对机器人模仿学习中长时记忆缺失的问题，本文提出VPWEM，一种结合工作记忆与情节记忆的非马尔可夫视觉运动策略。该方法利用滑动窗口保留近期观察，并通过Transformer压缩器将历史信息递归转化为固定长度的情节记忆。实验表明，VPWEM在处理复杂非马尔可夫任务时，在保持计算效率的同时显著提升了操作精度，在MIKASA等基准测试中表现优于现有扩散策略和VLA模型。
selection_source: fresh_fetch
motivation: 现有的视觉运动策略受限于短上下文窗口，难以处理需要长时记忆的非马尔可夫任务，且简单扩大窗口会带来巨大的计算开销和过拟合风险。
method: 提出一种配备工作记忆和情节记忆的策略，通过Transformer递归压缩历史观察为情节记忆令牌，并结合扩散策略生成动作。
result: "在MIKASA记忆密集型任务上比现有基准提升超过20%，在MoMaRT移动操作基准上平均提升5%，且保持了近乎恒定的计算开销。"
conclusion: VPWEM通过高效的记忆压缩机制，在不增加实时计算负担的前提下，显著增强了机器人处理长程依赖任务的能力。
---

## 摘要
从人类演示中进行模仿学习在机器人控制方面取得了显著成功，然而大多数视觉运动策略仍然以单步观测或短上下文历史为条件，这使得它们在处理需要长期记忆的非马尔可夫任务时面临困难。简单地扩大上下文窗口会带来巨大的计算和内存开销，并容易导致对伪相关性的过拟合，从而在分布偏移下引发灾难性失败，并违反机器人系统的实时性约束。相比之下，人类可以将重要的过往经验压缩为长期记忆，并利用它们在整个生命周期中解决任务。在本文中，我们提出了 VPWEM，这是一种配备了工作记忆和情节记忆的非马尔可夫视觉运动策略。VPWEM 保留了最近观测 token 的滑动窗口作为短期工作记忆，并引入了一个基于 Transformer 的上下文记忆压缩器，该压缩器递归地将窗口外的观测转换为固定数量的情节记忆 token。该压缩器对过去摘要 token 的缓存使用自注意力，对历史观测的缓存使用交叉注意力，并与策略进行联合训练。我们在扩散策略上实例化了 VPWEM，以利用短期和全情节信息进行动作生成，且每步的内存和计算开销几乎恒定。实验表明，在 MIKASA 的记忆密集型操作任务中，VPWEM 的表现优于包括扩散策略和视觉-语言-动作（VLA）模型在内的最先进基准模型 20% 以上，并在移动操作基准测试 MoMaRT 上实现了平均 5% 的提升。代码可在 https://github.com/HarryLui98/code_vpwem 获取。

## Abstract
Imitation learning from human demonstrations has achieved significant success in robotic control, yet most visuomotor policies still condition on single-step observations or short-context histories, making them struggle with non-Markovian tasks that require long-term memory. Simply enlarging the context window incurs substantial computational and memory costs and encourages overfitting to spurious correlations, leading to catastrophic failures under distribution shift and violating real-time constraints in robotic systems. By contrast, humans can compress important past experiences into long-term memories and exploit them to solve tasks throughout their lifetime. In this paper, we propose VPWEM, a non-Markovian visuomotor policy equipped with working and episodic memories. VPWEM retains a sliding window of recent observation tokens as short-term working memory, and introduces a Transformer-based contextual memory compressor that recursively converts out-of-window observations into a fixed number of episodic memory tokens. The compressor uses self-attention over a cache of past summary tokens and cross-attention over a cache of historical observations, and is trained jointly with the policy. We instantiate VPWEM on diffusion policies to exploit both short-term and episode-wide information for action generation with nearly constant memory and computation per step. Experiments demonstrate that VPWEM outperforms state-of-the-art baselines including diffusion policies and vision-language-action (VLA) models by more than 20% on the memory-intensive manipulation tasks in MIKASA and achieves an average 5% improvement on the mobile manipulation benchmark MoMaRT. Code is available at https://github.com/HarryLui98/code_vpwem.

---

## 论文详细总结（自动生成）

以下是对论文《VPWEM: Non-Markovian Visuomotor Policy with Working and Episodic Memory》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的机器人视觉运动策略（Visuomotor Policies）大多基于马尔可夫假设，仅依赖当前观察或极短的历史上下文（如2-10帧）。这使得它们在处理需要长时记忆的**非马尔可夫任务**（如记住遮挡物下的物体位置）时表现糟糕。
*   **研究动机**：
    *   **计算瓶颈**：简单增加上下文窗口会导致计算量（$O(L^2)$）和内存开销剧增，难以满足实时控制需求。
    *   **因果困惑**：盲目增加历史长度容易引入伪相关性，导致模型出现“复读机”效应（Copycat Problem）或在分布偏移时崩溃。
    *   **生物启发**：人类大脑通过海马体将工作记忆压缩为长期记忆。论文旨在模仿这一机制，构建一种既能保留关键历史又能保持低开销的记忆模型。

### 2. 方法论
VPWEM 框架结合了**工作记忆（Working Memory, WM）**和**情节记忆（Episodic Memory, EM）**，并将其集成到扩散策略（Diffusion Policy）中。
*   **工作记忆 (WM)**：采用滑动窗口机制，保留最近 $L$ 帧的观察 token。这部分信息未经压缩，提供高精度的短期上下文。
*   **情节记忆 (EM) 与上下文记忆压缩器**：
    *   **递归压缩**：当观察 token 移出滑动窗口时，不再被丢弃，而是输入一个基于 Transformer 的压缩器。
    *   **双重注意力机制**：压缩器包含 N 层，每层利用**自注意力**处理过去的摘要 token 缓存，利用**交叉注意力**检索历史观察缓存。
    *   **固定尺寸输出**：无论任务持续多久，压缩器最终输出固定数量（$M$ 个）的情节记忆 token，代表对全剧集历史的动态总结。
*   **动作生成**：将 WM 和 EM 的 token 拼接，作为扩散模型（Transformer-based Noise Predictor）的条件输入，通过去噪过程生成动作序列。
*   **训练策略**：采用端到端优化，通过行为克隆损失函数同时训练压缩器和策略网络。为了降低显存压力，历史 token 在存入缓存前会切断梯度传播。

### 3. 实验设计
*   **数据集与场景**：
    *   **MIKASA**：专门用于评估智能体记忆能力的基准，包括“三杯球猜位置”（ShellGame）和“颜色记忆选择”任务。
    *   **MoMaRT**：移动操作基准，涉及洗碗机清理、餐桌布置等长程非马尔可夫任务。
    *   **Robomimic**：标准的桌面操作基准（Square, Transport），用于验证模型在近马尔可夫任务中的通用性。
*   **对比方法**：
    *   **基础模型**：RNN (LSTM-GMM)、Diffusion Policy (DP)。
    *   **长上下文变体**：DP-PTP（带过去 token 预测的扩散策略）、MaIL（基于 Mamba 的模仿学习）。
    *   **VLA 大模型**：Octo, OpenVLA, $\pi_0$, MemoryVLA 等。

### 4. 资源与算力
*   **硬件**：所有实验均在单张 **NVIDIA RTX 4090 GPU** 上运行。
*   **训练时长/规模**：
    *   MIKASA 任务训练 60 万步；MoMaRT 和 Robomimic 任务训练 100 万步。
    *   批大小（Batch Size）为 64。
*   **模型规模**：VPWEM 引入的额外记忆模块非常轻量，仅约 **2.24M 参数**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流基准测试中的 10 多个子任务，实验设置较为全面。
*   **统计显著性**：所有实验均使用 3 个随机种子运行，并报告了均值和标准差，增强了结果的可信度。
*   **消融实验**：针对上下文长度、压缩器层数、缓存大小、缓存管理策略（FIFO, K-Means, AdjSim 等）进行了详细的消融研究，验证了各组件的有效性。
*   **公平性**：对比了不同上下文长度下的计算开销（推理延迟、显存占用），证明了该方法在性能提升的同时保持了极高的效率。

### 6. 主要结论与发现
*   **性能飞跃**：在记忆密集型任务（MIKASA）中，VPWEM 比现有最强基准提升了 **20% 以上**的成功率。
*   **长程任务优势**：在 MoMaRT 移动操作任务中平均提升了 **5%**，证明了情节记忆对长程依赖的建模能力。
*   **通用性**：在几乎是马尔可夫的 Robomimic 任务中，VPWEM 表现与基准持平，说明引入长时记忆模块不会损害基础任务的性能。
*   **高效性**：相比于直接增加窗口长度的 DP-PTP，VPWEM 在推理延迟和显存占用上表现出近乎恒定的开销，更适合实时机器人系统。

### 7. 优点
*   **架构优雅**：通过递归压缩实现了 $O(1)$ 的单步推理复杂度，解决了长序列处理的计算瓶颈。
*   **抗过拟合**：通过压缩机制过滤了历史中的冗余信息和噪声，有效缓解了模仿学习中的因果困惑问题。
*   **即插即用**：该记忆机制可以轻松集成到不同的策略骨干（如 DP 或 MaIL）中，具有良好的扩展性。

### 8. 不足与局限
*   **采样损失**：为了训练效率，对进入压缩器的历史 token 进行了随机下采样（Subsampling），这可能导致某些瞬时关键信息的丢失。
*   **实机验证缺失**：论文主要在仿真环境（MIKASA, MoMaRT）中进行验证，尚未展示在真实物理机器人上的部署效果。
*   **梯度截断**：为了节省显存，压缩器在存储历史状态时切断了梯度，这虽然降低了开销，但也限制了模型学习极长程复杂逻辑的能力。

（完）
