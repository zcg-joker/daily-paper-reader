---
title: Chow-Liu Ordering for Long-Context Reasoning in Chain-of-Agents
title_zh: Chain-of-Agents 中用于长上下文推理的 Chow-Liu 排序
authors: "Naman Gupta, Vaibhav Singh, Arun Iyer, Kirankumar Shiragur, Pratham Grover, Ramakrishna B. Bairi, Ritabrata Maiti, Sankarshan Damle, Shachee Mishra Gupta, Rishikesh Maurya, Vageesh D. C"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09835v1"
tags: ["query:agimem"]
score: 9.0
evidence: 智能体中的有界共享记忆与长上下文推理
tldr: 针对长文本推理中的 Chain-of-Agents (CoA) 框架，本文研究了分块处理顺序对信息损耗的影响。由于 CoA 采用有限内存的顺序处理，分块顺序决定了信息瓶颈的严重程度。作者提出利用 Chow-Liu 树学习分块间的依赖结构，并通过广度优先遍历确定处理顺序。实验证明，该方法在多个长文本基准测试中显著优于默认顺序和基于语义评分的顺序，有效提升了回答准确性。
selection_source: fresh_fetch
motivation: 顺序多智能体推理框架中的有限内存会导致信息损耗，且最终推理结果高度依赖于文本块的处理顺序。
method: 利用 Chow-Liu 树构建分块间的依赖结构，并采用广度优先遍历生成能减少信息损失的最优处理顺序。
result: 在三个长文本基准测试中，该方法在答案相关性和精确匹配准确度上均优于默认文档顺序和语义评分排序。
conclusion: 通过优化分块排序可以有效缓解长文本推理中的信息瓶颈问题，提升多智能体协作的推理性能。
---

## 摘要
串行多智能体推理框架（如 Chain-of-Agents, CoA）通过将输入分解为块，并使用基于大语言模型（LLM）的工作智能体顺序处理这些块来处理长上下文查询，这些智能体从受限的共享内存中读取并更新信息。从概率角度来看，CoA 旨在近似对应于能够对整个长上下文进行联合推理的模型的条件分布。CoA 通过隐状态分解实现这一点，其中仅在智能体之间传递先前处理证据的有界摘要。由此产生的有界内存近似引入了有损信息瓶颈，使得最终的证据状态本质上取决于块的处理顺序。在这项工作中，我们研究了长上下文推理中的块排序问题。我们利用著名的 Chow-Liu 树来学习一种依赖结构，该结构优先处理强相关的块。实验表明，对所得树进行广度优先遍历产生的块排序减少了智能体间的信息损失，并在三个长上下文基准测试中，在答案相关性和精确匹配准确度方面始终优于默认的文档块排序和基于语义评分的排序。

## Abstract
Sequential multi-agent reasoning frameworks such as Chain-of-Agents (CoA) handle long-context queries by decomposing inputs into chunks and processing them sequentially using LLM-based worker agents that read from and update a bounded shared memory. From a probabilistic perspective, CoA aims to approximate the conditional distribution corresponding to a model capable of jointly reasoning over the entire long context. CoA achieves this through a latent-state factorization in which only bounded summaries of previously processed evidence are passed between agents. The resulting bounded-memory approximation introduces a lossy information bottleneck, making the final evidence state inherently dependent on the order in which chunks are processed.   In this work, we study the problem of chunk ordering for long-context reasoning. We use the well-known Chow-Liu trees to learn a dependency structure that prioritizes strongly related chunks. Empirically, we show that a breadth-first traversal of the resulting tree yields chunk orderings that reduce information loss across agents and consistently outperform both default document-chunk ordering and semantic score-based ordering in answer relevance and exact-match accuracy across three long-context benchmarks.

---

## 论文详细总结（自动生成）

这篇论文针对长文本推理中的顺序多智能体协作框架（Chain-of-Agents, CoA）提出了基于 Chow-Liu 树的块排序策略。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：大语言模型（LLM）在处理超长上下文时存在窗口限制。CoA 框架通过将长文本分块并由多个智能体顺序处理、维护一个有界共享内存（摘要）来解决这一问题。
*   **核心问题**：由于共享内存的大小有限，处理过程本质上是有损压缩。**分块的处理顺序**会显著影响信息的保留和最终推理结果。
*   **研究动机**：现有的 CoA 方法通常采用默认的文档顺序或简单的语义相似度排序，忽略了文本块之间复杂的相互依赖关系。作者试图寻找一种最优排序，以在内存瓶颈下最大限度地减少信息损失。

### 2. 方法论：核心思想与算法流程
*   **核心思想**：将文本块视为相互依赖的随机变量，利用概率图模型中的 **Chow-Liu 树** 来近似表示块之间的全局依赖结构。
*   **关键技术细节**：
    1.  **嵌入与相似度计算**：使用嵌入模型将每个文本块向量化，以余弦相似度作为互信息（Mutual Information）的代理指标，构建一个完全加权图。
    2.  **构建 Chow-Liu 树**：在图中计算**最大权重生成树（MWST）**。根据 Chow-Liu 算法，该树是原始联合分布的最佳二阶近似。
    3.  **确定根节点**：选择与用户查询（Query）语义最相似的块作为树的根节点。
    4.  **生成排序（CL-ORDER）**：从根节点开始进行**广度优先遍历（BFS）**。这种方式能确保语义和统计上相关的块在处理序列中彼此靠近，从而减少跨步骤压缩时的信息丢失。
    5.  **顺序推理**：按照生成的顺序 $\pi$ 调用 CoA 流程，由 Worker 智能体更新摘要，最后由 Manager 智能体生成答案。

### 3. 实验设计
*   **数据集/Benchmark**：
    *   **HELMET (LongQA)**：长文档问答。
    *   **$\infty$BENCH (LongQA-MC)**：长文本多选题。
    *   **NarrativeQA**：筛选出上下文超过 256K token 的复杂叙事问答。
*   **对比方法**：
    *   **DEFAULT**：文档原始的自然顺序。
    *   **DENSE**：基于语义相似度对块进行简单降序排列（类似 RAG 的重排序）。
*   **评估模型**：GPT-4.1、GPT-4.1-mini、Qwen-3-14B。
*   **评估指标**：Ragas 答案相关性评分、精确匹配（EM）准确率。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练/推理时长。
*   **模型配置**：实验使用了 OpenAI 的 `text-embedding-3-large` 和 Qwen 的开源嵌入模型。CoA 设置中每个块的大小为 8K token，内存预算同样为 8K token。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同规模和类型的 LLM（闭源旗舰、闭源轻量、开源模型）以及 3 个主流长文本基准测试。
*   **消融实验**：
    *   **表示方法消融**：对比了 OpenAI 嵌入、Qwen 嵌入和 BM25（词频统计），验证了方法在不同表征下的鲁棒性。
    *   **遍历策略消融**：对比了基于 Chow-Liu 树的 BFS 与直接在全图上进行深度优先搜索（DFS）的效果。
*   **充分性评价**：实验设计较为全面，通过跨模型和跨任务的对比验证了方法的普适性，消融实验有效地证明了“全局依赖建模”优于“局部贪婪搜索”。

### 6. 主要结论与发现
*   **性能提升**：CL-ORDER 在所有模型和基准测试中均优于 DEFAULT 和 DENSE。在 EM 任务中，相比默认顺序实现了 **10.68%** 的相对提升。
*   **排序的重要性**：实验证明 DENSE（仅考虑查询相关性）在某些情况下甚至不如 DEFAULT，说明**块与块之间的依赖关系**在长文本推理中比单纯的“查询-块相关性”更重要。
*   **模型敏感度**：较小的模型（如 GPT-4.1-mini 和 Qwen-3-14B）从优化排序中获益更多，说明排序策略能有效弥补模型自身处理复杂上下文能力的不足。

### 7. 优点
*   **理论严谨**：将启发式的块排序问题转化为概率图模型中的结构学习问题，具有坚实的数学基础。
*   **计算高效**：构建最大权重生成树的算法复杂度较低，相比于长文本推理本身的 LLM 调用开销，排序过程的计算量几乎可以忽略。
*   **即插即用**：该方法不改变 LLM 架构，可直接应用于任何顺序处理框架。

### 8. 不足与局限
*   **代理指标限制**：使用余弦相似度作为互信息的代理可能无法完全捕捉复杂的逻辑依赖（例如因果关系或跨度极大的引用）。
*   **分块固定**：实验中使用了固定的 8K 分块，未探讨分块大小对依赖树构建及最终推理的影响。
*   **实时性挑战**：虽然排序算法快，但对于极大规模的文档库，构建全图相似度矩阵的开销仍需关注。

（完）
