---
title: "When OpenClaw Meets Hospital: Toward an Agentic Operating System for Dynamic Clinical Workflows"
title_zh: 当 OpenClaw 走进医院：迈向面向动态临床工作流的智能体操作系统
authors: "Wenxian Yang, Hanzheng Qiu, Bangqun Zhang, Chengquan Li, Zhiyong Huang, Xiaobin Feng, Rongshan Yu, Jiahong Dong"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11721v1"
tags: ["query:agimem"]
score: 9.0
evidence: 临床LLM代理中长期记忆机制不足的问题
tldr: 本研究针对大语言模型智能体在医疗环境部署中面临的可靠性、安全性和长期记忆不足等挑战，提出了一种基于OpenClaw框架的医院智能体操作系统架构。该架构通过受限执行环境、以文档为中心的交互范式、分页索引记忆机制及医疗技能库，实现了临床工作流的自动化协调。该系统在提升医疗决策效率的同时，通过资源隔离和预定义接口确保了临床部署的安全性和可审计性，为构建动态临床工作流提供了底层计算支撑。
selection_source: fresh_fetch
motivation: 现有LLM智能体在医疗应用中存在可靠性差、安全风险高以及缺乏有效的长期记忆机制等瓶颈。
method: 提出一种基于OpenClaw的医院智能体操作系统，集成受限执行环境、文档交互范式、分页记忆架构和医疗技能库。
result: 该系统通过预定义的技能接口和资源隔离机制，实现了对复杂临床任务的自动化编排与安全管控。
conclusion: 本研究为开发安全、透明且可审计的医疗智能体系统提供了架构基础，推动了AI在动态临床环境中的落地应用。
---

## 摘要
大语言模型（LLM）智能体通过集成推理、工具调用和持久化记忆，扩展了传统的生成式模型。最近的研究表明，此类智能体可以通过自动化文档记录、协调护理流程和辅助医疗决策，显著改善临床工作流。然而，尽管取得了快速进展，但由于可靠性限制、安全风险以及长期记忆机制不足，在医疗环境中部署自主智能体仍然面临困难。本研究提出了一种使 LLM 智能体适应医院环境的架构。该设计引入了四个核心组件：受 Linux 多用户系统启发的受限执行环境、连接患者和临床医生智能体的以文档为中心的交互范式、专为长期临床上下文管理设计的页面索引记忆架构，以及支持临床任务序列即时组合的精选医学技能库。该架构并非授予智能体不受限制的系统访问权限，而是通过预定义的技能接口和资源隔离来约束其行为。我们认为，此类系统构成了医院智能体操作系统（Agentic Operating System for Hospital）的基础，这是一个能够在保持安全性、透明度和可审计性的同时协调临床工作流的计算层。本研究将设计建立在 OpenClaw 之上，这是一个将智能体能力构建为离散技能精选库的开源自主智能体框架，并针对安全临床部署所需的底层基础设施约束对其进行了扩展。

## Abstract
Large language model (LLM) agents extend conventional generative models by integrating reasoning, tool invocation, and persistent memory. Recent studies suggest that such agents may significantly improve clinical workflows by automating documentation, coordinating care processes, and assisting medical decision making. However, despite rapid progress, deploying autonomous agents in healthcare environments remains difficult due to reliability limitations, security risks, and insufficient long-term memory mechanisms. This work proposes an architecture that adapts LLM agents for hospital environments. The design introduces four core components: a restricted execution environment inspired by Linux multi-user systems, a document-centric interaction paradigm connecting patient and clinician agents, a page-indexed memory architecture designed for long-term clinical context management, and a curated medical skills library enabling ad-hoc composition of clinical task sequences. Rather than granting agents unrestricted system access, the architecture constrains actions through predefined skill interfaces and resource isolation. We argue that such a system forms the basis of an Agentic Operating System for Hospital, a computing layer capable of coordinating clinical workflows while maintaining safety, transparency, and auditability. This work grounds the design in OpenClaw, an open-source autonomous agent framework that structures agent capabilities as a curated library of discrete skills, and extends it with the infrastructure-level constraints required for safe clinical deployment.

---

## 论文详细总结（自动生成）

这篇论文提出了一种专为医院环境设计的智能体操作系统架构，旨在解决大语言模型（LLM）智能体在临床应用中的安全性、可靠性和长期记忆挑战。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：虽然 LLM 智能体在自动化临床文档和辅助决策方面潜力巨大，但现有的智能体框架（如 OpenClaw）通常假设处于开放计算环境，具有不受限的文件访问和网络权限，这与医疗隐私法规（如 HIPAA）和安全审计要求严重冲突。
*   **核心挑战**：
    1.  **安全性风险**：现有框架缺乏对智能体行为的物理隔离和权限控制。
    2.  **记忆机制缺陷**：传统的基于向量检索（RAG）的平铺式记忆会破坏临床记录的时间轴和上下文结构。
    3.  **系统僵化**：现有的医院信息系统（HIS）多为预设的固定工作流，无法应对临床中复杂多变的“长尾”需求。
*   **整体含义**：论文主张将医疗 AI 的部署视为一个**基础设施设计问题**，而非单纯的模型能力问题。通过构建“医院智能体操作系统”，在底层强制执行安全约束和结构化记忆。

### 2. 方法论：核心思想与关键技术
该系统基于 **OpenClaw** 框架扩展，提出了四个核心组件：
*   **受限执行环境（Restricted Execution Environment）**：
    *   借鉴 Linux 多用户系统，为每个角色（患者、医生、护士）分配独立的 OS 用户和命名空间。
    *   智能体仅能通过受限的“技能接口”和文件读写进行交互，禁止直接访问网络或执行任意代码。
*   **以文档为中心的交互范式（Document-Centric Interaction）**：
    *   智能体之间不进行直接对话，而是通过对共享临床文档的“追加写（Append-only Mutation）”进行协作。
    *   引入事件代理（Event Broker），当文档更新时通知相关智能体，实现异步协调。
*   **页面索引记忆架构（Page-Indexed Memory Architecture）**：
    *   **核心思想**：完全放弃向量嵌入（Embedding），采用分层文档树。
    *   **技术细节**：每个目录包含一个 `manifest.md`（清单文件），用自然语言描述子目录内容。
    *   **算法流程**：采用“渐进式披露检索（Progressive Disclosure）”。智能体从根清单开始，利用 LLM 的推理能力选择相关的子路径，逐层深入直到找到具体的临床页面。
*   **医疗技能库（Medical Skills Library）**：
    *   预定义且经过审计的离散功能模块（如：生命体征聚合、药物依从性跟踪）。
    *   支持**即时任务组合（Ad-hoc Composition）**：智能体根据临床目标，动态规划并调用一系列技能。

### 3. 实验设计
*   **实验场景**：论文并未采用传统的公开数据集进行 SOTA 对比，而是通过 **6 个典型的临床应用场景** 进行了定性验证和架构展示：
    1.  持续监测与常规随访。
    2.  急诊分诊与初步评估。
    3.  紧急情况升级（高优先级事件处理）。
    4.  药物管理与依从性跟踪。
    5.  主动风险识别与预防性干预。
    6.  多病共存患者的跨学科协作。
*   **对比对象**：主要对比了传统固定功能 HIS 系统和通用型智能体框架在处理复杂、非预设临床任务时的差异。

### 4. 资源与算力
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：该研究侧重于系统架构设计和推理侧的逻辑编排，而非模型本身的训练。其核心开销在于推理时的 LLM 调用（用于清单生成和路径选择）。

### 5. 实验数量与充分性
*   **实验规模**：论文通过逻辑推演和场景模拟展示了架构的可行性。
*   **充分性评价**：作为一篇**架构提案类论文**，其逻辑论证较为严密，覆盖了从常规护理到紧急抢救的多种复杂度。但由于缺乏大规模真实临床环境的定量性能指标（如响应延迟、检索准确率的统计学分布），其在工程落地上的极端情况表现仍需进一步验证。

### 6. 主要结论与发现
*   **基础设施决定安全性**：通过将智能体接口简化为文件读写，可以将安全问题交给成熟的 OS 内核（如 SELinux、AppArmor）处理，使不安全行为在结构上变得不可能。
*   **清单导航优于向量检索**：在处理具有严密逻辑和时间顺序的临床记录时，基于清单的层级导航比平铺的向量检索更具可解释性，且无需维护复杂的向量数据库。
*   **动态组合解决长尾需求**：智能体通过组合原子技能，能够处理 HIS 系统未预设的复杂临床路径。

### 7. 优点
*   **安全性极高**：利用 Linux 原生权限模型，实现了比“提示词工程”更可靠的物理隔离。
*   **可解释性强**：智能体检索记忆的路径（阅读了哪些清单、选择了哪些分支）形成了天然的审计追踪。
*   **低维护成本**：无需重新计算 Embedding，文档更新时仅需增量更新受影响的清单文件。

### 8. 不足与局限
*   **清单准确性依赖**：清单由 LLM 生成，若生成摘要时出现幻觉或遗漏关键信息，会导致检索失败。
*   **推理成本**：每一层导航都需要一次 LLM 调用，在文档树极深或更新极频繁（如 ICU 实时数据）时，API 开销和延迟可能较高。
*   **并发冲突**：文中未详细说明多个智能体同时修改同一文档时的冲突解决机制（如乐观锁的具体实现）。
*   **系统集成挑战**：如何将该架构与现有的 HL7 FHIR 等医疗数据标准及 Epic/Cerner 等主流 EHR 系统进行双向同步，文中尚未给出具体方案。

（完）
