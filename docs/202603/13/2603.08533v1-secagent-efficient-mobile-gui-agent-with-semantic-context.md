---
title: "SecAgent: Efficient Mobile GUI Agent with Semantic Context"
title_zh: SecAgent：具有语义上下文的高效移动端 GUI 智能体
authors: "Yiping Xie, Song Chen, Jingxuan Xing, Wei Jiang, Zekun Zhu, Yingyao Wang, Pi Bu, Jun Song, Yuning Jiang, Bo Zheng"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08533v1"
tags: ["query:agimem"]
score: 8.0
evidence: 提炼历史表示的语义上下文机制
tldr: 针对移动端GUI智能体面临的非英语数据集匮乏和历史信息表示效率低的问题，本文提出了3B规模的高效智能体SecAgent。研究者构建了包含18k个定位样本和121k个导航步骤的大规模中文移动GUI数据集及基准测试。通过引入语义上下文机制，将冗长的历史截图和动作提炼为简洁的自然语言摘要，并结合监督微调与强化学习，使SecAgent在保持低计算成本的同时，性能达到了7B-8B模型水平。
selection_source: carryover_cache
motivation: 现有移动GUI智能体缺乏高质量多语言数据集，且处理历史信息的方法效率低下。
method: 提出了语义上下文机制，将历史操作和截图转化为自然语言摘要，并结合大规模中文数据集进行监督和强化学习微调。
result: SecAgent在中文导航基准上优于同规模模型，且性能可与7B-8B规模的模型相媲美。
conclusion: 本研究通过高质量数据集和高效的上下文表示方法，显著提升了轻量级移动GUI智能体的自动化任务处理能力。
---

## 摘要
由多模态大语言模型驱动的移动端图形用户界面（GUI）智能体在自动化复杂智能手机任务方面展现出了巨大的潜力。然而，现有方法面临两个关键局限：高质量多语言数据集的稀缺（特别是针对非英语生态系统），以及低效的历史表示方法。为了应对这些挑战，我们提出了 SecAgent，一个 3B 参数规模的高效移动端 GUI 智能体。我们首先构建了一个经过人工验证的中文移动端 GUI 数据集，包含跨 44 个应用程序的 1.8 万个定位样本和 12.1 万个导航步骤，以及一个具有多选动作标注的中文导航基准。基于该数据集，我们提出了一种语义上下文机制，将历史截图和动作提炼为简洁的自然语言摘要，在保留任务相关信息的同时显著降低了计算成本。通过监督微调和强化学习微调，SecAgent 在我们自建的以及公开的导航基准测试中，表现优于同等规模的基准模型，并达到了与 7B-8B 模型相当的性能。我们将开源训练数据集、基准、模型和代码，以推动多语言移动端 GUI 自动化领域的研究。

## Abstract
Mobile Graphical User Interface (GUI) agents powered by multimodal large language models have demonstrated promising capabilities in automating complex smartphone tasks. However, existing approaches face two critical limitations: the scarcity of high-quality multilingual datasets, particularly for non-English ecosystems, and inefficient history representation methods. To address these challenges, we present SecAgent, an efficient mobile GUI agent at 3B scale. We first construct a human-verified Chinese mobile GUI dataset with 18k grounding samples and 121k navigation steps across 44 applications, along with a Chinese navigation benchmark featuring multi-choice action annotations. Building upon this dataset, we propose a semantic context mechanism that distills history screenshots and actions into concise, natural language summaries, significantly reducing computational costs while preserving task-relevant information. Through supervised and reinforcement fine-tuning, SecAgent outperforms similar-scale baselines and achieves performance comparable to 7B-8B models on our and public navigation benchmarks. We will open-source the training dataset, benchmark, model, and code to advance research in multilingual mobile GUI automation.

---

## 论文详细总结（自动生成）

以下是对论文《SecAgent: Efficient Mobile GUI Agent with Semantic Context》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：移动端 GUI 智能体（Agent）在自动化复杂任务方面潜力巨大，但目前面临两大瓶颈。
*   **核心问题**：
    1.  **数据集匮乏**：高质量的非英语（尤其是中文）移动端 GUI 数据集稀缺，且现有的大规模中文数据集多为私有。
    2.  **历史表示低效**：现有的多步导航方法要么只保留动作序列（信息不足），要么保留多张历史截图（计算开销巨大，视觉 Token 数量激增），导致推理延迟高且效率低下。

### 2. 方法论
*   **核心思想**：提出**语义上下文机制（Semantic Context Mechanism）**，将冗长的历史截图和动作序列压缩为简洁的自然语言摘要，从而在极低计算成本下保留关键历史信息。
*   **关键技术细节**：
    *   **输入构造**：模型输入包括当前指令 $I$、当前截图 $X_t$、上一时刻的语义上下文 $C_{t-1}$、上一时刻截图 $X_{t-1}$ 和动作 $A_{t-1}$。
    *   **输出结构**：模型自回归生成更新后的语义上下文 $C_t$（记录已完成的操作和发现）、思维链 $H_t$（推理过程）以及最终动作 $A_t$。
    *   **训练流程**：
        1.  **监督微调 (SFT)**：将定位（Grounding）数据转换为导航兼容格式（将 Bbox 转换为中心点坐标），并与导航数据混合训练。
        2.  **强化学习微调 (RFT)**：采用 **GRPO 算法**，利用基于规则的奖励（格式奖励 + 动作正确性奖励）进一步增强模型的推理和泛化能力。

### 3. 实验设计
*   **数据集与基准**：
    *   **CMGUI 数据集**：包含 1.8 万个定位样本和 12.1 万个导航步骤，覆盖 44 个中文 App，经过严格人工验证。
    *   **CMGUI-Bench**：包含 390 个完整轨迹，特色是提供**多选动作标注（Multi-choice）**，即承认在某些步骤下存在多个合理的正确操作。
    *   **公开基准**：在英文基准 AndroidControl 和 GUIOdyssey 上进行跨语言验证。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Claude-3-Sonnet。
    *   **开源模型**：Qwen2.5-VL (3B/7B), UI-TARS-7B, OS-Atlas-7B, AgentCPM-GUI-8B, UI-Venus-Navi-7B 等。

### 4. 资源与算力
*   **基础模型**：Qwen2.5-VL-3B。
*   **训练细节**：SFT 阶段使用 LoRA 微调，RFT 阶段使用全参数微调。
*   **算力说明**：论文中未明确给出具体的 GPU 型号和数量，但提供了详细的超参数（如 Batch Size 为 2-8，学习率为 1e-6 至 2e-5 等）。在效率评估中，对比了输入 Token 数（ITC）、首字延迟（TTFT）和每秒 Token 数（TPS）。

### 5. 实验数量与充分性
*   **实验规模**：进行了多维度的评估，包括中文基准测试、英文基准测试、消融实验以及域外（OOD）App 泛化性测试。
*   **充分性评价**：实验设计非常充分且客观。
    *   **消融实验**：验证了历史窗口大小（$N=0, 1, 2, 5$）对性能与效率的影响，证明了 $N=1$ 配合语义上下文即可达到 $N=5$ 原始截图的效果。
    *   **泛化性验证**：通过在未见过的 App 上进行测试，验证了 RFT 对模型泛化能力的显著提升。

### 6. 主要结论与发现
*   **性能卓越**：SecAgent-3B 在中文基准上的步骤准确率（SA）达到 96.4%，任务准确率（TA）达到 80.0%，远超同规模模型，甚至优于部分 7B-8B 模型。
*   **效率极高**：语义上下文机制在不损失准确率的前提下，相比保留 5 张历史截图的方法，输入 Token 数减少了约 38%，首字延迟降低了一半。
*   **RFT 的价值**：在 SFT 基础上加入 RFT，使任务准确率（TA）提升了 7.2%，证明了强化学习在复杂 GUI 导航逻辑中的关键作用。

### 7. 优点
*   **高质量资源贡献**：开源了大规模、人工验证且带多选标注的中文 GUI 数据集，填补了社区空白。
*   **创新的历史表示**：语义上下文机制巧妙地利用了 LLM 的语言理解能力来替代沉重的视觉计算，实现了性能与效率的平衡。
*   **训练方案完善**：通过 Grounding 数据格式对齐和两阶段微调，使小规模模型（3B）迸发出极强的竞争力。

### 8. 不足与局限
*   **视觉依赖限制**：虽然引入了语义上下文，但模型仍主要依赖最近一两帧截图，对于需要极长程视觉记忆的任务可能仍有挑战。
*   **App 覆盖范围**：虽然覆盖了 44 个主流 App，但面对海量的长尾 App，其领域知识可能仍存在盲区。
*   **多语言局限**：尽管在英文基准上表现良好，但核心训练数据仍集中在中文，跨语言迁移的深度仍有待进一步探索。

（完）
