---
title: "Adaptive Loops and Memory in Transformers: Think Harder or Know More?"
title_zh: Transformer 中的自适应循环与记忆：思考得更深还是知道得更多？
authors: "Markus Frey, Behzad Shomali, Ali Hamza Bashir, David Berghaus, Joachim Koehler, Mehdi Ali"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08391v3"
tags: ["query:agimem"]
score: 8.0
evidence: 自适应循环与门控记忆库存储
tldr: 本研究探讨了Transformer模型中自适应循环与门控记忆库的结合。针对循环模型存储容量不足的问题，作者提出了一种通过学习停止机制进行迭代计算，并利用记忆库增强存储的方法。实验表明，循环机制显著提升了数学推理能力，而记忆库则改善了常识任务表现。该组合模型在相同计算量下优于更深层的基准模型，并展现出层间的功能专业化，为高效推理提供了新思路。
selection_source: fresh_fetch
motivation: 循环Transformer虽然参数效率高但存储容量有限，且传统的思维链推理需要显式输出，限制了隐状态下的深度推理能力。
method: 提出一种结合了自适应逐层循环（通过学习停止机制）和门控记忆库的新型Transformer架构，以平衡计算深度与存储需求。
result: 循环机制增强了数学推理，记忆库提升了常识任务性能，组合模型在同等FLOPs下优于层数多三倍的基准模型。
conclusion: 自适应循环与记忆库的结合能有效提升模型在复杂推理和知识密集型任务中的表现，并诱导模型产生层间功能专业化。
---

## 摘要
思维链（CoT）提示使语言模型能够进行推理，但需要显式地将中间步骤语言化。循环 Transformer（Looped transformers）提供了一种替代方案，通过在隐藏状态内迭代地细化表示。这种参数效率是有代价的，因为循环模型缺乏更深层模型的存储能力，而后者在每一层都使用唯一的权重。在这项工作中，我们研究了同时具备自适应逐层循环（每个 Transformer 块通过学习到的停机机制学习迭代其隐藏状态）和门控记忆库（提供额外的学习存储）的 Transformer 模型。我们发现，与参数量和 FLOPs 匹配的模型相比，循环主要有利于数学推理，而记忆库则有助于恢复在常识任务上的性能。结合这两种机制所产生的模型在数学基准测试中优于具有三倍层数的等效 FLOPs 基准模型。对模型内部的分析揭示了层的专业化：早期层学习进行极少的循环并谨慎地访问记忆，而后期层则更频繁地进行这两项操作。

## Abstract
Chain-of-thought (CoT) prompting enables reasoning in language models but requires explicit verbalization of intermediate steps. Looped transformers offer an alternative by iteratively refining representations within hidden states. This parameter efficiency comes at a cost, as looped models lack the storage capacity of deeper models which use unique weights per layer. In this work, we investigate transformer models that feature both adaptive per-layer looping, where each transformer block learns to iterate its hidden state via a learned halting mechanism, and gated memory banks, that provide additional learned storage. We find that looping primarily benefits mathematical reasoning, while memory banks help recover performance on commonsense tasks compared to parameter and FLOP matched models. Combining both mechanisms yields a model that outperforms an iso-FLOP baseline, with three times the number of layers, across math benchmarks. Analysis of model internals reveals layer specialization: early layers learn to loop minimally and access memory sparingly, while later layers do both more heavily.

---

## 论文详细总结（自动生成）

这篇论文由 Lamarr 研究所和波恩大学的研究人员发表于 ICLR 2026 的 Latent & Implicit Thinking 工作坊。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：大语言模型通常依靠思维链（CoT）进行推理，但这需要显式的文本生成。循环 Transformer（Looped Transformers）通过在隐藏状态内进行迭代计算来实现“隐式推理”，具有极高的参数效率。
*   **核心矛盾**：循环模型虽然增加了计算深度，但由于重复使用同一组参数，其存储知识的容量（Capacity）远低于具有相同计算量的深层模型。
*   **核心问题**：能否通过结合“自适应循环”（增加计算深度/思考）和“外部记忆库”（增加存储容量/知识），让模型在保持参数效率的同时，既能“想得深”又能“记得多”？

### 2. 方法论：核心思想与关键技术
论文提出了一种增强型 Transformer 架构，包含两个核心组件：
*   **自适应循环（Adaptive Looping）**：
    *   借鉴 PonderNet，每个 Transformer 块可以迭代运行多次（最高 $N_{max}$ 次）。
    *   引入**学习停止机制（Halting Mechanism）**：通过一个路由器预测每一步的停止概率，最终输出是所有迭代步骤状态的加权组合。
    *   **可学习缩放参数**：初始时将循环部分设为接近恒等变换，让模型在训练过程中逐渐学会何时介入计算。
*   **门控记忆库（Gated Memory Banks）**：
    *   **本地记忆（Local Memory）**：每一层特有的存储空间，用于存放该深度相关的特定知识。
    *   **全局记忆（Global Memory）**：所有层共享的存储空间，用于存放通用知识。
    *   **检索与集成**：使用 QK-归一化注意力机制从静态可学习参数（非 KV-cache）中检索信息，并通过**输入相关的门控（Gating）**控制记忆信息流入残差流的比例。

### 3. 实验设计
*   **数据集**：在 140 亿（14B）Token 的 FineWeb-Edu 数据集上进行预训练。
*   **评估基准**：
    *   **常识任务（CS）**：ARC、HellaSwag、PIQA、Winogrande 等（评估准确率和 BPB）。
    *   **数学任务（Math）**：代数、几何、数论、预微积分等（评估 BPB，即每字节比特数，越低越好）。
*   **对比方法（Baselines）**：
    *   **Iso-Parameter (IsoPar)**：通过加宽 FFN 达到相同参数量的标准模型。
    *   **Iso-FLOP (IsoFLOP)**：通过增加层数（如 36 层）达到与循环模型相同计算量的标准模型。

### 4. 资源与算力
*   **模型规模**：基础模型为 12 层、2 亿（200M）参数的 Decoder-only Transformer。
*   **训练细节**：使用 AdamW 优化器，Batch Size 约 36 万 Token，共训练约 3.8 万步。
*   **算力说明**：文中未明确提及具体的 GPU 型号和数量，但提到使用了 PyTorch 原生的 Modalities 训练框架。

### 5. 实验数量与充分性
*   **实验设置**：涵盖了不同循环次数（$N_{max}=3, 5, 7$）、不同记忆门控初始化（关闭、平衡、开启）以及多种基准模型的对比。
*   **充分性**：实验设计较为严谨，通过 Iso-Parameter 和 Iso-FLOP 两个维度控制变量，确保了对比的公平性。此外，还进行了关于训练动力学和层专业化的深入分析。

### 6. 主要结论与发现
*   **功能分工**：**循环机制**显著提升了数学推理能力（BPB 降低 22%）；而**记忆库**主要帮助模型在常识任务上恢复因参数共享而损失的性能。
*   **超越深层模型**：在数学任务上，结合了循环和记忆的模型优于层数多出三倍的 Iso-FLOP 基准模型，证明了循环比单纯堆叠层数更具参数效率。
*   **层专业化**：模型自动演化出分工——早期层循环极少且几乎不访问记忆；后期层则高度依赖循环和记忆进行复杂推理。
*   **阶段性转变**：模型并非从一开始就学习循环，而是在语言建模损失（Cross-Entropy）降至约 3.27 之后，才开始爆发式地利用循环机制。

### 7. 优点
*   **架构创新**：有机结合了自适应计算（Pondering）和神经图灵机式的记忆增强，解决了循环 Transformer 的固有缺陷。
*   **分析深入**：不仅关注最终分数，还通过可视化揭示了模型“何时开始思考”以及“各层如何分工”的内在逻辑。
*   **效率验证**：在同等计算预算下证明了该架构的优越性。

### 8. 不足与局限
*   **模型规模较小**：实验仅在 200M 规模上进行，尚不确定相关结论在百亿或千亿参数模型中是否依然成立。
*   **数学评估指标**：数学任务仅使用了 BPB（预测概率），而非直接的解题准确率，这可能无法完全反映真实的逻辑推理水平。
*   **计算开销平衡**：虽然参数效率高，但循环增加了推理时的延迟（FLOPs 增加），论文未提供详尽的推理延迟与性能增益的帕累托前沿分析。

（完）
