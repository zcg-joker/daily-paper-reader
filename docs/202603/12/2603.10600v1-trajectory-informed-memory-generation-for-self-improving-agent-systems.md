---
title: Trajectory-Informed Memory Generation for Self-Improving Agent Systems
title_zh: 面向自我改进智能体系统的轨迹感知记忆生成
authors: "Gaodan Fang, Vatche Isahagian, K. R. Jayaram, Ritesh Kumar, Vinod Muthusamy, Punleuk Oum, Gegi Thomas"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10600v1"
tags: ["query:agimem"]
score: 9.0
evidence: 从执行轨迹中提取可操作的学习成果用于上下文记忆检索
tldr: 针对LLM智能体在执行任务时重复错误且难以从经验中学习的问题，本文提出一种基于轨迹感知的记忆生成框架。该框架通过分析执行轨迹的语义模式和决策归因，自动提取策略、恢复和优化建议，并通过自适应检索系统在未来任务中注入相关经验。实验表明，该方法在AppWorld基准测试中显著提升了任务成功率，尤其在复杂任务上表现优异。
selection_source: fresh_fetch
motivation: 现有的LLM智能体往往无法从过去的执行经验中有效学习，导致其在面对相似任务时重复低效模式或无法从错误中恢复。
method: 提出一个包含轨迹智能提取、决策归因分析、上下文学习生成和自适应记忆检索四个组件的自动化框架，从执行轨迹中提取结构化教训。
result: "在AppWorld基准测试中，该方法使场景目标完成率最高提升了14.3个百分点，在复杂任务上实现了149%的相对增长。"
conclusion: 通过从执行轨迹中提取具有因果关系的结构化经验，可以显著增强智能体的自我进化能力并提升其处理复杂任务的性能。
---

## 摘要
大语言模型（LLM）驱动的智能体面临着一个持久的挑战：如何从执行经验中学习以提升未来的性能。尽管智能体能够成功完成许多任务，但它们往往会重复低效的模式，无法从类似的错误中恢复，并错失应用以往执行中成功策略的机会。我们提出了一种新颖的框架，用于从智能体执行轨迹中自动提取可操作的学习成果，并通过上下文记忆检索利用这些成果来提升未来性能。我们的方法包含四个组件：(1) 轨迹智能提取器（Trajectory Intelligence Extractor），对智能体推理模式进行语义分析；(2) 决策归因分析器（Decision Attribution Analyzer），识别哪些决策和推理步骤导致了失败、恢复或低效；(3) 上下文学习生成器（Contextual Learning Generator），产生三类指导——来自成功模式的策略提示、来自失败处理的恢复提示，以及来自低效但成功执行的优化提示；(4) 自适应记忆检索系统（Adaptive Memory Retrieval System），基于多维相似性将相关的学习成果注入智能体提示词中。与存储通用对话事实的现有记忆系统不同，我们的框架能够理解执行模式，提取具有溯源信息的结构化学习成果，并检索针对特定任务上下文定制的指导。在 AppWorld 基准测试上的评估表明了持续的改进，在留存任务的情景目标完成率上提升了高达 14.3 个百分点，在复杂任务上的收益尤为显著（情景目标提升 28.5 个百分点，相对增长 149%）。

## Abstract
LLM-powered agents face a persistent challenge: learning from their execution experiences to improve future performance. While agents can successfully complete many tasks, they often repeat inefficient patterns, fail to recover from similar errors, and miss opportunities to apply successful strategies from past executions. We present a novel framework for automatically extracting actionable learnings from agent execution trajectories and utilizing them to improve future performance through contextual memory retrieval. Our approach comprises four components: (1) a Trajectory Intelligence Extractor that performs semantic analysis of agent reasoning patterns, (2) a Decision Attribution Analyzer that identifies which decisions and reasoning steps led to failures, recoveries, or inefficiencies, (3) a Contextual Learning Generator that produces three types of guidance -- strategy tips from successful patterns, recovery tips from failure handling, and optimization tips from inefficient but successful executions, and (4) an Adaptive Memory Retrieval System that injects relevant learnings into agent prompts based on multi-dimensional similarity. Unlike existing memory systems that store generic conversational facts, our framework understands execution patterns, extracts structured learnings with provenance, and retrieves guidance tailored to specific task contexts. Evaluation on the AppWorld benchmark demonstrates consistent improvements, with up to 14.3 percentage point gains in scenario goal completion on held-out tasks and particularly strong benefits on complex tasks (28.5~pp scenario goal improvement, a 149\% relative increase).

---

## 论文详细总结（自动生成）

这篇论文由 IBM Research 的团队撰写，提出了一种名为“轨迹感知记忆生成”（Trajectory-Informed Memory Generation）的框架，旨在解决 LLM 智能体在执行任务时“健忘”且无法从过去经验中学习的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：尽管 LLM 智能体在 API 调用和任务编排方面表现出色，但由于 LLM 本身是无状态的，智能体往往表现出“失忆症”。
*   **核心问题**：智能体在面对重复任务时，经常重复低效的模式，无法从相似的错误中吸取教训，也无法自动应用过去成功的策略。
*   **现有局限**：目前的记忆系统（如 Mem0）多侧重于存储对话事实（Fact），而非执行模式；规则系统难以维护；强化学习则成本过高且缺乏可解释性。

### 2. 方法论
该框架通过一个三阶段的流水线将原始执行轨迹转化为可操作的指导：

*   **阶段 1：轨迹分析与提示提取**
    *   **轨迹智能提取器**：对智能体的推理（Thoughts）进行语义分类（分析、计划、验证、反思），识别认知模式。
    *   **决策归因分析器**：进行因果分析，区分失败的直接原因、近因和根源，并识别导致成功恢复的关键决策。
    *   **上下文学习生成器**：生成三类结构化提示（Tips）：
        1.  **策略提示（Strategy）**：来自干净成功的执行，强调预防性验证。
        2.  **恢复提示（Recovery）**：来自失败后成功的案例，记录错误识别与修正步骤。
        3.  **优化提示（Optimization）**：针对低效但成功的执行，提出更高效的替代方案（如批量操作）。
    *   **粒度选择**：支持“任务级”和“子任务级”提取，后者通过将轨迹分解为通用模块（如身份验证、分页检索）来增强跨任务的可重用性。

*   **阶段 2：存储与管理**
    *   通过实体抽象和动作规范化对描述进行泛化，利用语义聚类和 LLM 合并冗余或冲突的提示。

*   **阶段 3：运行时检索**
    *   提供**余弦相似度检索**（低成本、快速）和 **LLM 引导选择**（高精度、考虑元数据过滤）两种策略。

### 3. 实验设计
*   **数据集/场景**：使用 **AppWorld** 基准测试，这是一个模拟真实世界多应用交互（电商、邮件、日历等）的复杂环境。
*   **评估指标**：
    *   **TGC (Task Goal Completion)**：单个任务的成功率。
    *   **SGC (Scenario Goal Completion)**：同一场景下所有任务变体的连贯成功率（更严格）。
*   **对比方法**：
    *   Base Agent（无记忆的基础智能体）。
    *   不同粒度（任务级 vs 子任务级）的配置。
    *   不同检索策略（余弦相似度 vs LLM 引导）的对比。

### 4. 资源与算力
*   **模型使用**：实验主要基于 **GPT-4o** 进行智能体推理和提示提取。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于该研究主要基于商业模型 API 进行推理和处理，不涉及大规模的基础模型预训练，因此算力消耗主要体现在 API 调用成本上。

### 5. 实验数量与充分性
*   **实验规模**：在 AppWorld 的训练集、开发集和测试集（Test-Normal）上进行了广泛测试。
*   **充分性**：实验涵盖了从简单（Difficulty 1）到极难（Difficulty 3）的三个难度等级。
*   **客观性**：通过留存测试集（Held-out tasks）验证了泛化能力，并进行了消融实验来对比检索阈值（$\tau$）和 Top-k 设置对结果的影响，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **显著提升**：在测试集上，该框架使 SGC 提升了 **14.3 个百分点**。
*   **复杂任务受益最大**：在最困难的任务（Difficulty 3）中，SGC 从 19.1% 提升至 47.6%，实现了 **149% 的相对增长**。
*   **一致性增强**：记忆系统对 SGC 的提升远高于 TGC，说明它显著增强了智能体在处理相关任务变体时的一致性和鲁棒性。
*   **粒度影响**：子任务级提取在 TGC 上表现更好，而 LLM 引导的检索在维持场景一致性（SGC）方面至关重要。

### 7. 优点
*   **结构化学习**：不同于传统的非结构化记忆，该方法区分了策略、恢复和优化，使指导更具针对性。
*   **因果归因**：能够追溯到导致失败的根源决策，而非仅仅记录结果。
*   **可解释性与溯源**：每个提示都保留了来源轨迹的 ID，便于审计和调试。
*   **子任务复用**：通过分解轨迹，实现了跨领域（如从 Spotify 到 Venmo）的经验迁移。

### 8. 不足与局限
*   **模型依赖**：该框架高度依赖高性能 LLM（如 GPT-4o）进行轨迹分析和提示生成，在较弱的模型上效果可能打折扣。
*   **检索噪声**：实验显示，如果检索阈值设置过低，引入的无关提示可能会干扰智能体的判断。
*   **实时性挑战**：LLM 引导的检索虽然准确，但增加了一次额外的 API 调用开销，可能增加响应延迟。
*   **应用限制**：目前主要在 API 驱动的智能体上验证，对于长程开放式探索任务的效果尚待观察。

（完）
