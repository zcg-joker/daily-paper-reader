---
title: Hybrid Self-evolving Structured Memory for GUI Agents
title_zh: 面向 GUI 智能体的混合自演化结构化记忆
authors: "Sibo Zhu, Wenyi Wu, Kun Zhou, Stephen Wang, Biwei Huang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10291v1"
tags: ["query:agimem"]
score: 10.0
evidence: 智能体长程工作流的图结构记忆
tldr: 针对GUI智能体在处理长流程、复杂界面及错误恢复时的局限性，本文受人脑启发提出HyMEM（混合自进化结构化记忆）。HyMEM采用图结构组织记忆，结合离散高层符号节点与连续轨迹嵌入，支持多跳检索、节点自进化更新及推理时的实时工作记忆刷新。实验证明，HyMEM显著提升了开源模型性能，使其在多个基准测试中超越了GPT-4o等闭源模型。
selection_source: fresh_fetch
motivation: 现有的GUI智能体外部记忆多采用扁平化检索，缺乏人类记忆的结构化组织和自进化能力，难以应对复杂的现实任务。
method: 提出一种基于图结构的混合记忆系统HyMEM，通过符号节点与轨迹嵌入的耦合，实现多跳检索、节点动态更新和推理中的工作记忆刷新。
result: "HyMEM使Qwen2.5-VL-7B性能提升22.5%，并助力开源小模型在GUI任务上超越Gemini 1.5 Pro和GPT-4o。"
conclusion: 结构化且可自进化的记忆机制是提升GUI智能体处理复杂、长程任务能力的关键路径。
---

## 摘要
视觉语言模型（VLM）的显著进展使得 GUI 智能体能够以类人的方式与计算机进行交互。然而，由于长程工作流、多样化的界面以及频繁的中间错误，现实世界的计算机使用任务仍然具有挑战性。先前的研究通过大量轨迹集合为智能体构建外部记忆，但依赖于对离散摘要或连续嵌入的扁平化检索，缺乏人类记忆的结构化组织和自演化特性。受大脑启发，我们提出了混合自演化结构化记忆（HyMEM），这是一种基于图的记忆，将离散的高层符号节点与连续的轨迹嵌入相结合。HyMEM 维持一种图结构，以支持多跳检索、通过节点更新操作实现的自演化，以及推理过程中的即时工作记忆刷新。大量实验表明，HyMEM 持续提升了开源 GUI 智能体的性能，使 7B/8B 参数规模的主干网络能够媲美或超越强大的闭源模型；值得注意的是，它将 Qwen2.5-VL-7B 的性能提升了 22.5%，并优于 Gemini2.5-Pro-Vision 和 GPT-4o。

## Abstract
The remarkable progress of vision-language models (VLMs) has enabled GUI agents to interact with computers in a human-like manner. Yet real-world computer-use tasks remain difficult due to long-horizon workflows, diverse interfaces, and frequent intermediate errors. Prior work equips agents with external memory built from large collections of trajectories, but relies on flat retrieval over discrete summaries or continuous embeddings, falling short of the structured organization and self-evolving characteristics of human memory. Inspired by the brain, we propose Hybrid Self-evolving Structured Memory (HyMEM), a graph-based memory that couples discrete high-level symbolic nodes with continuous trajectory embeddings. HyMEM maintains a graph structure to support multi-hop retrieval, self-evolution via node update operations, and on-the-fly working-memory refreshing during inference. Extensive experiments show that HyMEM consistently improves open-source GUI agents, enabling 7B/8B backbones to match or surpass strong closed-source models; notably, it boosts Qwen2.5-VL-7B by +22.5% and outperforms Gemini2.5-Pro-Vision and GPT-4o.

---

## 论文详细总结（自动生成）

这是一份关于论文《Hybrid Self-evolving Structured Memory for GUI Agents》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管视觉语言模型（VLM）在 GUI 智能体领域取得了进展，但在处理长流程任务、应对多样化界面以及从中间错误中恢复时仍面临巨大挑战。
*   **核心痛点**：现有的智能体记忆系统通常采用“扁平化”的存储和检索方式（如简单的文本摘要或连续向量检索），缺乏人类记忆那种将具体经验与抽象策略相结合的**结构化组织**能力，也缺乏随新经验不断**自我演化**的动态机制。
*   **研究目标**：受人脑记忆机制（海马体处理具体经验，新皮层提取抽象规律）的启发，构建一种既能保留多模态细节又能进行高层逻辑推理的混合结构化记忆系统。

### 2. 方法论：HyMEM 系统架构
HyMEM（Hybrid Self-evolving Structured Memory）的核心由三个关键部分组成：

*   **混合图结构记忆（Hybrid Structured Memory Graph）**：
    *   **节点设计**：每个节点包含三个层级：**高层策略**（离散符号，描述核心逻辑）、**中层属性**（语义标签，如 #搜索、#价格）、**低层轨迹嵌入**（连续向量，保留细粒度的视觉和动作细节）。
    *   **图连接**：共享相同中层属性的节点之间建立边，形成关联拓扑结构，支持多跳检索。
*   **记忆的自演化构建（Self-Evolving Construction）**：
    *   当新轨迹产生时，系统通过 VLM 充当“法官”进行冗余检查，执行三种操作：**ADD**（发现新策略时新增节点）、**MERGE**（策略相同但有补充信息时合并）、**REPLACE**（发现更优执行路径时替换旧节点）。
*   **推理时的动态利用**：
    *   **结构化检索**：采用“种子与扩展”策略，先通过语义相似度找到种子节点，再通过图结构扩展到 1-hop 邻居，获取更多样化的上下文。
    *   **即时刷新（On-the-fly Refresh）**：在执行过程中，VLM 会检测任务是否进入新阶段（如从“搜索”转入“结账”），一旦检测到阶段偏移，立即刷新工作记忆，确保上下文的实时相关性。

### 3. 实验设计
*   **数据集与场景**：
    *   **WebVoyager**：涵盖 Amazon、Google Maps 等真实网站的端到端任务。
    *   **Multimodal-Mind2Web**：跨域、跨网站的通用 Web 任务。
    *   **MMInA**：需要多步推理和跨域规划的复杂基准。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Gemini 2.5 Pro Vision, Claude 4。
    *   **开源基座**：Qwen2.5-VL (7B/32B), Qwen3-VL (8B), UI-TARS-1.5 (7B)。
    *   **记忆增强基准**：ReasoningBank（离散记忆）、AWM（工作流记忆）、CoMEM（连续记忆）。

### 4. 资源与算力
*   **硬件环境**：实验使用了单张 **NVIDIA H100 GPU**。
*   **训练细节**：采用了参数高效微调（PEFT），仅更新了 Q-Former 和 LoRA 层（约占总参数的 **1.2%**）。
*   **训练时长**：整个训练过程在 **20 小时**内完成，且仅需 1 个 Epoch 即可收敛，显示出极高的训练效率。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个主流 Benchmark 上进行了详尽测试，涵盖了购物、旅游、地图导航、百科等多个真实领域。
*   **消融实验**：
    *   验证了**全局演化**（记忆库更新）和**局部演化**（推理时刷新）的各自贡献。
    *   分析了**记忆规模**（从 500 到 8000 条轨迹）对性能的缩放效应（Scaling Law）。
    *   探讨了检索策略中**相似性与多样性**的平衡。
*   **客观性**：实验对比了多种不同架构的开源模型，并与当前最顶尖的闭源模型进行了直接对标，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能飞跃**：HyMEM 显著提升了开源模型的表现。例如，将 **Qwen2.5-VL-7B** 的成功率从 12.5% 提升至 **35.0%**（+22.5%）。
*   **以小博大**：搭载 HyMEM 的 7B/8B 规模开源模型在平均性能上**超越了 GPT-4o 和 Gemini 2.5 Pro Vision**。
*   **演化优势**：自演化机制能有效压缩冗余信息（图节点增长呈次线性），同时保持记忆的高质量。
*   **动态刷新至关重要**：在长程任务中，能够感知“阶段转换”并刷新记忆的智能体，其成功率远高于使用静态记忆的智能体。

### 7. 优点（亮点）
*   **脑启发式架构**：成功将离散符号的逻辑性与连续嵌入的感知细节结合在图结构中。
*   **高效的演化机制**：通过 ADD/MERGE/REPLACE 解决了外部记忆无限膨胀和过时的问题。
*   **推理灵活性**：引入了推理时的“工作记忆刷新”，解决了长程任务中的上下文漂移问题。
*   **极高的资源效率**：仅需单卡和极少量的参数微调即可实现 SOTA 性能。

### 8. 不足与局限
*   **更新决策的启发式**：目前记忆更新的决策依赖于 VLM 的 Prompt 启发式判断，而非通过强化学习（RL）等更系统的方法进行优化。
*   **模型规模覆盖**：受限于算力，实验主要集中在 7B/8B 模型上，尚未在 32B 或 70B 以上的超大规模模型上验证其增益是否会边际递减。
*   **安全性与伦理**：虽然实验在公开网站进行，但智能体在真实世界大规模部署时的误操作风险和隐私保护仍需更深入的安全机制设计。

（完）
