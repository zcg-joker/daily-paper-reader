---
title: "KernelSkill: A Multi-Agent Framework for GPU Kernel Optimization"
title_zh: KernelSkill：一种用于 GPU 算子优化的多智能体框架
authors: "Qitong Sun, Jun Han, Tianlin Li, Zhe Tang, Sheng Chen, Fei Yang, Aishan Liu, Xianglong Liu, Yang Liu"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10085v1"
tags: ["query:agimem"]
score: 9.0
evidence: 具有长期和短期记忆的双层记忆架构
tldr: "针对现有LLM在GPU算子优化中依赖隐式启发式导致效率低下的问题，本文提出KernelSkill多智能体框架。该框架引入双层记忆架构，利用长期记忆存储可复用的专家优化技能，利用短期记忆记录任务轨迹以避免重复尝试。实验表明，KernelSkill在KernelBench上取得了100%的成功率，并显著提升了算子执行速度，为自动化高性能算子开发提供了新思路。"
selection_source: fresh_fetch
motivation: 现有的基于大语言模型的GPU算子优化方法依赖不透明的隐式启发式策略，导致优化过程效率低下且缺乏可解释性。
method: 提出一种具有双层记忆架构的多智能体框架，通过长期记忆存储专家技能并利用短期记忆防止重复回溯。
result: "在KernelBench测试中实现100%成功率，且相比Torch Eager在不同难度级别上分别获得5.44倍、2.82倍和1.92倍的加速。"
conclusion: 该研究证明了将知识驱动的专家技能与记忆机制引入多智能体框架，能显著提升GPU算子自动优化的性能与效率。
---

## 摘要
提高 GPU 算子效率对于推进 AI 系统至关重要。最近的研究探索了利用大语言模型（LLMs）进行 GPU 算子的生成与优化。然而，现有的基于 LLM 的算子优化流水线通常依赖于 LLM 内部不透明且隐式学习的启发式方法来确定优化策略。这导致了低效的试错过程以及可解释性较弱的优化结果。我们的核心见解是用知识驱动且感知任务轨迹的专家优化技能来取代隐式的启发式方法。具体而言，我们提出了 KernelSkill，这是一个具有双层记忆架构的多智能体框架。KernelSkill 通过协调具有可重用专家技能长期记忆和防止重复回溯短期记忆的智能体来运行。在 KernelBench 第 1-3 级上，KernelSkill 实现了 100% 的成功率，并且在第 1、2 和 3 级上相对于 Torch Eager 分别实现了 5.44 倍、2.82 倍和 1.92 倍的平均加速，优于之前的基准模型。代码可在 https://github.com/0satan0/KernelMem/ 获取。

## Abstract
Improving GPU kernel efficiency is crucial for advancing AI systems. Recent work has explored leveraging large language models (LLMs) for GPU kernel generation and optimization. However, existing LLM-based kernel optimization pipelines typically rely on opaque, implicitly learned heuristics within the LLMs to determine optimization strategies. This leads to inefficient trial-and-error and weakly interpretable optimizations. Our key insight is to replace implicit heuristics with expert optimization skills that are knowledge-driven and aware of task trajectories. Specifically, we present KernelSkill, a multi-agent framework with a dual-level memory architecture. KernelSkill operates by coordinating agents with long-term memory of reusable expert skills and short-term memory to prevent repetitive backtracking. On KernelBench Levels 1-3, KernelSkill achieves a 100% success rate and average speedups of 5.44x, 2.82x, and 1.92x over Torch Eager on Levels 1, 2, and 3, respectively, outperforming prior baselines. Code is available at https://github.com/0satan0/KernelMem/.

---

## 论文详细总结（自动生成）

以下是对论文《KernelSkill: A Multi-Agent Framework for GPU Kernel Optimization》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何提升大语言模型（LLM）在 GPU 算子（Kernel）优化中的效率、稳定性和可解释性。
*   **研究背景**：高效的 GPU 算子是 AI 系统的核心，但其开发高度依赖专家经验。现有的基于 LLM 的优化方法主要分为“基于训练的适配”和“推理时的闭环微调”。
*   **主要痛点**：
    1.  **隐式启发式**：现有模型依赖内部模糊的经验，导致优化过程像“黑盒”，难以解释且试错成本高。
    2.  **缺乏轨迹感知**：在多轮迭代中，模型容易出现“循环修复”或“无效尝试”，无法有效利用之前的失败或成功经验。
    3.  **策略匹配不准**：模型往往无法准确识别性能瓶颈（如内存受限 vs 计算受限），导致选择了低效的优化手段。

### 2. 方法论：KernelSkill 框架
KernelSkill 提出了一个**双层记忆增强的多智能体框架**，旨在将隐式的模型经验转化为显式的专家技能。

*   **核心思想**：通过“长期记忆”提供专家级的优化知识库，通过“短期记忆”记录当前任务的尝试轨迹，协调多个专门的智能体完成从生成、诊断到优化的闭环。
*   **关键技术细节**：
    *   **双层记忆架构**：
        *   **长期记忆（Long-term Memory）**：存储结构化的专家知识。包含“确定性决策策略”（基于 Nsight Compute 指标和代码特征的规则匹配）和“方法知识库”（提供具体的优化原理和代码实现提示）。
        *   **短期记忆（Short-term Memory）**：记录当前任务的修复链和优化轨迹，防止模型在错误方案中循环，并支持多步耦合优化。
    *   **多智能体协作流**：
        *   **生成器（Generator）**：将 PyTorch 代码转为基础 CUDA 代码。
        *   **特征提取器（Feature Extractor）**：结合规则和 LLM 提取代码的静态特征（如访存模式）。
        *   **评审器（Reviewer）**：包含编译器、验证器和性能分析器（Profiler），提供硬件级的反馈（NCU/NSYS 指标）。
        *   **规划器与优化器（Planner & Optimizer）**：根据长期记忆检索出的技能制定计划并执行代码修改。
*   **算法流程**：从种子算子开始，若编译/验证失败则进入“修复分支”（利用短期记忆诊断）；若成功则进入“优化分支”（利用长期记忆检索技能并根据性能反馈迭代），直到达到最大轮数或性能达标。

### 3. 实验设计
*   **数据集/场景**：使用 **KernelBench** 基准测试，涵盖了从简单算子到复杂模型架构的三个难度等级：
    *   **Level 1**：单算子任务（100个）。
    *   **Level 2**：多算子工作负载（100个）。
    *   **Level 3**：完整模型架构（50个）。
*   **Benchmark**：以 **Torch Eager**（未优化的 PyTorch 实现）作为性能基准。
*   **对比方法**：
    *   **基于训练的方法**：Kevin-32B, QiMeng。
    *   **基于智能体的方法**：Astra, PRAGMA, CudaForge, STARK。

### 4. 资源与算力
*   **硬件环境**：使用 **NVIDIA A100 (80GB)** GPU。
*   **软件环境**：Python 3.11, PyTorch 2.6, CUDA 12.4, cuDNN 9.1。
*   **底层模型**：所有智能体均调用 **ChatGPT-5.1** (OpenAI, 2025) 作为基础模型。
*   **迭代设置**：每个任务最多运行 **15 轮**优化迭代。

### 5. 实验数量与充分性
*   **实验规模**：共针对 250 个不同的 KernelBench 任务进行了测试，实验覆盖面较广。
*   **消融实验**：论文详细对比了“无记忆”、“无长期记忆”和“无短期记忆”的情况，验证了各组件的必要性。
*   **客观性**：
    *   采用了统一的预热（25次）和计时（100次）流程以确保性能测量的稳定性。
    *   对比了当前最先进的（SOTA）开源和非开源基准，实验设计较为公平。

### 6. 主要结论与发现
*   **成功率**：KernelSkill 在所有三个等级上均实现了 **100% 的成功率**，显著高于 Kevin 和 QiMeng 等方法。
*   **加速比**：相比 Torch Eager，在 Level 1/2/3 分别实现了 **5.44x、2.82x 和 1.92x** 的平均加速。
*   **效率提升**：KernelSkill 在 15 轮内的优化效果优于 STARK 在 30 轮内的效果，证明了其“单轮优化含金量”更高。
*   **记忆的作用**：长期记忆显著提升了最终的加速比，而短期记忆则是保证 100% 成功率（修复稳定性）的关键。

### 7. 优点与亮点
*   **可解释性强**：通过长期记忆的规则匹配，系统可以明确解释“为什么要使用某种优化方法”，使优化决策可审计。
*   **稳定性高**：双层记忆机制有效解决了 LLM 在复杂代码修复中的“幻觉”和“循环”问题。
*   **知识驱动**：将人类专家的 GPU 优化手册（如访存对齐、Shared Memory 瓦片化等）结构化，弥补了模型通用知识的不足。

### 8. 不足与局限
*   **知识库依赖**：KernelSkill 的表现高度依赖于长期记忆中专家知识的覆盖范围。如果遇到知识库之外的新型硬件特性或特殊算子，性能可能会退化到普通 LLM 水平。
*   **成本问题**：多智能体框架配合 ChatGPT-5.1 这种顶级模型，在进行 15 轮迭代时，API 调用成本和推理延迟较高。
*   **硬件局限**：实验主要在 A100 上完成，对于不同架构（如 H100 或国产 GPU）的迁移适配能力尚需进一步验证。
*   **端到端模型挑战**：虽然在 Level 3 表现良好，但对于 Level 4（超大规模端到端模型）的复杂依赖处理仍有待观察。

（完）
