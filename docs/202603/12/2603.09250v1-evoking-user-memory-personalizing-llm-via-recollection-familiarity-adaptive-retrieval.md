---
title: "Evoking User Memory: Personalizing LLM via Recollection-Familiarity Adaptive Retrieval"
title_zh: 唤起用户记忆：通过回忆-熟悉度自适应检索实现大语言模型个性化
authors: "Yingyi Zhang, Junyi Li, Wenlin Zhang, Penyue Jia, Xianneng Li, Yichao Wang, Derong Xu, Yi Wen, Huifeng Guo, Yong Liu, Xiangyu Zhao"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09250v1"
tags: ["query:agimem"]
score: 9.0
evidence: 具有熟悉度和回忆路径的双过程记忆检索
tldr: 针对个性化大模型在记忆检索中存在的全上下文开销大或单次检索精度不足的问题，本文受认知科学启发提出RF-Mem。该方法通过评估熟悉度信号自适应切换检索路径：高熟悉度执行快速Top-K检索，低熟悉度则通过聚类与alpha-mix模拟人类的“回想”过程进行迭代重构。实验证明该方法在固定预算下显著提升了检索质量与个性化表现。
selection_source: fresh_fetch
motivation: 现有的LLM记忆检索方法在处理复杂用户历史时，往往在计算成本与检索深度之间难以平衡，缺乏类似人类的深度回想机制。
method: 提出RF-Mem框架，利用熟悉度不确定性引导双路径检索，结合快速相似度匹配与基于嵌入空间迭代重构的深度回想路径。
result: 在三个基准测试中，RF-Mem在固定延迟和预算约束下，表现均优于传统的单次检索和全上下文推理方法。
conclusion: 将人类双重过程认知模型引入检索系统，为实现高效、可扩展且自适应的个性化大模型提供了新思路。
---

## 摘要
个性化大语言模型（LLMs）依赖记忆检索来整合用户特定的历史、偏好和上下文。现有方法要么通过将用户所有的过去记忆输入提示词（prompt）来使 LLM 过载，这既昂贵且不可扩展；要么将检索简化为单次相似度搜索，这仅能捕捉表面匹配。然而，认知科学表明，人类记忆通过双重过程运作：熟悉度（Familiarity），提供快速但粗略的识别；以及回忆（Recollection），能够进行深思熟虑的链式重构，以深度恢复情境内容。目前的系统既缺乏执行回忆检索的能力，也缺乏在双重检索路径之间自适应切换的机制，导致召回不足或引入噪声。为了解决这一问题，我们提出了 RF-Mem（回忆-熟悉度记忆检索），一种由熟悉度不确定性引导的双路径记忆检索器。RF-Mem 通过平均分和熵来衡量熟悉度信号。高熟悉度会引导至直接的 Top-K 熟悉度检索路径，而低熟悉度则会激活回忆路径。在回忆路径中，系统对候选记忆进行聚类，并对查询（query）应用 alpha-mix，以在嵌入空间中迭代扩展证据，模拟深思熟虑的上下文重构。这种设计将类人的双重过程识别嵌入到检索器中，避免了全上下文开销，并实现了可扩展的自适应个性化。在三个基准测试和不同语料库规模上的实验表明，在固定的预算和延迟约束下，RF-Mem 的性能始终优于单次检索和全上下文推理。我们的代码可以在可复现性声明中找到。

## Abstract
Personalized large language models (LLMs) rely on memory retrieval to incorporate user-specific histories, preferences, and contexts. Existing approaches either overload the LLM by feeding all the user's past memory into the prompt, which is costly and unscalable, or simplify retrieval into a one-shot similarity search, which captures only surface matches. Cognitive science, however, shows that human memory operates through a dual process: Familiarity, offering fast but coarse recognition, and Recollection, enabling deliberate, chain-like reconstruction for deeply recovering episodic content. Current systems lack both the ability to perform recollection retrieval and mechanisms to adaptively switch between the dual retrieval paths, leading to either insufficient recall or the inclusion of noise. To address this, we propose RF-Mem (Recollection-Familiarity Memory Retrieval), a familiarity uncertainty-guided dual-path memory retriever. RF-Mem measures the familiarity signal through the mean score and entropy. High familiarity leads to the direct top-K Familiarity retrieval path, while low familiarity activates the Recollection path. In the Recollection path, the system clusters candidate memories and applies alpha-mix with the query to iteratively expand evidence in embedding space, simulating deliberate contextual reconstruction. This design embeds human-like dual-process recognition into the retriever, avoiding full-context overhead and enabling scalable, adaptive personalization. Experiments across three benchmarks and corpus scales demonstrate that RF-Mem consistently outperforms both one-shot retrieval and full-context reasoning under fixed budget and latency constraints. Our code can be found in the Reproducibility Statement.

---

## 论文详细总结（自动生成）

这是一份关于论文《Evoking User Memory: Personalizing LLM via Recollection-Familiarity Adaptive Retrieval》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
个性化大语言模型（LLM）的核心挑战在于如何高效、准确地从海量用户历史中检索相关记忆。现有方法存在两个极端：
*   **全上下文（Full Context）：** 将所有历史喂给模型，成本极高且难以扩展（受限于长文本窗口）。
*   **单次检索（One-shot Retrieval）：** 仅通过简单的向量相似度搜索，往往只能捕捉表面匹配，无法处理复杂、模糊或需要深度关联的个性化查询。

**研究动机：** 受认知科学中人类记忆的“双重过程理论”（Dual-Process Theory）启发，人类记忆包含**熟悉度（Familiarity，快速粗略识别）**和**回忆（Recollection，慢速链式重构）**。论文旨在构建一个能够模拟这种机制的自适应检索框架，在效率和深度之间取得平衡。

### 2. 方法论：RF-Mem 框架
RF-Mem 提出了一种由“熟悉度不确定性”引导的双路径检索机制：

*   **熟悉度信号评估：** 首先进行探测检索（Probe Retrieval），计算初始候选列表的**平均相似度得分（Mean Score）**和**信息熵（Entropy）**。
    *   高得分或低熵：表示系统对结果很“熟悉”且确定，走快速路径。
    *   低得分或高熵：表示系统感到“陌生”或不确定，激活深度回忆路径。
*   **熟悉度路径（Familiarity Path）：** 标准的单次 Top-K 向量检索，确保高置信度下的极低延迟。
*   **回忆路径（Recollection Path）：** 模拟人类深思熟虑的过程，采用“检索-聚类-混合”循环：
    1.  **聚类：** 对初步检索到的记忆片段进行 KMeans 聚类，提取语义中心（Centroids）。
    2.  **$\alpha$-mix 查询重构：** 将当前查询向量与聚类中心进行加权融合（公式：$x_{new} = \text{norm}(\alpha x_{old} + (1-\alpha)g + x_{initial})$），生成新的“回想查询”。
    3.  **迭代扩展：** 在嵌入空间中多次迭代，逐步重构证据链，挖掘隐藏在深处的关联记忆。

### 3. 实验设计
*   **数据集：**
    *   **PersonaMem：** 包含 32K、128K 到 1M 不同规模的对话语料，涵盖医疗、法律、推荐等 7 类任务。
    *   **PersonaBench：** 侧重于私有文档和个人背景信息的检索。
    *   **LongMemEval：** 针对长短期交互记忆的基准测试。
*   **对比方法（Baselines）：**
    *   无记忆（Zero Memory）、全上下文（Full Context）。
    *   标准稠密检索（Dense Retrieval/Familiarity）。
    *   纯回忆路径（Recollection-only）。
*   **评估指标：** 生成任务使用准确率（Accuracy），检索任务使用 Recall@K。

### 4. 资源与算力
*   **硬件环境：** 论文明确指出实验在单台 **NVIDIA A100 GPU** 上运行（Ubuntu 系统）。
*   **算力细节：** 检索过程主要涉及向量运算和小型聚类，延迟极低（PersonaMem 32K 规模下平均检索时间约为 5.09ms）。论文未强调大规模模型训练时长，因为该方法主要作为检索插件使用，具有轻量化特征。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从 32K 到 1M 词元的三个语料库量级，验证了可扩展性。
*   **消融与敏感性分析：** 针对混合系数 $\alpha$、熵阈值 $\tau$、束宽 $B$、扇出 $F$ 等关键超参数进行了详尽的网格搜索和敏感性测试。
*   **自适应研究：** 验证了 RF-Mem 与现有索引方法（如 MemoryBank）、查询扩展（如 HyDE）以及迭代 RAG（如 Search-o1）的兼容性。
*   **充分性评价：** 实验设计非常充分，不仅对比了性能，还深入分析了不同任务类型（如事实类 vs. 推理类）下的表现差异，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先：** RF-Mem 在所有基准测试中均优于单次检索，且在 1M 规模下远超 Full Context（后者因窗口限制失效）。
*   **自适应优势：** 熵引导的切换机制能有效识别何时需要“深思熟虑”，在保持低延迟的同时显著提升了复杂问题的召回率。
*   **语义重构有效：** 回忆路径通过在嵌入空间迭代，能找回被单次检索遗漏的、具有时间跨度的关键证据链。

### 7. 优点
*   **理论支撑扎实：** 将认知科学的成熟理论成功转化为可落地的检索算法。
*   **轻量且高效：** 无需调用 LLM 进行多次查询重写，仅在向量空间操作，极大地节省了推理成本。
*   **模块化强：** 可以作为现有 RAG 系统的增强插件，适配不同的 Embedding 模型和索引结构。

### 8. 不足与局限
*   **模态限制：** 目前仅限于文本记忆，未探讨多模态（如图片、视频历史）的个性化检索。
*   **静态索引假设：** 实验主要基于静态语料，对于实时动态更新、存在记忆冲突（用户改变主意）的场景处理能力尚待验证。
*   **阈值依赖：** 虽然论文展示了可以学习策略选择器，但默认的阈值设定（$\theta, \tau$）在不同领域可能需要微调。

（完）
