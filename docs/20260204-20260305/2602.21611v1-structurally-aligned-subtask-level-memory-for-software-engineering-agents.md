---
title: Structurally Aligned Subtask-Level Memory for Software Engineering Agents
title_zh: 面向软件工程智能体的结构化对齐子任务级记忆
authors: "Kangning Shen, Jingyuan Zhang, Chenxi Sun, Wencong Zeng, Yang Yue"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.21611v1"
tags: ["query:agimem"]
score: 9.0
evidence: 子任务级记忆更新与检索策略
tldr: "本研究针对软件工程（SWE）智能体在长程推理中记忆粒度过粗的问题，指出传统的实例级记忆会导致检索误导。为此，提出了一种“结构化对齐的子任务级记忆”方法，通过将记忆的存取与智能体的功能分解对齐，实现更精准的经验复用。在 SWE-bench Verified 上的实验表明，该方法显著提升了不同大模型底座的性能，平均 Pass@1 提高 4.7%，证明了细粒度记忆对复杂软件开发任务的有效性。"
selection_source: fresh_fetch
motivation: 现有的实例级记忆机制由于粒度不匹配，在处理描述相似但逻辑不同的任务阶段时容易产生错误的检索引导。
method: 提出结构化对齐的子任务级记忆方法，将记忆的存储、检索和更新过程与智能体的功能分解结构相结合。
result: "在 SWE-bench Verified 基准测试中，该方法使平均 Pass@1 提升了 4.7 个百分点，且在 Gemini 2.5 Pro 上提升达 6.8 个百分点。"
conclusion: 将记忆管理细化至子任务级别，能更有效地利用过去经验来辅助软件工程智能体处理复杂的长程推理任务。
---

## 摘要
大语言模型（LLMs）在作为自主软件工程（SWE）智能体方面展现出了巨大潜力。近期的研究进一步探索了通过记忆机制增强这些智能体，以支持长程推理。然而，这些方法通常在粗粒度的实例级别运行，将整个问题解决过程视为存储和检索的原子单位。我们通过实证研究证明，实例级记忆存在根本性的粒度不匹配问题，当具有相似表面描述的任务在特定阶段需要不同的推理逻辑时，会导致错误的检索。为解决这一问题，我们提出了结构化对齐的子任务级记忆（Structurally Aligned Subtask-Level Memory），该方法将记忆的存储、检索和更新与智能体的功能分解相对齐。在 SWE-bench Verified 上的广泛实验表明，我们的方法在多种骨干模型上均一致优于原始智能体和强大的实例级记忆基线，平均 Pass@1 较原始智能体提升了 4.7 个百分点（例如，在 Gemini 2.5 Pro 上提升了 6.8 个百分点）。性能增益随交互步骤的增加而增长，表明利用过往经验有助于复杂软件工程任务中的长程推理。

## Abstract
Large Language Models (LLMs) have demonstrated significant potential as autonomous software engineering (SWE) agents. Recent work has further explored augmenting these agents with memory mechanisms to support long-horizon reasoning. However, these approaches typically operate at a coarse instance granularity, treating the entire problem-solving episode as the atomic unit of storage and retrieval. We empirically demonstrate that instance-level memory suffers from a fundamental granularity mismatch, resulting in misguided retrieval when tasks with similar surface descriptions require distinct reasoning logic at specific stages. To address this, we propose Structurally Aligned Subtask-Level Memory, a method that aligns memory storage, retrieval, and updating with the agent's functional decomposition. Extensive experiments on SWE-bench Verified demonstrate that our method consistently outperforms both vanilla agents and strong instance-level memory baselines across diverse backbones, improving mean Pass@1 over the vanilla agent by +4.7 pp on average (e.g., +6.8 pp on Gemini 2.5 Pro). Performance gains grow with more interaction steps, showing that leveraging past experience benefits long-horizon reasoning in complex software engineering tasks.

---

## 论文详细总结（自动生成）

以下是对论文《Structurally Aligned Subtask-Level Memory for Software Engineering Agents》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：大语言模型（LLM）在处理复杂的软件工程（SWE）任务时，通常需要多轮交互和长程推理。为了提升效率，研究者开始引入记忆机制来复用过往经验。
*   **核心痛点**：现有的记忆机制大多基于“实例级”（Instance-level），即将整个问题的解决过程作为存储和检索的原子单位。
*   **问题本质**：论文指出实例级记忆存在**粒度不匹配**的问题。
    *   **干扰性**：表面描述相似的任务（如“修复登录按钮”与“修复登录超时”）在特定阶段可能需要完全不同的推理逻辑（前端 UI vs 后端服务）。
    *   **低复用**：表面不相关的任务可能共享相同的子任务操作（如更新事件监听器），但实例级检索因全局相似度低而无法复用这些技能。

### 2. 方法论
*   **核心思想**：提出**结构化对齐的子任务级记忆**，将记忆的存储、检索和更新操作与智能体的功能分解（子任务）直接对齐。
*   **关键技术细节**：
    *   **子任务建模（Dynamic Segmentation）**：将推理过程动态划分为四个功能类别：`ANALYZE`（分析）、`REPRODUCE`（复现）、`EDIT`（编辑）、`VERIFY`（验证）。智能体在运行中自主预测下一阶段的意图。
    *   **结构化存储**：记忆条目以三元组 $(z, d, e)$ 存储。其中 $z$ 是功能类别，$d$ 是包含目标和关键词的意图描述，$e$ 是从原始轨迹中抽象出的可迁移经验。
    *   **分层检索策略**：
        1.  **类别过滤**：首先根据当前子任务类别 $z$ 进行硬约束过滤，排除跨阶段噪声。
        2.  **语义匹配**：在同类记忆中，通过计算当前意图描述 $d$ 与存储条目的余弦相似度，检索最相关的经验。
    *   **经验提取与更新**：子任务完成后，利用 LLM 算子将原始执行轨迹（Trajectory）转化为抽象经验，过滤掉特定仓库的噪声（如具体文件路径），仅保留行动指南或模式。

### 3. 实验设计
*   **数据集/基准**：使用 **SWE-bench Verified**，包含 500 个真实的 GitHub Issue。
*   **对比方法**：
    1.  **Vanilla Agent**：无记忆的原始智能体（基于 Mini SWE Agent 框架）。
    2.  **Instance-level Memory**：复现了 Reasoning-Bank 方法，采用实例级摘要和全局相似度检索。
*   **骨干模型**：评估了 Gemini 2.5 Flash/Pro 和 Claude 3.7/4.0 Sonnet 四种不同能力的模型。
*   **实验协议**：采用测试时流式处理（Test-time streaming），记忆库从空开始随任务执行动态累积。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **推理成本**：实验主要通过 API 调用完成。作者提到，为了保证对比的公平性，子任务级记忆的额外开销（如经验提取步骤）被计入每个实例的总步数预算内。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 种模型底座，每种模型均进行了 3 次独立运行（使用不同随机种子打乱任务顺序），并报告了平均 Pass@1 和 Best@3 结果。
*   **消融实验**：设计了三组消融实验，分别验证了“结构化提示”、“类别隔离过滤”以及“抽象经验 vs 原始轨迹”的必要性。
*   **充分性评价**：实验覆盖了从轻量级到前沿推理级的多种模型，且通过 3 次运行证明了结果的统计显著性，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能提升**：该方法在所有模型上均取得一致增益，平均 Pass@1 提升 **4.7%**。在 Gemini 2.5 Pro 上提升最显著，达 **6.8%**。
*   **复杂度正相关**：在“困难”任务（执行步数多）上，该方法提升了 **8.7%**，表明细粒度记忆能有效防止智能体在长程推理中迷失。
*   **在线进化**：随着执行任务数量增加，记忆库越丰富，性能提升越明显，验证了在线学习的有效性。
*   **功能特化**：分析显示，不同阶段检索到的记忆具有明显的语义差异（如分析阶段侧重诊断，编辑阶段侧重实现策略）。

### 7. 亮点
*   **粒度创新**：首次系统性地指出并解决了 SWE 任务中记忆粒度错位的问题。
*   **结构化对齐**：通过类别硬过滤显著降低了检索干扰，提高了经验复用的精准度。
*   **鲁棒性强**：相比实例级记忆在 Claude 模型上出现的性能波动，子任务级记忆表现出更好的稳定性。

### 8. 不足与局限
*   **推理开销**：虽然在步数预算内，但每个子任务增加的意图预测和经验提取步骤会增加实际的 Token 消耗和推理延迟。
*   **类别定义局限**：预定义的四个功能类别（Analyze, Reproduce, Edit, Verify）虽然经典，但可能无法完全覆盖所有复杂的软件开发活动。
*   **冷启动依赖**：在任务流的初期，由于记忆库稀疏，性能提升有限，甚至可能因检索开销产生轻微负面影响。

（完）
