---
title: "MemFly: On-the-Fly Memory Optimization via Information Bottleneck"
title_zh: MemFly：基于信息瓶颈的实时记忆优化
authors: "Zhenyuan Zhang, Xianzhang Jia, Zhiqin Yang, Zhenbo Song, Wei Xue, Sirui Han, Yike Guo"
date: 2026-02-08
pdf: "https://arxiv.org/pdf/2602.07885v1"
tags: ["query:agimem"]
score: 9.0
evidence: 长效记忆的即时演化与分层存储结构
tldr: 针对大语言模型在处理长程任务时面临的记忆压缩与检索精度失衡问题，本文提出 MemFly 框架。该框架基于信息瓶颈原理，通过无梯度优化器最小化压缩熵并最大化相关熵，构建分层记忆结构。同时，MemFly 引入了集成语义、符号和拓扑路径的混合检索机制，支持多步迭代优化。实验表明，该方法在记忆连贯性和响应准确性上显著超越了现有技术。
selection_source: fresh_fetch
motivation: 旨在解决大模型长期记忆中冗余信息高效压缩与下游任务精确检索之间的核心矛盾。
method: 基于信息瓶颈原理，利用无梯度优化器构建分层记忆结构，并结合语义、符号和拓扑的混合检索与迭代优化机制。
result: 实验结果显示，MemFly 在记忆连贯性、响应忠实度和准确性等多个维度上均显著优于当前最先进的基准模型。
conclusion: MemFly 通过动态内存演化和多路径检索，为提升大模型处理复杂多跳查询和长程交互的能力提供了有效方案。
---

## 摘要
长期记忆使大语言模型（LLM）智能体能够通过历史交互处理复杂任务。然而，现有框架在高效压缩冗余信息与保持下游任务的精确检索之间面临着根本性的权衡难题。为了弥补这一差距，我们提出了 MemFly，这是一个基于信息瓶颈原理的框架，旨在促进大语言模型的实时记忆演化。我们的方法通过无梯度优化器最小化压缩熵并最大化相关熵，构建了一个分层记忆结构以实现高效存储。为了充分利用 MemFly，我们开发了一种混合检索机制，无缝集成了语义、符号和拓扑路径，并结合迭代细化来处理复杂的多跳查询。全面的实验表明，MemFly 在记忆连贯性、响应忠实度和准确性方面显著优于现有的最先进基准。

## Abstract
Long-term memory enables large language model agents to tackle complex tasks through historical interactions. However, existing frameworks encounter a fundamental dilemma between compressing redundant information efficiently and maintaining precise retrieval for downstream tasks. To bridge this gap, we propose MemFly, a framework grounded in information bottleneck principles that facilitates on-the-fly memory evolution for LLMs. Our approach minimizes compression entropy while maximizing relevance entropy via a gradient-free optimizer, constructing a stratified memory structure for efficient storage. To fully leverage MemFly, we develop a hybrid retrieval mechanism that seamlessly integrates semantic, symbolic, and topological pathways, incorporating iterative refinement to handle complex multi-hop queries. Comprehensive experiments demonstrate that MemFly substantially outperforms state-of-the-art baselines in memory coherence, response fidelity, and accuracy.

---

## 论文详细总结（自动生成）

以下是对论文《MemFly: On-the-Fly Memory Optimization via Information Bottleneck》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）智能体在处理长程交互时，其长期记忆系统面临“压缩冗余”与“保持精度”之间的根本矛盾。
*   **研究背景**：
    *   **检索中心化方法**（如 RAG）：保留了原始细节但积累了大量冗余，导致检索噪声随时间单调增加。
    *   **记忆增强型方法**（如 MemGPT）：利用总结进行压缩，但往往牺牲了精确推理所需的细粒度忠实度。
*   **动机**：引入**信息瓶颈（Information Bottleneck, IB）**原理，在最小化记忆表示复杂度的同时，最大化对未来任务有用的相关信息。

### 2. 方法论
MemFly 框架将记忆构建视为一个动态的 IB 优化问题，核心包含以下三个层面：

*   **核心思想**：通过最小化 $I(X; M)$（压缩熵）和最大化 $I(M; Y)$（相关熵）的拉格朗日目标函数，实现记忆的实时演化。
*   **分层记忆结构（基于双聚类原理）**：
    1.  **笔记层 (Notes)**：存储去噪后的原始事实，保证高忠实度。
    2.  **关键词层 (Keywords)**：作为中间符号锚点，稳定语义空间，缓解向量稀释。
    3.  **主题层 (Topics)**：对关键词进行聚类，实现宏观语义导航。
*   **关键技术细节**：
    *   **无梯度优化器**：利用 LLM 作为 JS 散度（Jensen-Shannon Divergence）的近似器，评估新信息与旧记忆的冗余度（sred）和互补性（scomp）。
    *   **门控结构更新**：根据评估分数执行三种操作：**合并 (Merge)**（减少冗余）、**链接 (Link)**（建立拓扑关联）、**追加 (Append)**（保留新异信息）。
    *   **三路径混合检索**：并行执行宏观语义导航（主题）、微观符号锚定（关键词）和拓扑扩展（关联边）。
    *   **迭代证据细化 (IER)**：针对复杂查询，通过 LLM 自我评估证据充分性，并自动生成子查询进行多轮检索。

### 3. 实验设计
*   **数据集**：使用 **LoCoMo** 基准测试集，专门用于评估 LLM 智能体的长程对话记忆能力。
*   **评估场景**：涵盖五大推理类别：多跳推理 (Multi-Hop)、时间推理 (Temporal)、开放域 (Open Domain)、单跳推理 (Single Hop) 和对抗性任务 (Adversarial)。
*   **对比方法**：对比了 6 种最先进（SOTA）的方法，包括 LOCOMO 原生方法、Read Agent、MemoryBank、MemGPT、A-Mem 和 Mem0。
*   **骨干模型**：在 GPT-4o、GPT-4o-mini、Qwen3-8B 和 Qwen3-14B 四种模型上进行了验证。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或训练时长。
*   **实现细节**：提到使用 **Neo4j** 图数据库作为存储后端，支持向量索引和显式拓扑关系。由于该框架主要依赖 LLM 的推理能力（API 或本地推理），其开销主要体现在推理时的 Token 消耗和处理延迟上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个不同的闭源和开源模型上进行了全面测试。
    *   针对记忆构建和检索的各个组件进行了详尽的**消融实验**（如：w/o Update, w/o Link, w/o Topic, w/o IER 等）。
    *   使用了 F1 分数、BLEU-1、召回率 (Recall) 和命中率 (Hit Rate) 等多维指标。
*   **充分性评价**：实验设计较为充分且客观。通过跨模型对比证明了通用性，通过分类别分析证明了其在处理复杂逻辑（如多跳和对抗任务）时的优势。

### 6. 主要结论与发现
*   **性能领先**：MemFly 在所有骨干模型上均取得了最高平均分。在 Qwen3-8B 上，其 F1 分数比次优方法 A-Mem 高出 5.86%。
*   **结构优势**：消融实验证明，**门控更新（Link/Merge）**对性能影响最大，尤其是在对抗性和时间推理任务中，拓扑链接能有效过滤干扰信息。
*   **检索增益**：关键词锚定对单跳任务至关重要，而迭代细化（IER）显著提升了开放域和对抗性任务的准确率。

### 7. 优点
*   **理论严谨**：将记忆管理形式化为信息瓶颈问题，为“存什么”和“丢什么”提供了理论依据。
*   **结构创新**：结合了双聚类原理的分层结构，既保留了符号化的精确性，又具备向量检索的灵活性。
*   **动态演化**：支持实时（On-the-fly）的记忆合并与关联，能够处理实体状态的演变。

### 8. 不足与局限
*   **计算开销**：由于在记忆构建阶段引入了 LLM 评估（Merge/Link 判断），会增加实时交互的延迟和计算成本。
*   **多模态缺失**：目前仅限于文本交互，尚未扩展到多模态记忆（如图像、视频）的处理。
*   **超参数敏感性**：合并和链接的阈值（$\tau_m, \tau_l$）可能需要针对不同领域进行微调。

（完）
