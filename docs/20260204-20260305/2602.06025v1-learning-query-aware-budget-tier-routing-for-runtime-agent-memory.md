---
title: Learning Query-Aware Budget-Tier Routing for Runtime Agent Memory
title_zh: 学习面向运行时智能体记忆的查询感知预算层级路由
authors: "Haozhen Zhang, Haodong Yue, Tao Feng, Quanyu Long, Jianzhu Bao, Bowen Jin, Weizhi Zhang, Xiao Li, Jiaxuan You, Chengwei Qin, Wenya Wang"
date: 2026-02-05
pdf: "https://arxiv.org/pdf/2602.06025v1"
tags: ["query:agimem"]
score: 9.0
evidence: 具有查询感知预算层路由的运行时智能体内存框架
tldr: 针对大模型智能体在处理长上下文时内存构建效率低且成本难以控制的问题，本文提出 BudgetMem 框架。该框架将内存处理模块化，并为每个模块提供低、中、高三种预算等级。通过强化学习训练的轻量级路由策略，BudgetMem 能够根据查询动态选择预算等级，在保证任务性能的同时有效控制内存构建成本，实现了性能与成本的最优权衡。
selection_source: fresh_fetch
motivation: 现有的智能体内存系统多为离线构建或缺乏对运行时性能与成本权衡的显式控制。
method: 提出 BudgetMem 框架，利用强化学习训练的路由策略在不同复杂度的内存模块等级间进行动态切换。
result: 在多个基准测试中，BudgetMem 在高预算下超越了强基线模型，并在紧凑预算下提供了更优的准确率-成本边界。
conclusion: 该研究证明了通过查询感知的预算路由可以有效优化智能体内存的运行时效率，并揭示了不同分级策略在不同预算环境下的适用性。
---

## 摘要
记忆对于在单个上下文窗口之外运行的大语言模型（LLM）智能体而言日益重要，但现有系统大多依赖于离线的、与查询无关的记忆构建方式，这可能导致效率低下并丢失关键的查询信息。尽管运行时记忆利用是一种自然的替代方案，但先前的工作往往会产生巨大的开销，且对性能与成本之间的权衡缺乏明确的控制。在本研究中，我们提出了 BudgetMem，这是一个用于显式、查询感知的性能-成本控制的运行时智能体记忆框架。BudgetMem 将记忆处理结构化为一组记忆模块，每个模块提供三个预算层级（即低/中/高）。一个轻量级路由器在各模块间执行预算层级路由，以平衡任务性能和记忆构建成本，该路由器通过强化学习训练的紧凑神经策略实现。以 BudgetMem 作为统一测试平台，我们研究了实现预算层级的三种互补策略：实现（方法复杂度）、推理（推理行为）和容量（模块模型大小）。在 LoCoMo、LongMemEval 和 HotpotQA 数据集上，当性能优先（即高预算设置）时，BudgetMem 超越了强基准模型，并在预算受限的情况下提供了更好的准确度-成本边界。此外，我们的分析解构了不同层级策略的优缺点，阐明了在不同预算机制下，各个维度何时能提供最有利的权衡。

## Abstract
Memory is increasingly central to Large Language Model (LLM) agents operating beyond a single context window, yet most existing systems rely on offline, query-agnostic memory construction that can be inefficient and may discard query-critical information. Although runtime memory utilization is a natural alternative, prior work often incurs substantial overhead and offers limited explicit control over the performance-cost trade-off. In this work, we present \textbf{BudgetMem}, a runtime agent memory framework for explicit, query-aware performance-cost control. BudgetMem structures memory processing as a set of memory modules, each offered in three budget tiers (i.e., \textsc{Low}/\textsc{Mid}/\textsc{High}). A lightweight router performs budget-tier routing across modules to balance task performance and memory construction cost, which is implemented as a compact neural policy trained with reinforcement learning. Using BudgetMem as a unified testbed, we study three complementary strategies for realizing budget tiers: implementation (method complexity), reasoning (inference behavior), and capacity (module model size). Across LoCoMo, LongMemEval, and HotpotQA, BudgetMem surpasses strong baselines when performance is prioritized (i.e., high-budget setting), and delivers better accuracy-cost frontiers under tighter budgets. Moreover, our analysis disentangles the strengths and weaknesses of different tiering strategies, clarifying when each axis delivers the most favorable trade-offs under varying budget regimes.

---

## 论文详细总结（自动生成）

以下是对论文《Learning Query-Aware Budget-Tier Routing for Runtime Agent Memory》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保证大语言模型（LLM）智能体记忆提取质量的同时，显式且动态地控制其运行时的计算成本？
*   **研究背景**：
    *   现有的智能体记忆系统多采用“离线构建”模式（如预先总结或索引），这种方式与查询无关，容易导致计算浪费或丢失特定查询所需的关键信息。
    *   “运行时（Runtime）提取”虽更灵活，但计算开销巨大且缺乏精细的成本控制手段。
    *   工业界已出现“思考模式”或“推理等级”等分级计算需求，但智能体记忆领域尚缺乏系统性的性能-成本权衡机制。

### 2. 方法论：BudgetMem 框架
*   **核心思想**：将运行时记忆提取过程分解为模块化流水线，并为每个模块提供不同复杂度的“预算层级”（Budget Tiers），通过强化学习训练的路由器根据查询内容动态选择最优层级。
*   **关键技术细节**：
    *   **模块化流水线**：固定结构为 `过滤 (Filter) -> 并行提取 (实体/时间/主题) -> 摘要 (Summary)`。
    *   **预算层级策略**：提出三种互补的实现方式：
        1.  **实现层级 (Implementation)**：改变方法复杂度（如：启发式规则 vs. BERT模型 vs. LLM）。
        2.  **推理层级 (Reasoning)**：改变推理行为（如：直接生成 vs. 思维链CoT vs. 多步反思）。
        3.  **容量层级 (Capacity)**：改变模型参数规模（如：3B vs. 8B vs. 70B 模型）。
    *   **预算路由 (Budget-Tier Routing)**：
        *   **状态空间**：包含查询 $q$、当前模块输入及模块描述符。
        *   **动作空间**：每个模块可选 $\{LOW, MID, HIGH\}$ 三个层级。
        *   **算法流程**：使用 **PPO (Proximal Policy Optimization)** 强化学习算法训练轻量级路由器。
    *   **成本感知奖励函数**：
        $$r = r_{task} + \lambda \cdot \alpha \cdot r_{cost}$$
        其中 $\lambda$ 是权衡系数，$\alpha$ 是用于稳定训练的方差对齐因子，$r_{cost}$ 是基于 Token 使用量归一化后的成本奖励。

### 3. 实验设计
*   **数据集/场景**：
    *   **LoCoMo**：长程对话记忆评估。
    *   **LongMemEval**：长文本交互记忆基准。
    *   **HotpotQA**：多跳推理长上下文问答。
*   **对比基线 (Baselines)**：对比了 7 种强力基线，包括 ReadAgent, MemoryBank, A-MEM, LangMem, Mem0, MemoryOS 和 LightMem。
*   **评估指标**：F1 分数、LLM-as-a-Judge（使用 GPT-4 等级模型评分）以及实际货币成本（美元）。

### 4. 资源与算力
*   **模型使用**：主干模型采用 **LLaMA-3.3-70B-Instruct** 和 **Qwen3-Next-80B-A3B-Instruct**。
*   **训练细节**：路由器使用 Adam 优化器，Batch Size 为 32，训练步数最高 600 步。
*   **算力说明**：论文**未明确说明**具体的 GPU 硬件型号（如 A100 数量）或总训练时长。成本计算是基于 Together.ai 的 API 公开定价（Token 计费）进行的。

### 5. 实验数量与充分性
*   **实验组数**：
    *   在 3 个数据集上进行了全量对比实验。
    *   针对 3 种预算策略（实现、推理、容量）分别绘制了性能-成本帕累托前沿曲线。
    *   进行了奖励对齐消融实验、检索块数量敏感性分析、模块路由比例分析。
*   **充分性与公平性**：实验设计较为充分，通过统一检索 Top-K=5 等参数确保了 BudgetMem 与基线方法在相同上下文范围内的公平对比。

### 6. 主要结论与发现
*   **性能优势**：在“性能优先”设置下，BudgetMem 显著超越了所有基线模型。
*   **成本控制**：BudgetMem 成功构建了更优的帕累托前沿（Pareto Frontier），即在相同成本下准确率更高，或在相同准确率下成本更低。
*   **策略特性**：
    *   **容量策略**在高预算下表现最强，能持续推高性能上限。
    *   **实现策略**在低预算区间性价比极高。
    *   **推理策略**的成本分布最集中，适合在有限成本波动内优化质量。

### 7. 优点
*   **显式权衡**：首次为运行时智能体记忆提供了可量化、可调节的预算控制开关。
*   **查询感知**：能够识别查询的难易程度，简单问题用低预算，复杂问题自动调用高预算模块。
*   **模块化解耦**：预算层级与流水线结构解耦，具有良好的扩展性和模型无关性。

### 8. 不足与局限
*   **流水线拓扑固定**：目前采用固定的“过滤-提取-摘要”顺序，可能无法涵盖所有复杂的记忆操作场景。
*   **API 成本模型局限**：实验成本基于特定 API 定价，若在本地部署（关注显存或延迟而非 Token 计费），其路由策略可能需要重新训练。
*   **训练成本**：虽然路由器本身轻量，但强化学习训练过程需要大量环境交互（调用 LLM 获取奖励），初始训练开销不容忽视。

（完）
