---
title: "REMem: Reasoning with Episodic Memory in Language Agent"
title_zh: REMem：语言智能体中基于情境记忆的推理
authors: "Yiheng Shu, Saisri Padmaja Jonnalagedda, Xiang Gao, Bernal Jiménez Gutiérrez, Weijian Qi, Kamalika Das, Huan Sun, Yu Su"
date: 2026-02-13
pdf: "https://arxiv.org/pdf/2602.13530v2"
tags: ["query:agimem"]
score: 10.0
evidence: 用于交互历史的情景记忆框架
tldr: 针对现有语言智能体在情境记忆方面存在的建模缺失和推理能力不足问题，本文提出了 REMem 框架。该框架通过离线构建包含时间感知摘要与事实的混合记忆图谱，并结合在线代理式检索器进行迭代推理，显著提升了智能体在复杂交互历史中的回忆与推理表现，在多项基准测试中优于现有主流系统。
selection_source: fresh_fetch
motivation: 现有智能体记忆多为语义化，缺乏对具有时空背景的具体经历进行有效建模和跨事件复杂推理的能力。
method: 提出 REMem 框架，通过离线构建混合记忆图谱并利用在线代理式检索工具实现对情境记忆的迭代检索与推理。
result: "在四个基准测试中，REMem 在情境回忆和推理任务上分别比 Mem0 和 HippoRAG 2 提升了 3.4% 和 13.4%。"
conclusion: REMem 有效增强了语言智能体处理复杂历史交互的能力，并展现出更稳健的不可回答问题拒绝机制。
---

## 摘要
人类擅长记忆时空背景下的具体经历，并能跨事件进行推理，即具备情境记忆（episodic memory）的能力。相比之下，语言智能体的记忆主要仍是语义性的，目前的智能体尚无法有效地回忆和推理交互历史。我们针对这一差距识别并形式化了情境回忆与推理的核心挑战，并观察到现有工作往往忽视了情境性、缺乏显式的事件建模，或者过度强调简单检索而非复杂推理。我们提出了 REMem，一个用于构建情境记忆并进行推理的两阶段框架：1) 离线索引，REMem 将经历转换为混合记忆图谱，灵活地链接具有时间感知能力的要点（gists）和事实；2) 在线推理，REMem 采用一个配备精心设计工具的智能体检索器，在记忆图谱上进行迭代检索。在四个情境记忆基准测试上的全面评估表明，REMem 的性能显著优于 Mem0 和 HippoRAG 2 等最先进的记忆系统，在情境回忆和推理任务上分别实现了 3.4% 和 13.4% 的绝对提升。此外，REMem 在处理无法回答的问题时也表现出更稳健的拒绝行为。

## Abstract
Humans excel at remembering concrete experiences along spatiotemporal contexts and performing reasoning across those events, i.e., the capacity for episodic memory. In contrast, memory in language agents remains mainly semantic, and current agents are not yet capable of effectively recollecting and reasoning over interaction histories. We identify and formalize the core challenges of episodic recollection and reasoning from this gap, and observe that existing work often overlooks episodicity, lacks explicit event modeling, or overemphasizes simple retrieval rather than complex reasoning. We present REMem, a two-phase framework for constructing and reasoning with episodic memory: 1) Offline indexing, where REMem converts experiences into a hybrid memory graph that flexibly links time-aware gists and facts. 2) Online inference, where REMem employs an agentic retriever with carefully curated tools for iterative retrieval over the memory graph. Comprehensive evaluation across four episodic memory benchmarks shows that REMem substantially outperforms state-of-the-art memory systems such as Mem0 and HippoRAG 2, showing 3.4% and 13.4% absolute improvements on episodic recollection and reasoning tasks, respectively. Moreover, REMem also demonstrates more robust refusal behavior for unanswerable questions.

---

## 论文详细总结（自动生成）

### 论文总结：REMem —— 语言智能体中基于情境记忆的推理

#### 1. 核心问题与整体含义（研究动机和背景）
人类具备“心理时间旅行”的能力，即**情境记忆（Episodic Memory）**，能够记住具体事件发生的时空背景（时间、地点、参与者、情感）并进行跨事件推理。
目前的语言智能体（Language Agents）主要依赖**语义记忆（Semantic Memory）**，即存储事实性知识，但在处理长期的交互历史时存在以下缺陷：
*   **缺乏显式事件建模**：现有的 RAG 或图记忆系统往往将信息碎片化，丢失了事件的连贯性和时空上下文。
*   **推理能力不足**：现有方法多依赖简单的相似度检索，难以处理涉及时间顺序、持续时间、事件关联等复杂逻辑的查询。
**REMem** 的提出旨在通过构建一个时间感知的混合记忆图谱，并结合智能体化的迭代推理，解决智能体在“情境回忆”和“情境推理”方面的核心挑战。

#### 2. 论文提出的方法论
REMem 框架分为两个核心阶段：

*   **离线索引阶段（Indexing）**：将原始交互记录转换为**混合记忆图谱（Hybrid Memory Graph）**。
    *   **要点提取（Gist Extraction）**：将每个事件或对话片段总结为自然语言摘要（Gist），并解析出绝对时间戳。
    *   **事实提取（Fact Extraction）**：提取（主语、谓语、宾语）三元组，并附加 Wikidata 风格的时间限定符（如开始/结束时间）。
    *   **图谱构建**：图谱包含“要点节点”和“短语节点”。同一次交互产生的要点与短语相连；语义相似的要点之间建立“同义边”以增强连通性。
*   **在线推理阶段（Agentic Inference）**：采用 **ReAct 风格**的智能体进行多步推理。
    *   **工具箱**：智能体可调用语义检索（Embedding）、词法检索（BM25）、图探索（寻找特定要点或实体的上下文）以及流程控制工具。
    *   **迭代检索**：智能体根据查询分解子问题，通过工具在图谱中进行“心理时间旅行”，过滤时间范围并收集证据，最后汇总生成答案。

#### 3. 实验设计
*   **数据集与场景**：
    *   **情境回忆任务**：使用 **LoCoMo**（合成对话）和 **REALTALK**（真实人类对话），评估智能体对过去事件细节的回忆能力。
    *   **情境推理任务**：使用 **Complex-TR** 和 **Test of Time**，评估涉及时间排序、计数、持续时间估计等复杂逻辑的能力。
*   **对比方法**：
    *   **强基线 RAG**：使用 Qwen3-Embedding-8B 和 NV-Embed-v2。
    *   **结构化记忆系统**：Mem0、Graphiti、HippoRAG 2。
    *   **提示词增强方法**：TISER（专门针对时间推理的提示词技术）。
*   **评估指标**：F1 分数、BLEU-1、LLM-as-a-judge 评分，以及 Test of Time 专用的 Exact Match (EM)。

#### 4. 资源与算力
*   **硬件环境**：论文在附录 F.2 中明确提到，实验在一台配备 **双路 AMD EPYC 7643 CPU**、**4 张 NVIDIA A100-SXM4-80GB GPU** 以及 **1 TB 系统内存**的服务器上运行。
*   **模型使用**：默认使用 **GPT-4o-mini** 作为推理和提取的骨干 LLM，使用 **NV-Embed-v2** 作为嵌入模型。
*   **效率指标**：论文记录了不同数据集的索引时间和单次查询推理时间（如 LoCoMo 索引约 3604 秒，单次推理约 4.3 秒）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流基准测试，总计处理了数千个查询和超过 10 万条消息/文本块。
*   **消融实验**：非常充分。作者分别验证了“去掉要点（Gists）”、“去掉事实（Facts）”、“去掉同义边”以及“去掉特定检索工具”对性能的影响，证明了混合结构的必要性。
*   **多维度评估**：除了标准 QA 指标，还进行了**拒绝行为分析**（针对不可回答问题的处理）、**人类评估**（验证 LLM 裁判的公正性）以及**错误分析**（分类总结了 100 组错误案例）。
*   **公平性**：所有对比方法均在相同的 LLM 和嵌入模型下重新运行或对齐，确保了比较的客观性。

#### 6. 论文的主要结论与发现
*   **性能领先**：REMem 在情境回忆上比 SOTA 提升了 **3.4%**，在情境推理上提升了 **13.4%**。
*   **推理能力极强**：在 Test of Time 基准测试中，REMem 是唯一一个 EM 分数超过 **90%** 的方法。
*   **拒绝行为更稳健**：相比于其他系统容易产生幻觉或过度拒绝，REMem 在处理不可回答的问题时表现出更高的精确度（Precision 73.3%）。
*   **混合结构的价值**：要点（Gist）提供了主要的场景上下文，而事实（Fact）在跨会话的多跳推理中起到了关键的锚点作用。

#### 7. 优点
*   **显式时空建模**：通过将时间戳和情境维度（参与者、地点等）直接绑定到记忆节点，解决了传统 RAG 丢失时空背景的问题。
*   **工具化推理的灵活性**：允许智能体自主决定何时进行语义搜索、何时进行图探索，能够处理复杂的逻辑组合（如“在 A 事件之后、B 事件之前发生了什么”）。
*   **高 Token 效率**：相比于全上下文（Full-Context）输入，REMem 在保持高性能的同时显著减少了推理时的 Token 消耗。

#### 8. 不足与局限
*   **推理成本**：虽然比全上下文省钱，但多步 Agent 推理（REMem-I）相比单步检索仍需多次 LLM 调用，增加了延迟和成本。
*   **逻辑词歧义**：错误分析显示，智能体在处理“之前/之后”等边界条件时，有时无法准确映射到工具调用的参数上。
*   **静态索引限制**：目前主要针对离线批处理索引，对于实时流式（Streaming）交互中的记忆动态更新和遗忘机制讨论较少。
*   **对骨干模型依赖**：性能高度依赖于 LLM 的工具调用能力，在较弱的模型上可能效果打折。

（完）
