---
title: "HyMem: Hybrid Memory Architecture with Dynamic Retrieval Scheduling"
title_zh: HyMem：具有动态检索调度的混合记忆架构
authors: "Xiaochen Zhao, Kaikai Wang, Xiaowen Zhang, Chen Yao, Aili Wang"
date: 2026-02-15
pdf: "https://arxiv.org/pdf/2602.13933v1"
tags: ["query:agimem"]
score: 10.0
evidence: 具有动态检索调度功能的LLM智能体混合记忆架构
tldr: "针对大语言模型在长对话中内存管理效率与效果难以兼得的问题，本文提出HyMem架构。该架构受认知经济原则启发，采用双粒度存储（摘要与原始文本）和动态两级检索系统。通过轻量级模块处理简单查询，并仅在复杂任务时激活基于LLM的深度检索与反思机制。HyMem在保持高性能的同时，将计算成本降低了92.6%，实现了长效记忆管理的最优平衡。"
selection_source: fresh_fetch
motivation: 现有的单体内存表示和静态检索机制无法在保证推理精度的同时兼顾计算效率，难以适应多样化的任务场景。
method: 提出一种结合摘要级和原始级双粒度存储的混合架构，并利用动态两级检索系统根据查询复杂度按需调度计算资源。
result: "在LOCOMO和LongMemEval基准测试中表现优异，超越了全上下文基准，并显著降低了92.6%的计算开销。"
conclusion: HyMem通过多粒度表示与动态调度机制，为长文本对话中的高效记忆管理提供了一种兼具性能与经济性的新方案。
---

## 摘要
大语言模型 (LLM) 智能体在短文本上下文中表现出强大的性能，但由于记忆管理效率低下，在长对话中往往表现不佳。现有方法面临效率与有效性之间的根本权衡：记忆压缩存在丢失复杂推理所需关键细节的风险，而保留原始文本则会为简单查询引入不必要的计算开销。问题的核心在于单一记忆表示和静态检索机制的局限性，它们无法模拟人类所具备的灵活且主动的记忆调度能力，从而难以适应多样的问题场景。受认知经济性原则的启发，我们提出了 HyMem，这是一种混合记忆架构，通过多粒度记忆表示实现动态按需调度。HyMem 采用双粒度存储方案，并配以动态两层检索系统：轻量级模块构建摘要级上下文以实现高效的响应生成，而基于 LLM 的深度模块仅在处理复杂查询时被选择性激活，并辅以反思机制进行迭代推理优化。实验表明，HyMem 在 LOCOMO 和 LongMemEval 基准测试中均取得了优异的性能，在超越全上下文表现的同时，降低了 92.6% 的计算成本，在长期记忆管理的效率与性能之间建立了最先进的平衡。

## Abstract
Large language model (LLM) agents demonstrate strong performance in short-text contexts but often underperform in extended dialogues due to inefficient memory management. Existing approaches face a fundamental trade-off between efficiency and effectiveness: memory compression risks losing critical details required for complex reasoning, while retaining raw text introduces unnecessary computational overhead for simple queries. The crux lies in the limitations of monolithic memory representations and static retrieval mechanisms, which fail to emulate the flexible and proactive memory scheduling capabilities observed in humans, thus struggling to adapt to diverse problem scenarios. Inspired by the principle of cognitive economy, we propose HyMem, a hybrid memory architecture that enables dynamic on-demand scheduling through multi-granular memory representations. HyMem adopts a dual-granular storage scheme paired with a dynamic two-tier retrieval system: a lightweight module constructs summary-level context for efficient response generation, while an LLM-based deep module is selectively activated only for complex queries, augmented by a reflection mechanism for iterative reasoning refinement. Experiments show that HyMem achieves strong performance on both the LOCOMO and LongMemEval benchmarks, outperforming full-context while reducing computational cost by 92.6\%, establishing a state-of-the-art balance between efficiency and performance in long-term memory management.

---

## 论文详细总结（自动生成）

以下是对论文《HyMem: Hybrid Memory Architecture with Dynamic Retrieval Scheduling》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对大语言模型（LLM）智能体在长对话场景中面临的**记忆管理困境**：
*   **效率与效果的矛盾**：现有的记忆压缩技术（如摘要化）虽然节省计算资源，但容易丢失复杂推理所需的关键细节；而保留原始文本虽然信息完整，但在处理简单查询时会产生巨大的计算开销（Token 冗余）。
*   **静态检索的局限**：传统方法多采用固定的向量检索模式，无法像人类记忆系统那样根据任务复杂度灵活调整检索深度。
*   **研究动机**：受人类“认知经济性”原则启发，即大脑对常规查询优先进行低成本的概要检索，仅在需要深度逻辑推理时才激活高成本的详细记忆。

### 2. 方法论：核心思想与关键技术
HyMem 提出了一种**双粒度存储**与**动态两级检索**相结合的混合架构：
*   **双粒度存储模块**：
    *   **Level-1（摘要级）**：将原始对话划分为事件单元，提取核心要素（时间、地点、人物、梗概），用于快速检索和轻量级推理。
    *   **Level-2（原始文本级）**：保留原始对话细节，并与 Level-1 建立映射链接，仅在需要时回溯。
*   **动态两级检索系统**：
    *   **轻量级模块（Lightweight Module）**：首先对 Level-1 记忆进行向量检索。如果该模块判断当前上下文足以回答问题，则直接生成答案。
    *   **深度模块（Deep Module）**：当轻量级模块检测到信息缺失时被激活。它采用两阶段检索：先通过语义相似度粗筛，再利用 LLM 进行“自检索”以识别逻辑或因果相关的关键事件，最后回溯到 Level-2 原始文本重建上下文。
*   **反思模块（Reflection Module）**：在输出前进行完整性检查。若发现问题未完全解决（如多跳查询），则自动分解问题并重写查询，触发新一轮的迭代检索。

### 3. 实验设计
*   **数据集**：
    *   **LOCOMO**：包含单跳、多跳、开放域知识和时间推理四类任务，平均对话长度达 9,000 Token。
    *   **LongMemEval-s**：专注于超长上下文记忆和复杂因果链推理的基准测试。
*   **对比方法**：包括 LangMem、A-Mem、Mem0、Zep、LightMem、Nemori、MIRIX 等 SOTA 记忆管理框架，以及 **Full-Context**（全上下文输入）和 **Naive RAG**（朴素检索增强生成）。
*   **评估指标**：采用 LLM-as-a-Judge（使用 GPT-4o-mini 和 GPT-4.1-mini 作为裁判）评估答案的准确性，并统计平均消耗的 Token 数量。

### 4. 资源与算力
*   **模型使用**：实验主要基于 **GPT-4o-mini** 和 **GPT-4.1-mini** 作为基础模型和裁判模型。
*   **嵌入模型**：使用了 **Qwen3-Embedding-0.6B** 进行向量化。
*   **算力细节**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）及训练/推理的总时长。由于该方法主要基于现有的闭源或开源模型进行推理侧的架构设计，其核心开销体现在 API 调用或模型推理的 Token 消耗上。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流长文本基准上进行了全面测试，涵盖了数千个测试用例。
*   **消融实验**：非常充分。分别验证了 Level-1/Level-2 存储、轻量级/深度检索模块以及反思模块对性能和成本的影响。
*   **分析深度**：额外进行了**压缩损失分析**（对比 LLMLingua-2）和**超参数敏感性分析**（如检索数量 $k$ 的影响）。
*   **客观性**：通过与“全上下文”这一理论上限进行对比，证明了 HyMem 在某些复杂任务（如多跳推理）上甚至超越了全上下文表现，实验设计较为公正。

### 6. 主要结论与发现
*   **性能卓越**：HyMem 在 LOCOMO 的多跳和开放域任务上分别比全上下文基准提高了 **10.46%** 和 **5.20%**。
*   **极端高效**：在保持高准确率的同时，相比全上下文方法，HyMem 将计算成本（Token 消耗）降低了 **92.6%**。
*   **动态调度的价值**：实验证明约 70% 的查询可以通过轻量级模块解决，而剩下的 30% 复杂查询通过深度模块和反思机制得到了有效处理，验证了按需分配资源的合理性。

### 7. 优点（亮点）
*   **认知启发式设计**：成功将人类记忆的“认知经济性”转化为可实现的工程架构。
*   **回溯机制**：通过 Level-1 到 Level-2 的链接，既保证了检索的广度（摘要），又保证了推理的深度（原文）。
*   **迭代优化**：反思模块解决了传统 RAG 难以处理的复杂依赖和多轮推理问题。
*   **高性价比**：在长对话场景下实现了极佳的性能-成本平衡点。

### 8. 不足与局限
*   **延迟风险**：对于需要激活深度模块和多次反思的复杂查询，由于涉及多次 LLM 调用，可能会增加端到端的响应延迟。
*   **依赖基础模型能力**：深度检索和反思高度依赖 LLM 的判断力，若基础模型（如 GPT-4.1-mini）在自检索阶段出错，可能导致回溯失败。
*   **应用范围**：目前主要针对文本对话，尚未探讨在多模态记忆或实时流式任务中的表现。
*   **算力细节缺失**：缺乏具体的本地部署成本和推理时间统计，不便于开发者评估实际部署的硬件需求。

（完）
