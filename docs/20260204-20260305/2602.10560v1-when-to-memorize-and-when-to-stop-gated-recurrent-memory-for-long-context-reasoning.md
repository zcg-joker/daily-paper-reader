---
title: "When to Memorize and When to Stop: Gated Recurrent Memory for Long-Context Reasoning"
title_zh: 何时记忆，何时停止：用于长上下文推理的门控循环记忆
authors: "Leheng Sheng, Yongtao Zhang, Wenchang Ma, Yaorui Shi, Ting Huang, Xiang Wang, An Zhang, Ke Shen, Tat-Seng Chua"
date: 2026-02-11
pdf: "https://arxiv.org/pdf/2602.10560v1"
tags: ["query:agimem"]
score: 9.0
evidence: 文本控制的门控记忆更新和退出机制
tldr: "针对大语言模型在长文本推理中性能下降及计算冗余问题，本文提出GRU-Mem。该方法借鉴门控循环单元思想，在MemAgent基础上引入更新门和退出门，通过强化学习训练模型自主决定何时更新记忆及何时停止处理。实验表明，GRU-Mem在提升长文本推理准确性的同时，推理速度最高可提升400%，实现了效率与效果的双重优化。"
selection_source: fresh_fetch
motivation: 现有的循环记忆机制存在无差别更新导致记忆爆炸以及缺乏退出机制导致计算浪费的问题。
method: 提出GRU-Mem，通过引入文本控制的更新门和退出门，并结合端到端强化学习训练，实现对记忆更新和循环终止的精准控制。
result: 在多种长文本推理任务中，GRU-Mem不仅性能优于MemAgent，且推理速度最高提升了4倍。
conclusion: 引入门控机制和提前退出策略能有效解决长文本处理中的记忆冗余与计算效率问题，是提升长文本推理能力的有效途径。
---

## 摘要
虽然长上下文推理对各种现实应用至关重要，但对于大语言模型（LLMs）而言仍具挑战性，因为随着上下文长度的增加，它们的性能会下降。最近的工作 MemAgent 尝试通过在类似 RNN 的循环中逐块处理上下文，并更新文本记忆以进行最终回答来解决这一问题。然而，这种朴素的循环记忆更新面临两个关键缺陷：（i）记忆可能会迅速膨胀，因为它会不加区分地更新，即使是在没有证据的块上；（ii）循环缺乏退出机制，导致在收集到足够证据后仍进行不必要的计算。为了解决这些问题，我们提出了 GRU-Mem，它结合了两个文本控制的门控，以实现更稳定、更高效的长上下文推理。具体而言，在 GRU-Mem 中，只有当更新门开启时记忆才会更新，且一旦退出门开启，循环将立即退出。为了赋予模型这种能力，我们在端到端强化学习（RL）中引入了两个奖励信号 $r^{\text{update}}$ 和 $r^{\text{exit}}$，分别奖励正确的更新和退出行为。在各种长上下文推理任务上的实验证明了 GRU-Mem 的有效性和效率，其表现通常优于原始的 MemAgent，且推理速度提升高达 400%。

## Abstract
While reasoning over long context is crucial for various real-world applications, it remains challenging for large language models (LLMs) as they suffer from performance degradation as the context length grows. Recent work MemAgent has tried to tackle this by processing context chunk-by-chunk in an RNN-like loop and updating a textual memory for final answering. However, this naive recurrent memory update faces two crucial drawbacks: (i) memory can quickly explode because it can update indiscriminately, even on evidence-free chunks; and (ii) the loop lacks an exit mechanism, leading to unnecessary computation after even sufficient evidence is collected. To address these issues, we propose GRU-Mem, which incorporates two text-controlled gates for more stable and efficient long-context reasoning. Specifically, in GRU-Mem, the memory only updates when the update gate is open and the recurrent loop will exit immediately once the exit gate is open. To endow the model with such capabilities, we introduce two reward signals $r^{\text{update}}$ and $r^{\text{exit}}$ within end-to-end RL, rewarding the correct updating and exiting behaviors respectively. Experiments on various long-context reasoning tasks demonstrate the effectiveness and efficiency of GRU-Mem, which generally outperforms the vanilla MemAgent with up to 400\% times inference speed acceleration.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **GRU-Mem** 的新型长上下文推理框架，旨在解决大语言模型（LLM）在处理超长文本时的效率与稳定性问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：虽然 LLM 的上下文窗口在不断扩大，但在处理超长文本时仍面临性能下降（如“迷失在中间”现象）和超出窗口限制的问题。
*   **背景**：现有的循环记忆方法（如 MemAgent）通过分块处理文本并维护一个“文本记忆”来应对长文本。但这种方法存在两个致命缺陷：
    1.  **记忆爆炸**：模型会不加区分地将无关信息存入记忆，导致记忆量迅速膨胀并引入噪声。
    2.  **缺乏退出机制**：即使已经找到了答案所需的全部证据，模型仍必须处理完所有文本块，造成巨大的计算浪费。
*   **核心目标**：借鉴经典循环神经网络（RNN）中 GRU 的门控思想，为记忆代理引入“更新门”和“退出门”，实现“按需记忆”和“见好就收”。

### 2. 方法论
*   **核心思想**：在循环处理流程中引入两个文本控制的门控机制：**更新门（Update Gate, UG）**和**退出门（Exit Gate, EG）**。
*   **关键技术细节**：
    *   **工作流**：将长文本 $C$ 分为 $T$ 个固定大小的块。在每一步 $t$，记忆代理 $\phi_\theta$ 接收问题 $Q$、当前块 $C_t$ 和前一时刻记忆 $M_{t-1}$，输出：更新决策 $U_t$（是/否）、候选记忆 $\hat{M}_t$ 和退出决策 $E_t$（继续/结束）。
    *   **门控逻辑**：若 $U_t$ 为“是”，则更新记忆；否则保持旧记忆。若 $E_t$ 为“结束”，则立即终止循环，由回答代理 $\psi_\theta$ 给出答案。
*   **强化学习训练（RL）**：
    *   使用 **DAPO**（一种多轮 GRPO 扩展算法）进行端到端优化。
    *   **奖励设计**：
        *   **结果奖励 ($r^{outcome}$)**：答案是否正确。
        *   **更新奖励 ($r^{update}$)**：奖励模型在含证据块时选择更新，在无证据块时选择跳过。
        *   **退出奖励 ($r^{exit}$)**：奖励模型在获取最后一条证据后立即退出，惩罚过早或过晚退出。
        *   **格式奖励 ($r^{format}$)**：确保模型输出符合预定义的标签格式。
    *   **优势计算**：通过超参数 $\alpha$ 平衡轨迹级（结果/退出）和轮次级（更新）的优势函数。

### 3. 实验设计
*   **数据集/场景**：
    *   **多跳 QA**：HotpotQA (HQA)。
    *   **单跳 QA**：SQuAD。
    *   **大海捞针 (NIAH) 变体**：包括单键 (SK)、多键 (MK)、多查询 (MQ) 和多值 (MV) 任务。
*   **对比基准 (Benchmark)**：主要对比原始的 **MemAgent**（无门控的循环记忆模型）。
*   **模型规模**：基于 Qwen2.5-3B-Instruct 和 Qwen2.5-7B-Instruct。
*   **上下文长度**：测试范围从 7K 到 896K 标记。

### 4. 资源与算力
*   **算力设备**：实验在一个拥有 **8 张 GPU 的节点**上进行（具体型号未标明，但提到使用了 `verl` 强化学习框架）。
*   **训练细节**：学习率为 $1 \times 10^{-6}$，分块大小为 5000 标记，训练批次大小为 128，采样温度为 1.0。训练持续到验证集奖励收敛。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 10 种不同类型的长文本任务，并在 3B 和 7B 两种参数规模下进行了验证。
*   **消融实验**：系统分析了超参数 $\alpha$ 对训练动态的影响、RL 训练的有效性，以及在证据分布不均（模拟重排序场景）下的表现。
*   **充分性评价**：实验设计较为全面，不仅关注准确率，还深入分析了记忆增长曲线和退出位置的准确性，客观地展示了门控机制的作用。

### 6. 主要结论与发现
*   **性能提升**：GRU-Mem 在大多数任务上优于 MemAgent，尤其是在处理分布外（OOD）任务和更小规模模型（3B）时表现更稳健。
*   **效率飞跃**：相比 MemAgent，GRU-Mem 在不使用退出门时已有约 200% 的加速；开启退出门后，在某些任务上实现了 **400% 的推理加速**。
*   **稳定性**：更新门有效抑制了记忆膨胀，使记忆大小保持在合理范围内，避免了因噪声累积导致的性能崩溃。

### 7. 优点
*   **机制创新**：将经典的门控循环思想成功引入到基于文本的 Agent 记忆管理中，解决了长文本处理中的冗余计算问题。
*   **训练方案完备**：设计了精细的奖励函数和优势计算方法，使模型能够自主学习复杂的逻辑决策（何时记忆、何时停止）。
*   **灵活性**：提供了带退出门和不带退出门两种推理模式，以适应不同类型的任务需求。

### 8. 不足与局限
*   **任务覆盖面**：目前主要集中在问答（QA）领域，尚未在摘要生成等需要全局扫描的任务上进行广泛验证。
*   **训练复杂性**：引入额外的奖励信号（更新和退出奖励）可能会降低 RL 训练的稳定性，对超参数（如 $\alpha$）较为敏感。
*   **收敛速度**：由于任务逻辑变复杂，模型可能需要更长的收敛时间和更小的离策（off-policy）程度。

（完）
