---
title: "Pancake: Hierarchical Memory System for Multi-Agent LLM Serving"
title_zh: Pancake：面向多智能体 LLM 推理服务的层级化存储系统
authors: "Zhengding Hu, Zaifeng Pan, Prabhleen Kaur, Vibha Murthy, Zhongkai Yu, Yue Guan, Zhen Wang, Steven Swanson, Yufei Ding"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.21477v1"
tags: ["query:agimem"]
score: 10.0
evidence: 用于多智能体LLM服务的分层记忆系统和管理
tldr: Pancake 是专为多智能体大模型服务设计的层级化内存系统。针对大规模存储、频繁更新及多智能体共存带来的高昂近似最近邻（ANN）搜索开销，Pancake 引入了多级索引缓存、跨智能体协调管理及 GPU-CPU 协同加速技术。该系统兼容 Mem-GPT、LangChain 等主流框架，在真实负载下将端到端吞吐量提升了 4.29 倍以上，显著优化了智能体内存管理的效率。
selection_source: fresh_fetch
motivation: 多智能体场景下大规模内存存储、频繁更新和复杂的向量搜索导致了极高的计算成本和系统瓶颈。
method: 设计了集成多级索引缓存、跨智能体索引协调管理以及 GPU-CPU 协同加速的层级化内存管理系统。
result: 在真实智能体工作负载测试中，Pancake 相比现有框架实现了超过 4.29 倍的端到端吞吐量提升。
conclusion: Pancake 通过创新的层级化架构有效解决了多智能体 LLM 服务的内存管理难题，为高效智能体应用提供了有力支撑。
---

## 摘要
在这项工作中，我们识别并解决了 LLM 推理服务中智能体记忆管理的核心挑战。在这些场景下，大规模存储、频繁更新以及多个智能体共存共同引入了复杂且高成本的近似最近邻（ANN）搜索问题。我们提出了 Pancake，这是一个多层级智能体记忆系统，它统一了三项关键技术：(i) 面向单个智能体的多级索引缓存，(ii) 跨多个智能体的协调索引管理，以及 (iii) GPU-CPU 协同加速。Pancake 提供了易于使用的接口，可集成到 Mem-GPT 等基于记忆的智能体中，并兼容 LangChain 和 LlamaIndex 等智能体框架。在真实智能体工作负载上的实验表明，Pancake 的性能显著优于现有框架，实现了超过 4.29 倍的端到端吞吐量提升。

## Abstract
In this work, we identify and address the core challenges of agentic memory management in LLM serving, where large-scale storage, frequent updates, and multiple coexisting agents jointly introduce complex and high-cost approximate nearest neighbor (ANN) searching problems. We present Pancake, a multi-tier agentic memory system that unifies three key techniques: (i) multi-level index caching for single agents, (ii) coordinated index management across multiple agents, and (iii) collaborative GPU-CPU acceleration. Pancake exposes easy-to-use interface that can be integrated into memory-based agents like Mem-GPT, and is compatible with agentic frameworks such as LangChain and LlamaIndex. Experiments on realistic agent workloads show that Pancake substantially outperforms existing frameworks, achieving more than 4.29x end-to-end throughput improvement.

---

## 论文详细总结（自动生成）

以下是对论文《Pancake: Hierarchical Memory System for Multi-Agent LLM Serving》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
随着大模型智能体（Agent）向长程对话、复杂规划和多智能体协作演进，**智能体记忆（Agent Memory）**成为提升生成质量的关键。与传统的静态检索增强生成（RAG）不同，智能体记忆具有**高动态性**（频繁插入新生成的动作、思考和历史）和**多智能体共存**的特点。
*   **核心痛点**：现有的向量数据库（ANN 搜索）在处理智能体负载时存在严重瓶颈。当记忆规模增大时，内存操作耗时可占总执行时间的 82% 以上。
*   **具体挑战**：
    1.  **单智能体更新低效**：频繁的小批量插入会导致向量在索引空间中“散乱”，破坏搜索局部性。
    2.  **多智能体检索开销大**：为每个智能体维护独立索引会导致跨作用域搜索时粗粒度检索（Coarse Search）成本激增。
    3.  **硬件利用率失衡**：GPU 显存受限于模型权重和 KV Cache，难以容纳大规模动态更新的向量索引。

### 2. 论文提出的方法论：Pancake 系统架构
Pancake 是一个多层级存储系统，核心思想是**利用智能体工作负载的语义局部性和步骤规律性来优化索引结构**。

*   **关键技术一：模式驱动的多级索引缓存（Multi-Level Cache）**
    *   **三级结构**：L0（最近访问的微簇）、L1（频繁访问的邻域簇）、L2（稳定的大规模簇）。
    *   **FSM 模式建模**：将智能体的记忆访问抽象为有限状态机（FSM），捕捉智能体在推理步骤（如：规划 -> 工具调用 -> 反思）之间的语义转移规律。
    *   **预取与早停**：基于 FSM 预测下一步可能访问的簇并提前加载，同时利用距离阈值实现搜索早停。

*   **关键技术二：多智能体混合图索引（Hybrid Graph）**
    *   **统一导航**：将多个智能体的私有索引与全局静态索引通过“门户节点（Portal Nodes）”连接，形成混合图，使跨作用域搜索只需一次图遍历。
    *   **智能体画像（Agent Profile）**：在共享簇中记录特定智能体的访问偏好，优化细粒度搜索顺序。

*   **关键技术三：GPU-CPU 协同管理**
    *   **热点缓存**：将高频访问的簇驻留在 GPU，利用 GPU 加速高维向量计算。
    *   **插入缓冲区（Insertion Buffer）**：在 CPU 侧缓冲小批量更新，避免频繁触发昂贵的 GPU 显存重分配和数据迁移。
    *   **异步一致性**：通过异步传输和 GPU 侧的 K-means 聚类分裂，保证搜索与更新的并发效率。

### 3. 实验设计
*   **数据集/场景**：涵盖多轮对话（UltraChat, UltraFeedback）、数学推理（Prm800k, Gsm8k）、工具调用（APIGen）和环境交互（AgentGym）。
*   **Benchmark 负载**：定义了四种模式：一搜一插（对话）、多搜后插（总结）、一搜多插（个性化生成）、仅搜索（RAG）。
*   **对比方法**：
    *   **智能体框架**：Mem-GPT, A-Mem, LlamaIndex, LangMem (LangChain)。
    *   **向量数据库**：SpFresh, Quake, DiskANN。
    *   **消融变体**：Pancake-IVF-Static, Pancake-IVF-Split。

### 4. 资源与算力
*   **硬件配置**：使用了一台配备 **64 核 AMD EPYC 9534 处理器**和 **8 块 NVIDIA H100 (80GB) GPU** 的服务器。
*   **软件环境**：LLM 推理使用 vLLM 引擎（Llama 3.1-8B/70B），同时对比了 GPT-5 的 API 调用场景。
*   **显存分配**：实验中为 Pancake 预留了 5~15 GB 的 GPU 显存用于索引缓存，其余用于模型推理。

### 5. 实验数量与充分性
*   **实验规模**：论文在 6 个主流数据集上进行了端到端吞吐量测试，并针对多智能体扩展性（1 到 20 个智能体）、召回率与延迟的权衡、GPU 缓存大小的影响等进行了详细分析。
*   **充分性评价**：实验设计非常充分。不仅对比了应用层的智能体框架，还深入到底层向量数据库层面进行性能对标；涵盖了本地模型与云端 API 两种部署模式，证明了系统的通用性。

### 6. 论文的主要结论与发现
*   **性能提升**：Pancake 相比现有智能体框架实现了平均 **4.29 倍**（最高 26 倍）的端到端吞吐量提升。
*   **开销降低**：在大规模数据库下，将记忆操作的时间占比从超过 80% 降低至平均 **3.2%**。
*   **扩展性**：在多智能体混合负载下，Pancake 的性能下降控制在 10% 以内，而基准系统性能下降高达 55.9%。
*   **硬件效率**：GPU 协同机制在仅占用少量显存的情况下，使查询吞吐量进一步提升了 2.2 倍。

### 7. 优点：亮点与创新
*   **工作负载感知**：首次提出利用智能体推理的“步骤性（Step-wise）”特征（通过 FSM 建模）来优化动态向量索引，而非将其视为通用的数据库更新。
*   **系统级协同**：打通了应用层（智能体逻辑）、算法层（ANN 索引结构）与硬件层（GPU-CPU 异构存储），实现了全栈优化。
*   **易用性**：提供了简洁的 Python API，能够无缝集成到现有的 LangChain 或 LlamaIndex 生态中。

### 8. 不足与局限
*   **高维空间局限性**：虽然 FSM 缓解了散乱簇问题，但在极高维空间下的“距离集中效应”依然是向量索引的固有挑战。
*   **模型依赖性**：FSM 的有效性依赖于智能体推理步骤的规律性，对于行为极度随机或无固定逻辑的智能体，缓存命中率可能会下降。
*   **显存竞争**：在显存极度紧张（如运行超大参数模型）的场景下，GPU 缓存空间与 KV Cache 的动态竞争策略仍有待进一步细化研究。

（完）
