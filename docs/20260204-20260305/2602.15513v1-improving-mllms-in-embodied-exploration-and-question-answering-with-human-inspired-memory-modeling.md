---
title: Improving MLLMs in Embodied Exploration and Question Answering with Human-Inspired Memory Modeling
title_zh: 通过受人类启发的记忆建模提升多模态大语言模型在具身探索与问答中的表现
authors: "Ji Li, Jing Xia, Mingyi Li, Shiyan Hu"
date: 2026-02-17
pdf: "https://arxiv.org/pdf/2602.15513v1"
tags: ["query:agimem"]
score: 9.0
evidence: 为具身智能体解耦情境记忆和语义记忆
tldr: 针对具身智能体在长程观测和有限上下文下的挑战，本文提出一种受人类启发的非参数化存储框架。该框架显式解耦了情境记忆和语义记忆：通过语义检索与视觉推理结合的方式高效利用情境经验，并利用程序化规则提取构建可泛化的语义记忆。实验证明该方法在具身问答和探索任务中达到SOTA，显著提升了智能体的探索效率和复杂推理能力。
selection_source: fresh_fetch
motivation: 现有的具身存储方法多依赖文本摘要，导致视觉空间细节丢失且在动态环境中鲁棒性不足。
method: 提出一种解耦情境与语义记忆的框架，结合检索优先的视觉推理机制和程序化规则提取技术来增强智能体能力。
result: "在A-EQA和GOAT-Bench基准测试中均取得SOTA性能，其中A-EQA的LLM-Match指标提升了7.3%。"
conclusion: 研究表明情境记忆主要优化了探索效率，而语义记忆则显著增强了具身智能体的复杂推理能力。
---

## 摘要
将多模态大语言模型（MLLMs）部署为具身智能体的大脑仍面临挑战，特别是在长时程观测和有限上下文预算的情况下。现有的记忆辅助方法通常依赖于文本摘要，这会丢弃丰富的视觉和空间细节，且在非平稳环境中表现脆弱。在这项工作中，我们提出了一个非参数化记忆框架，该框架显式地解耦了用于具身探索和问答的情节记忆（episodic memory）与语义记忆（semantic memory）。我们的检索优先、推理辅助范式通过语义相似性召回情节经验，并通过视觉推理进行验证，从而在无需严格几何对齐的情况下实现对过去观测结果的鲁棒重用。与此同时，我们引入了一种程序风格的规则提取机制，将经验转化为结构化、可重用的语义记忆，从而促进跨环境的泛化。大量实验证明了该方法在具身问答和探索基准测试中达到了最先进的性能，在 A-EQA 上 LLM-Match 提升了 7.3%，LLM MatchXSPL 提升了 11.4%，在 GOAT-Bench 上成功率提升了 7.7%，SPL 提升了 6.8%。分析表明，我们的情节记忆主要提高了探索效率，而语义记忆则增强了具身智能体的复杂推理能力。

## Abstract
Deploying Multimodal Large Language Models as the brain of embodied agents remains challenging, particularly under long-horizon observations and limited context budgets. Existing memory assisted methods often rely on textual summaries, which discard rich visual and spatial details and remain brittle in non-stationary environments. In this work, we propose a non-parametric memory framework that explicitly disentangles episodic and semantic memory for embodied exploration and question answering. Our retrieval-first, reasoning-assisted paradigm recalls episodic experiences via semantic similarity and verifies them through visual reasoning, enabling robust reuse of past observations without rigid geometric alignment. In parallel, we introduce a program-style rule extraction mechanism that converts experiences into structured, reusable semantic memory, facilitating cross-environment generalization. Extensive experiments demonstrate state-of-the-art performance on embodied question answering and exploration benchmarks, yielding a 7.3% gain in LLM-Match and an 11.4% gain in LLM MatchXSPL on A-EQA, as well as +7.7% success rate and +6.8% SPL on GOAT-Bench. Analyses reveal that our episodic memory primarily improves exploration efficiency, while semantic memory strengthens complex reasoning of embodied agents.

---

## 论文详细总结（自动生成）

### 论文总结：基于人类启发式记忆建模提升具身智能体性能

#### 1. 核心问题与整体含义（研究动机和背景）
本文针对多模态大语言模型（MLLM）在具身智能体（Embodied Agents）应用中的两大瓶颈：**长程观测处理**和**有限上下文预算**。
*   **现有缺陷**：目前的存储辅助方法多依赖文本摘要（丢失视觉/空间细节）或硬性的几何地图融合（在环境变化、传感器噪声下易失效）。
*   **研究动机**：受人类记忆系统启发，作者认为智能体不应盲目信任过去信息或依赖死板的几何对齐，而应以“软性、关联”的方式召回经验，并通过主动感知进行验证。

#### 2. 论文提出的方法论
提出了一种非参数化存储框架，核心在于**显式解耦情节记忆与语义记忆**：
*   **情节记忆（Episodic Memory）**：
    *   **检索优先与推理辅助**：通过视觉语义相似性（CLIP/SAM 嵌入）检索最相关的过去观测。
    *   **视觉推理验证**：利用 MLLM 判断检索到的观测是否来自附近，并决定是否需要进一步探索。
    *   **跨空间交互**：将检索到的相机位姿投影到 2D 占据网格（Occupancy Grid）上，实现语义与物理空间的解耦交互，无需持续的在线几何融合。
*   **语义记忆（Semantic Memory）**：
    *   **程序风格规则提取**：将推理日志转化为伪代码工作流（控制流和决策逻辑）。
    *   **决策偏差检测**：通过计算智能体轨迹与真实轨迹的欧氏距离，识别关键错误点（Debug 模式），从而提取出结构化的“if-then”或“问题-方案”规则，增强跨环境泛化能力。
*   **元记忆（Meta Memory）**：通过预定义的认知状态（探索、目标验证、双重检查、准备回答）实现高层行为的自动切换。

#### 3. 实验设计
*   **数据集/场景**：
    *   **Open-EQA (A-EQA)**：评估具身问答能力，包含物体识别、空间推理、常识等类别。
    *   **GOAT-Bench**：评估多目标长程导航能力（Go-to-AnyThing）。
*   **Benchmark 与指标**：
    *   **LLM-Match**：使用 GPT-4o 作为裁判评估答案语义准确性。
    *   **SPL (Success weighted by Path Length)**：衡量路径效率。
    *   **LLM-Match × SPL**：综合衡量回答质量与探索效率。
*   **对比方法**：包括 GPT-4o/GPT-4 原生模型、基于场景图的方法（ConceptGraphs）、以及最新的 SOTA 方法（如 3D-Mem, ReExplore）。

#### 4. 资源与算力
*   **骨干网络**：实验使用了商用模型 **GPT-4o** 和开源模型 **Qwen2.5-VL-7B-Instruct**。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或训练/推理总时长。由于该框架主要基于非参数化检索和 Prompt Engineering，其核心开销可能集中在 MLLM 的 API 调用或本地推理上。

#### 5. 实验数量与充分性
*   **实验规模**：在 A-EQA 和 GOAT-Bench 两个主流基准上进行了大规模测试。
*   **消融实验**：分别禁用了情节记忆和语义记忆，验证了两者对“探索效率”和“推理准确性”的独立贡献。
*   **定性分析**：提供了可视化案例（Case Study），展示了程序化规则如何纠正智能体的错误逻辑。
*   **充分性评价**：实验设计较为充分，涵盖了不同能力的 MLLM 骨干网络，对比了多种基准方法，结果具有较强的说服力和客观性。

#### 6. 论文的主要结论与发现
*   **性能提升**：在 A-EQA 上，LLM-Match 提升 7.3%，LLM-Match × SPL 提升 11.4%；在 GOAT-Bench 上成功率提升 7.7%。
*   **功能分工**：**情节记忆**主要通过减少冗余探索来提高**探索效率**；**语义记忆**则通过提供结构化知识显著增强了**复杂推理**任务（如世界知识和功能性推理）的表现。
*   **鲁棒性**：解耦几何与语义的策略使智能体在处理非平稳环境时比传统的硬性建图方法更具鲁棒性。

#### 7. 优点（亮点）
*   **认知启发**：解耦情节与语义记忆的设计非常符合人类认知心理学，解决了具身智能体“记什么”和“怎么记”的问题。
*   **程序化抽象**：将杂乱的推理日志转化为伪代码规则，是一种极具创新性的经验重用方式，有效提升了知识的可迁移性。
*   **轻量化交互**：通过检索时投影而非实时几何融合，降低了系统对精确几何对齐的依赖。

#### 8. 不足与局限
*   **环境局限性**：目前实验主要集中在室内环境，室外大尺度场景下的视觉差异和地图漂移可能给情节记忆检索带来更大挑战。
*   **参数化程度**：目前记忆模块是非参数化的，虽然灵活但无法通过反向传播进行端到端的联合优化。
*   **实时性压力**：依赖 MLLM 进行频繁的视觉推理验证和规则提取，在实际机器人部署中可能会面临推理延迟问题。

（完）
