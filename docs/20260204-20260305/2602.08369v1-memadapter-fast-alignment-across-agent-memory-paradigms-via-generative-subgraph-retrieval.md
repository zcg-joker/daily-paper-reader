---
title: "MemAdapter: Fast Alignment across Agent Memory Paradigms via Generative Subgraph Retrieval"
title_zh: MemAdapter：通过生成式子图检索实现智能体记忆范式间的快速对齐
authors: "Xin Zhang, Kailai Yang, Chenyue Li, Hao Li, Qiyu Wei, Jun'ichi Tsujii, Sophia Ananiadou"
date: 2026-02-09
pdf: "https://arxiv.org/pdf/2602.08369v1"
tags: ["query:agimem"]
score: 9.0
evidence: 在单一系统中统一异构记忆范式
tldr: 针对大模型智能体内存范式（显式、参数化、隐式）孤立且难以融合的问题，本文提出 MemAdapter 框架。该框架通过生成式子图检索器统一内存空间，并利用轻量化对齐模块实现跨范式快速迁移。实验表明，MemAdapter 在多个基准测试中优于现有系统，且对齐成本极低，支持零样本内存融合，为构建通用智能体内存系统提供了高效方案。
selection_source: fresh_fetch
motivation: 现有的智能体内存系统范式孤立且检索方法耦合，阻碍了不同内存范式之间的泛化与有效融合。
method: 采用两阶段策略，先训练生成式子图检索器，再通过对比学习训练轻量化对齐模块以适配未见的内存范式。
result: "在三大基准测试中性能优于五种强基线，且仅需不到5%的计算资源即可在13分钟内完成跨范式对齐。"
conclusion: MemAdapter 是一种高效、即插即用的解决方案，成功实现了异构内存范式的快速对齐与零样本融合。
---

## 摘要
记忆机制是基于大语言模型（LLM）智能体的核心组件，能够在长时上下文中实现推理和知识发现。现有的智能体记忆系统通常设计在孤立的范式内（例如显式记忆、参数化记忆或潜空间记忆），其检索方法紧密耦合，阻碍了跨范式的泛化与融合。在这项工作中，我们迈出了在单一记忆系统中统一异构记忆范式的第一步。我们提出了 MemAdapter，这是一个能够实现智能体记忆范式间快速对齐的记忆检索框架。MemAdapter 采用两阶段训练策略：(1) 从统一记忆空间训练生成式子图检索器；(2) 通过对比学习训练轻量级对齐模块，使检索器适配未见的记忆范式。这种设计提高了记忆检索的灵活性，并显著降低了跨范式的对齐成本。在三个公开评估基准上的全面实验表明，生成式子图检索器在三种记忆范式和不同智能体模型规模下，始终优于五种强大的智能体记忆系统。值得注意的是，MemAdapter 在单张 GPU 上仅需 13 分钟即可完成跨范式对齐，以不到 5% 的训练计算量实现了优于原始记忆检索器的性能。此外，MemAdapter 实现了有效的跨记忆范式零样本融合，凸显了其作为智能体记忆系统即插即用解决方案的潜力。

## Abstract
Memory mechanism is a core component of LLM-based agents, enabling reasoning and knowledge discovery over long-horizon contexts. Existing agent memory systems are typically designed within isolated paradigms (e.g., explicit, parametric, or latent memory) with tightly coupled retrieval methods that hinder cross-paradigm generalization and fusion. In this work, we take a first step toward unifying heterogeneous memory paradigms within a single memory system. We propose MemAdapter, a memory retrieval framework that enables fast alignment across agent memory paradigms. MemAdapter adopts a two-stage training strategy: (1) training a generative subgraph retriever from the unified memory space, and (2) adapting the retriever to unseen memory paradigms by training a lightweight alignment module through contrastive learning. This design improves the flexibility for memory retrieval and substantially reduces alignment cost across paradigms. Comprehensive experiments on three public evaluation benchmarks demonstrate that the generative subgraph retriever consistently outperforms five strong agent memory systems across three memory paradigms and agent model scales. Notably, MemAdapter completes cross-paradigm alignment within 13 minutes on a single GPU, achieving superior performance over original memory retrievers with less than 5% of training compute. Furthermore, MemAdapter enables effective zero-shot fusion across memory paradigms, highlighting its potential as a plug-and-play solution for agent memory systems.

---

## 论文详细总结（自动生成）

### 论文总结：MemAdapter - 通过生成式子图检索实现智能体记忆范式的快速对齐

#### 1. 核心问题与研究动机
大语言模型（LLM）智能体的记忆系统目前存在**范式孤立**的问题。记忆通常被分为三类：**显式记忆**（外部存储的文本或图）、**参数化记忆**（通过微调嵌入模型参数）和**潜空间记忆**（如 KV Cache 或隐藏状态）。
*   **痛点**：现有的检索方法与特定范式高度耦合，导致跨范式泛化困难。例如，将图检索适配到潜空间记忆通常需要重构架构和昂贵的持续训练，且容易引发灾难性遗忘。
*   **目标**：建立一个统一的记忆检索框架，能够以极低的成本实现不同记忆范式之间的对齐与融合。

#### 2. 方法论
MemAdapter 提出了一个两阶段的训练策略，核心在于构建一个**统一记忆空间（Unified Memory Space）**：
*   **阶段一：生成式子图检索（模型蒸馏）**：
    *   定义一个“锚点范式”（如显式图记忆），通过一个强大的教师模型（Qwen2.5-32B）生成查询相关的证据子图作为监督信号。
    *   训练一个轻量级的学生检索器（Qwen2.5-1.5B），使其能够根据任务查询和统一空间中的记忆表示，自回归地生成结构化的证据子图。
*   **阶段二：跨范式对齐（对比学习）**：
    *   为了适配新的记忆范式（如潜空间记忆），无需重新训练检索器。
    *   仅训练一个极其轻量级的**对齐模块**（简单的前馈网络），利用 **InfoNCE 对比损失函数**，将目标范式的记忆状态映射到统一记忆空间中，使其与锚点范式的表示对齐。
*   **即插即用融合**：在推理时，来自不同范式的记忆表示可以通过简单的池化（如 Max-pooling）进行融合，实现零样本的异构记忆协同。

#### 3. 实验设计
*   **数据集**：选取了三个记忆密集型的多跳问答基准：**WikiMultiHopQA**、**NarrativeQA** 和 **MuSiQue**。
*   **智能体模型规模**：在 Qwen2.5-Instruct 的 1.5B、3B 和 7B 三个量级上进行了验证。
*   **对比方法**：
    *   **显式记忆**：A-Mem, Mem0。
    *   **参数化记忆**：StreamingLLM。
    *   **潜空间记忆**：MemoryLLM, CARE。
*   **评估指标**：Exact Match (EM), F1 分数, ROUGE-1。

#### 4. 资源与算力
*   **硬件**：使用 NVIDIA **H200** GPU。
*   **训练时长**：
    *   **阶段一（检索器蒸馏）**：在 6 张 GPU 上约 1 小时完成。
    *   **阶段二（跨范式对齐）**：在 **单张 GPU** 上仅需约 **13 分钟**（其中 10 分钟用于特征提取，2.5 分钟用于对齐模块训练）。
*   **计算效率**：与直接微调原始记忆检索器相比，MemAdapter 节省了超过 **95%** 的训练计算资源。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流基准、3 种记忆范式、3 种模型规模，总计测试了超过 22,000 个问题。
*   **深度分析**：除了主实验外，还进行了**记忆效率分析**（证明其能减少 50% 的记忆长度）、**跨范式融合实验**（证明性能随记忆比例增加而稳健提升）、**t-SNE 可视化**（直观展示对齐效果）以及**案例研究**。
*   **客观性**：实验设置了统一的随机种子，对比了多种范式的最先进（SOTA）基线，实验设计较为全面且公平。

#### 6. 主要结论与发现
*   **性能卓越**：MemAdapter 在所有基准和模型规模下均优于现有的五种强基线系统。
*   **高效对齐**：仅需 2,500 条演示数据即可完成跨范式对齐，且性能优于原始范式自带的检索器。
*   **信息压缩**：生成的证据子图极其精简，在减少 50% 以上冗余信息的同时，保持了极高的记忆利用率。
*   **融合能力**：证明了不同范式的记忆可以互补，大模型（如 7B）能更好地利用这种结构化的融合证据。

#### 7. 优点与亮点
*   **统一性**：首次尝试在单一框架下统一显式、参数化和潜空间三种异构记忆范式。
*   **极速适配**：13 分钟的对齐时间使其具有极强的实用性，真正实现了“即插即用”。
*   **可解释性**：通过生成显式的证据子图（节点和边），相比于纯向量检索，提供了更好的推理透明度。
*   **低门槛**：对算力要求极低，适合快速部署到不同的智能体架构中。

#### 8. 不足与局限
*   **教师模型依赖**：第一阶段的子图生成高度依赖于教师模型的质量，若教师模型存在偏见或错误，会影响检索器的上限。
*   **噪声敏感性**：在处理如 StreamingLLM 这种未经过精炼的原始长上下文记忆时，对齐模块的压力较大，性能提升幅度相对较小。
*   **子图模式固定**：目前的子图生成遵循特定的线性化模式，对于极其复杂的非线性关系可能仍存在表达瓶颈。

（完）
