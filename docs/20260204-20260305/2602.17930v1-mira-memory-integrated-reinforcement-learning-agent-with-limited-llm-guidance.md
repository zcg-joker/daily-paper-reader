---
title: "MIRA: Memory-Integrated Reinforcement Learning Agent with Limited LLM Guidance"
title_zh: MIRA：受限大语言模型引导下的集成记忆强化学习智能体
authors: "Narjes Nourzad, Carlee Joe-Wong"
date: 2026-02-20
pdf: "https://arxiv.org/pdf/2602.17930v1"
tags: ["query:agimem"]
score: 9.0
evidence: 用于轨迹和子目标的结构化演化记忆图
tldr: 针对强化学习在稀疏奖励环境下采样效率低的问题，本文提出MIRA框架。该框架通过构建一个集成了LLM先验知识和高回报经验的结构化记忆图，在训练初期引导智能体。通过将LLM查询转化为持久化存储并利用效用信号调整优势估计，MIRA在显著减少LLM在线调用次数的同时，提升了学习效率并保持了收敛性。
selection_source: fresh_fetch
motivation: 旨在解决强化学习在稀疏奖励任务中对LLM过度依赖导致的扩展性差和成本高昂的问题。
method: 构建一个动态演化的记忆图来存储决策信息，并从中导出效用信号以软性调整策略更新的优势估计。
result: 实验表明MIRA在稀疏奖励环境下优于传统RL基准，且在LLM查询次数大幅减少的情况下达到了与高频LLM指导方法相当的性能。
conclusion: MIRA证明了通过结构化记忆整合有限的LLM指导，可以有效加速强化学习过程并降低对外部模型的依赖。
---

## 摘要
强化学习（RL）智能体在稀疏或延迟奖励设置中，由于先验结构有限，往往面临高样本复杂度的问题。大语言模型（LLM）可以提供子目标分解、合理的轨迹以及抽象先验，从而促进早期学习。然而，过度依赖 LLM 监督会引入可扩展性限制，并产生对潜在不可靠信号的依赖。我们提出了 MIRA（集成记忆的强化学习智能体），它结合了一个结构化的、不断演化的记忆图来引导早期训练。该图存储了与决策相关的信息，包括轨迹片段和子目标结构，并由智能体的高回报经验和 LLM 输出共同构建。这种设计将 LLM 查询摊销到持久记忆中，而不是需要持续的实时监督。从该记忆图中，我们推导出一个效用信号，该信号可以软性地调整优势估计，从而在不修改底层奖励函数的情况下影响策略更新。随着训练的进行，智能体的策略逐渐超越初始的 LLM 衍生先验，且效用项逐渐衰减，从而保持了标准的收敛性保证。我们提供的理论分析表明，基于效用的塑造（shaping）改善了稀疏奖励环境中的早期学习。实验结果表明，MIRA 的表现优于 RL 基准模型，并达到了与依赖频繁 LLM 监督的方法相当的回报，同时所需的在线 LLM 查询显著减少。项目网页：https://narjesno.github.io/MIRA/

## Abstract
Reinforcement learning (RL) agents often suffer from high sample complexity in sparse or delayed reward settings due to limited prior structure. Large language models (LLMs) can provide subgoal decompositions, plausible trajectories, and abstract priors that facilitate early learning. However, heavy reliance on LLM supervision introduces scalability constraints and dependence on potentially unreliable signals. We propose MIRA (Memory-Integrated Reinforcement Learning Agent), which incorporates a structured, evolving memory graph to guide early training. The graph stores decision-relevant information, including trajectory segments and subgoal structures, and is constructed from both the agent's high-return experiences and LLM outputs. This design amortizes LLM queries into a persistent memory rather than requiring continuous real-time supervision. From this memory graph, we derive a utility signal that softly adjusts advantage estimation to influence policy updates without modifying the underlying reward function. As training progresses, the agent's policy gradually surpasses the initial LLM-derived priors, and the utility term decays, preserving standard convergence guarantees. We provide theoretical analysis showing that utility-based shaping improves early-stage learning in sparse-reward environments. Empirically, MIRA outperforms RL baselines and achieves returns comparable to approaches that rely on frequent LLM supervision, while requiring substantially fewer online LLM queries. Project webpage: https://narjesno.github.io/MIRA/

---

## 论文详细总结（自动生成）

这是一份关于论文《MIRA: Memory-Integrated Reinforcement Learning Agent with Limited LLM Guidance》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
强化学习（RL）在处理**稀疏奖励（Sparse Reward）**或**延迟反馈**的任务时，面临极高的采样复杂度，因为智能体在早期很难通过随机探索获得有效的信用分配（Credit Assignment）。
*   **现状与挑战**：虽然大语言模型（LLM）能提供高层级的规划和先验知识，但现有方法通常要求**高频（甚至每步）**的 LLM 监督。这导致了三个问题：1) **可扩展性差**（推理延迟和成本高）；2) **过度依赖**（LLM 幻觉可能误导智能体）；3) **干扰 RL 自主性**（可能导致策略无法收敛到最优）。
*   **核心目标**：开发一种既能利用 LLM 抽象知识加速早期学习，又能保持 RL 自主性、减少 LLM 调用频率并保证长期收敛的框架。

### 2. 方法论
MIRA 的核心思想是将 LLM 的指导“摊销”到一个**动态演化的记忆图（Memory Graph）**中，并通过**效用信号（Utility Signal）**软性地调整优势估计。

*   **记忆图设计**：
    *   **节点**：存储决策相关的上下文，包括轨迹片段（状态-动作对）、子目标描述、估计回报和置信度。
    *   **构建与演化**：初始由 LLM 离线生成先验；训练中，若智能体发现更高回报的路径，则更新节点；若当前记忆无法提供指导，则触发低频的在线 LLM 查询。
*   **筛选单元（Screening Unit）**：
    *   通过计算 Token 似然概率或进行多样本一致性测试（Majority-consistency test）来过滤 LLM 的幻觉或低质量建议。
*   **效用信号计算**：
    *   将智能体的当前轨迹与记忆图中的节点进行匹配，计算**行为相似度**和**语义目标对齐度**，生成效用值 $U_t$。
*   **自适应优势塑造（Adaptive Advantage Shaping）**：
    *   **核心公式**：$\tilde{A}_t = \eta_t A_t + \xi_t U_t$。
    *   其中 $A_t$ 是传统的 RL 优势函数，$\eta_t$ 和 $\xi_t$ 是随时间变化的权重。
    *   **动态衰减**：在训练初期，$\xi_t$ 较大以利用 LLM 指导；随着策略成熟，$\xi_t \to 0$ 且 $\eta_t \to 1$。这确保了最终策略完全由环境奖励驱动，保证了收敛性。

### 3. 实验设计
*   **数据集/场景**：
    *   **Gymnasium (Frozen Lake)**：基础表格环境，验证收敛性。
    *   **MiniGrid & BabyAI**：包括 Red Ball（导航）、Lava Crossing（避障）、Door Key（逻辑链）、Red Blue Door（顺序敏感）以及带干扰物的 Distracted Door Key。
*   **对比方法（Baselines）**：
    *   **PPO**：纯强化学习基准。
    *   **HRL**：分层强化学习。
    *   **LLM-RS**：基于 LLM 的奖励塑造方法。
    *   **LLM4Teach**：基于 LLM 策略蒸馏的教师-学生模型（代表高频监督方法）。
*   **评估指标**：平均回报、成功率、LLM 查询效率（单位查询带来的回报提升）、墙钟时间。

### 4. 资源与算力
*   **硬件**：使用了 Linux 服务器（Intel Xeon E5-2630 v4, 40 线程）和 Apple M2（8核 CPU, 10核 GPU, 16GB 统一内存）。
*   **模型**：调用了 GPT-o4-mini, GPT-4o, Claude 3.5 Sonnet, Gemini 2.5 Pro/Flash, Gemma 3 27B 等多种 LLM。
*   **训练时长**：文中未给出具体总小时数，但通过“墙钟时间”对比图显示，虽然 LLM 查询有延迟，但由于 MIRA 减少了无效探索，达到相同性能所需的总时间往往少于纯 PPO。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个不同的任务环境，每个实验至少重复 4 个随机种子。
*   **消融实验**：非常充分。包括：1) 在线查询频率的影响；2) 面对不可靠/错误 LLM 输出时的鲁棒性；3) 不同 LLM 模型（推理风格）对结果的影响；4) 提示词（Prompt）敏感度测试；5) 筛选阈值的敏感度。
*   **客观性**：使用了 Welch's t-test 进行统计显著性分析，证明 MIRA 在查询次数减少一个数量级的情况下，性能与高频监督方法（LLM4Teach）无显著差异。

### 6. 主要结论与发现
*   **高效率**：MIRA 在稀疏奖励任务中显著提升了早期学习速度，且最终性能优于 HRL 和纯 RL。
*   **低成本**：相比于 LLM4Teach（需 >500 次查询），MIRA 仅需约 30 次查询（7次离线 + 20次左右在线）即可达到同等水平。
*   **鲁棒性**：即使 LLM 提供错误建议，由于优势塑造的衰减机制和筛选单元，智能体最终仍能通过环境反馈纠正错误并收敛。
*   **理论验证**：理论证明了在奖励稀疏导致梯度消失的阶段，效用项能提供非零的更新方向。

### 7. 优点
*   **架构解耦**：不需要修改策略网络或价值网络的结构，具有很强的通用性，可适配任何基于优势函数的算法（如 PPO, A2C）。
*   **记忆持久化**：通过记忆图将瞬时的 LLM 建议转化为持久的结构化知识，避免了重复查询。
*   **收敛保证**：通过自适应衰减权重，巧妙地解决了 LLM 潜在偏见与 RL 最优性之间的矛盾。

### 8. 不足与局限
*   **超参数敏感性**：权重 $\eta_t$ 和 $\xi_t$ 的衰减速率需要针对不同环境进行调优。
*   **动作空间限制**：目前主要在离散动作空间（网格世界）中验证，在连续控制任务（如复杂机器人操纵）中的相似度函数定义会更复杂。
*   **依赖初始质量**：虽然有纠错机制，但如果初始离线先验质量极差，仍可能在训练初期浪费一定的计算资源。

（完）
