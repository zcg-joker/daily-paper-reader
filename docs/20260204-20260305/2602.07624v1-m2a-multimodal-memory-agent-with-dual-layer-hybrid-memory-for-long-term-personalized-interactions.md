---
title: "M2A: Multimodal Memory Agent with Dual-Layer Hybrid Memory for Long-Term Personalized Interactions"
title_zh: M2A：具有双层混合记忆的多模态记忆智能体，用于长期个性化交互
authors: "Junyu Feng, Binxiao Xu, Jiayi Chen, Mengyu Dai, Cenyang Wu, Haodong Li, Bohan Zeng, Yunliu Xie, Hao Liang, Ming Lu, Wentao Zhang"
date: 2026-02-07
pdf: "https://arxiv.org/pdf/2602.07624v1"
tags: ["query:agimem"]
score: 9.0
evidence: 用于长期个性化交互的双层混合记忆系统
tldr: 针对长期人机交互中个性化信息难以持续更新和利用的问题，本文提出了M2A，一种具有双层混合记忆的代理系统。该系统通过ChatAgent和MemoryManager协作，结合原始对话日志和高层语义记忆，实现了多模态信息的在线更新与检索。实验证明，M2A在处理长期个性化交互方面显著优于现有基准，为高质量个体化响应提供了新路径。
selection_source: fresh_fetch
motivation: 现有的个性化多模态模型多为静态，难以在长期交互中持续吸收和利用用户不断变化的增量概念和偏好。
method: 提出由ChatAgent和MemoryManager组成的双层混合记忆系统，结合原始消息存储和语义记忆存储，并开发了长对话数据合成流水线。
result: 实验结果显示，M2A在长期个性化交互任务中显著超越了现有基准模型，展现了更强的记忆管理和应用能力。
conclusion: 将个性化从一次性配置转变为协同演进的记忆机制，是实现高质量长期多模态交互的有效途径。
---

## 摘要
这项工作解决了长期人机交互中个性化问答的挑战：当对话历史跨越数周或数月并超出上下文窗口时，现有的个性化机制难以持续吸收和利用用户增量的概念、别名和偏好。目前的个性化多模态模型主要是静态的——概念在初始化时固定，无法在交互过程中演进。我们提出了 M2A，一种基于智能体的双层混合记忆系统，通过在线更新来维护个性化多模态信息。该系统采用两个协作智能体：ChatAgent 管理用户交互并自主决定何时查询或更新记忆，而 MemoryManager 将来自 ChatAgent 的记忆请求分解为对双层记忆库的具体操作。该记忆库结合了 RawMessageStore（不可变对话日志）和 SemanticMemoryStore（高层观察），提供不同粒度的记忆。此外，我们开发了一个可重用的数据合成流水线，在保持时间连贯性的同时，将来自 Yo'LLaVA 和 MC-LLaVA 的基于概念的会话注入到 LoCoMo 长对话中。实验表明，M2A 显著优于基准模型，证明了将个性化从单次配置转变为协同演进的记忆机制，为长期多模态交互中提供高质量的个性化响应提供了一条可行路径。代码可在 https://github.com/Little-Fridge/M2A 获取。

## Abstract
This work addresses the challenge of personalized question answering in long-term human-machine interactions: when conversational history spans weeks or months and exceeds the context window, existing personalization mechanisms struggle to continuously absorb and leverage users' incremental concepts, aliases, and preferences. Current personalized multimodal models are predominantly static-concepts are fixed at initialization and cannot evolve during interactions. We propose M2A, an agentic dual-layer hybrid memory system that maintains personalized multimodal information through online updates. The system employs two collaborative agents: ChatAgent manages user interactions and autonomously decides when to query or update memory, while MemoryManager breaks down memory requests from ChatAgent into detailed operations on the dual-layer memory bank, which couples a RawMessageStore (immutable conversation log) with a SemanticMemoryStore (high-level observations), providing memories at different granularities. In addition, we develop a reusable data synthesis pipeline that injects concept-grounded sessions from Yo'LLaVA and MC-LLaVA into LoCoMo long conversations while preserving temporal coherence. Experiments show that M2A significantly outperforms baselines, demonstrating that transforming personalization from one-shot configuration to a co-evolving memory mechanism provides a viable path for high-quality individualized responses in long-term multimodal interactions. The code is available at https://github.com/Little-Fridge/M2A.

---

## 论文详细总结（自动生成）

### M2A: 具有双层混合记忆的多模态记忆智能体论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
在长期的跨月、跨年人机交互中，用户会不断引入新的概念（如宠物名字）、视觉偏好和特定别名。现有的个性化多模态大模型（VLMs）面临两大挑战：
*   **静态性限制**：大多数模型在初始化时固定了用户概念，无法在对话过程中动态吸收、修正或更新增量信息。
*   **长上下文失效**：对话历史极长时会超出模型的上下文窗口，简单的检索增强生成（RAG）往往因缺乏语义深度或丢失细节而导致响应质量下降。
**M2A** 旨在通过一种**代理式（Agentic）在线更新机制**，将个性化从“一次性配置”转变为“随交互演进”的动态记忆系统。

#### 2. 方法论：核心思想与关键技术
M2A 采用双智能体协作架构，将交互逻辑与记忆管理解耦：
*   **双智能体协作**：
    *   **ChatAgent（交互智能体）**：负责与用户对话，通过 ReAct 工作流（查询→生成→更新）自主决定何时访问或修改记忆。
    *   **MemoryManager（记忆管理智能体）**：拥有记忆库的独家读写权，负责执行复杂的推理驱动检索和原子化更新操作。
*   **双层混合记忆结构**：
    *   **底层：RawMessageStore（原始消息库）**：存储不可变的原始对话日志，保留所有细节。
    *   **顶层：SemanticMemoryStore（语义记忆库）**：存储高层语义观察（如“用户的狗叫 Bobo，喜欢蓝色玩具”）。
    *   **证据链接（Evidence Linking）**：语义条目通过 `evidence_ids` 链接到原始消息，实现从“粗粒度语义”到“细粒度上下文”的渐进式精准定位。
*   **三路混合检索（Tri-path Retrieval）**：
    *   结合**稠密文本向量**（语义相关性）、**BM25 稀疏检索**（关键词/名称匹配）和**跨模态图像嵌入**（视觉匹配），通过互惠排名融合（RRF）提升召回鲁棒性。

#### 3. 实验设计
*   **数据集**：构建了增强版 **LoCoMo** 数据集。通过数据合成流水线，将基于特定概念的视觉会话（源自 Yo'LLaVA 和 MC-LLaVA）注入到长对话中，平均每条对话包含 621 轮，约 1 万个 Token。
*   **Benchmark 分类**：涵盖单跳（Single-Hop）、多跳（Multi-Hop）、时间推理（Temporal）、开放域（Open Domain）以及新增的**视觉中心（Visual-Centric）**问题。
*   **对比方法**：
    *   **LoCoMo (RAG)**：单层语义向量存储的传统 RAG。
    *   **Mem0**：支持跨会话知识持久化的通用记忆层（仅限文本）。
    *   **A-MEM**：支持编辑操作的代理式记忆系统（仅限文本）。
*   **评估方式**：采用 **LLM-as-a-judge**，使用 Qwen3-VL-32B 和 GPT-4o 作为独立裁判，进行语义一致性评分。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练总时长。
*   **实现细节**：提到本地模型（如 Qwen3-VL-8B）使用 **vLLM** 框架进行推理加速；闭源模型通过 GPT-4o/mini 的 API 调用。由于该系统主要基于 Agent 框架和 Prompt 工程，不涉及大规模的基础模型预训练，因此算力消耗主要集中在推理端。

#### 5. 实验数量与充分性
*   **实验规模**：对 10 组超长对话进行了详尽测试，涵盖了多种问题类型。
*   **消融实验**：非常充分，分别验证了**双层架构**（性能提升 13.3%）、**迭代检索**（提升 16%）、**三路检索**（提升 4.1%）以及**上下文窗口长度**对记忆操作的影响。
*   **客观性**：通过多模型（GPT-4o-mini, Qwen3-VL-8B, GLM-4.6V-Flash）作为基座模型进行交叉验证，并使用双法官评估，实验设计较为公平、客观。

#### 6. 主要结论与发现
*   **性能领先**：M2A 在所有指标上均显著优于基准模型，特别是在“视觉中心”和“单跳”问题上表现突出。
*   **动态更新的必要性**：实验证明，能够在线修正和更新记忆的系统比静态 RAG 更能适应长期个性化需求。
*   **双层结构的价值**：仅靠语义摘要会丢失关键细节，而通过证据链接回溯原始消息是保证回答准确性的核心。

#### 7. 优点与亮点
*   **架构创新**：双智能体解耦了对话与记忆维护，避免了长上下文对交互的干扰。
*   **多模态融合**：通过视觉描述符（Captioning）和跨模态嵌入，解决了纯文本记忆系统无法检索图像内容的问题。
*   **数据合成方案**：提出的流水线能有效生成具有时间连贯性和视觉关联性的长对话数据，对该领域有参考价值。

#### 8. 不足与局限
*   **推理延迟与成本**：由于采用了多轮 ReAct 工作流和双智能体协作，单次对话的 API 调用次数或推理步数较多，可能导致响应延迟增加和成本上升。
*   **模型依赖性**：系统的表现高度依赖于基座模型的推理能力（如调用 Tool 的准确性），在较弱的小模型上可能效果受限。
*   **数据集规模**：虽然对话长度很长，但总的对话样本数（10 组）相对较少，可能存在特定场景的覆盖偏差。

（完）
