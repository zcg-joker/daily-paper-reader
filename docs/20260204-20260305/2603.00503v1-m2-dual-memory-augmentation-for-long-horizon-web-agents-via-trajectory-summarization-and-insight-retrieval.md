---
title: "M$^2$: Dual-Memory Augmentation for Long-Horizon Web Agents via Trajectory Summarization and Insight Retrieval"
title_zh: M$^2$：通过轨迹摘要与见解检索实现的面向长程网络智能体的双重记忆增强
authors: "Dawei Yan, Haokui Zhang, Guangda Huzhang, Yang Li, Yibo Wang, Qing-Guo Chen, Zhao Xu, Weihua Luo, Ying Li, Wei Dong, Chunhua Shen"
date: 2026-02-28
pdf: "https://arxiv.org/pdf/2603.00503v1"
tags: ["query:agimem"]
score: 9.0
evidence: 包含内部轨迹摘要与外部见解的双层记忆机制
tldr: 针对多模态大模型在长程网页任务中面临的计算开销大和推理能力不足的问题，本文提出了M^2框架。这是一种无需训练的双重记忆增强方法，通过动态轨迹总结（内部记忆）压缩冗余历史，并利用见解检索增强（外部记忆）提供离线指南。该方法在WebVoyager等基准测试中显著提升了成功率并大幅降低了Token消耗，为构建高效长程智能体提供了新思路。
selection_source: fresh_fetch
motivation: 现有的网页智能体在处理长程任务时，面临计算成本高昂、上下文冗余以及复杂场景下推理能力不足的挑战。
method: 提出双层记忆机制，结合动态轨迹总结压缩交互历史，并利用外部见解库检索可操作的指南来辅助决策。
result: "在WebVoyager和OnlineMind2Web上，该方法使Qwen3-VL-32B的成功率提升高达19.6%，同时减少了58.7%的Token消耗。"
conclusion: M^2框架证明了通过高效的记忆管理和外部知识检索，可以在无需额外训练的情况下显著提升网页智能体的长程任务处理能力。
---

## 摘要
基于多模态大语言模型（MLLMs）的智能体在自主网络导航中展现出巨大潜力。然而，处理长程（long-horizon）任务仍然是一个关键瓶颈。现有的策略通常严重依赖大规模数据收集和模型训练，但在面对复杂、长程场景时，仍面临高昂的计算成本和推理能力不足的问题。为此，我们提出了 M$^2$，这是一个无需训练的记忆增强框架，旨在优化上下文效率和决策鲁棒性。我们的方法结合了双层记忆机制，协同利用动态轨迹摘要（内部记忆）将冗长的交互历史压缩为简洁的状态更新，以及见解检索增强（外部记忆）通过从离线见解库中检索可操作的指南来引导智能体。在 WebVoyager 和 OnlineMind2Web 上的广泛评估表明，M$^2$ 持续超越基准模型，使 Qwen3-VL-32B 的成功率提升高达 19.6%，Token 消耗减少 58.7%，而像 Claude 这样的闭源模型在显著降低计算开销的同时，准确率提升了高达 12.5%。

## Abstract
Multimodal Large Language Models (MLLMs) based agents have demonstrated remarkable potential in autonomous web navigation. However, handling long-horizon tasks remains a critical bottleneck. Prevailing strategies often rely heavily on extensive data collection and model training, yet still struggle with high computational costs and insufficient reasoning capabilities when facing complex, long-horizon scenarios. To address this, we propose M$^2$, a training-free, memory-augmented framework designed to optimize context efficiency and decision-making robustness. Our approach incorporates a dual-tier memory mechanism that synergizes Dynamic Trajectory Summarization (Internal Memory) to compress verbose interaction history into concise state updates, and Insight Retrieval Augmentation (External Memory) to guide the agent with actionable guidelines retrieved from an offline insight bank. Extensive evaluations across WebVoyager and OnlineMind2Web demonstrate that M$^2$ consistently surpasses baselines, yielding up to a 19.6% success rate increase and 58.7% token reduction for Qwen3-VL-32B, while proprietary models like Claude achieve accuracy gains up to 12.5% alongside significantly lower computational overhead.

---

## 论文详细总结（自动生成）

# 论文总结：M$^2$: Dual-Memory Augmentation for Long-Horizon Web Agents

## 1. 核心问题与整体含义（研究动机和背景）
在自主网页导航领域，多模态大语言模型（MLLM）智能体在处理**长程任务（Long-horizon tasks）**时面临两大瓶颈：
*   **计算成本高昂**：传统的“全上下文”策略会将所有历史截图和交互文本拼接，导致上下文窗口随步骤线性甚至指数级膨胀，消耗大量 Token。
*   **性能退化（Lost-in-the-Middle）**：冗长且充满噪声的历史信息（如广告、无关侧边栏）会干扰模型的推理能力，导致其在复杂任务中迷失目标。
*   **现有方案局限**：目前的改进方法多依赖于昂贵的监督微调（SFT）或强化学习（RL），且往往需要复杂的模型架构。

**M$^2$ 框架**旨在提出一种**无需训练（Training-free）**、轻量级的双重记忆增强机制，通过优化上下文效率来提升智能体在长程任务中的鲁棒性。

## 2. 方法论：核心思想与关键技术
M$^2$ 引入了**双层记忆架构**，将决策上下文从“全历史”转变为“精简状态+全局指导”：

*   **动态轨迹摘要（内部记忆，In-Mem）**：
    *   **核心思想**：模仿人类认知，只保留高层抽象而非视觉细节。
    *   **技术细节**：在系统提示词中加入自总结指令。每一步交互后，智能体需输出一个三元组（思考、动作、状态摘要）。
    *   **流程**：将历史的原始截图和冗长文本丢弃，仅保留一个递归更新的文本摘要链。这使得上下文长度随任务深度呈亚线性增长。
*   **见解检索增强（外部记忆，Ex-Mem）**：
    *   **核心思想**：利用跨任务的成功经验指导当前决策。
    *   **技术细节**：构建一个离线“见解库（Insight Bank）”，包含从 55k 条成功轨迹中提取的通用 UI 交互规则（如：搜索策略、点击逻辑、状态验证）。
    *   **流程**：推理时，利用 Sentence Transformer 计算当前用户查询与库中历史查询的语义相似度，检索 Top-5 相关见解作为“防御性提示（Defensive Hints）”注入 Prompt。

## 3. 实验设计
*   **数据集/场景**：
    *   **WebVoyager**：涵盖 12 个代表性网站（如 Amazon, GitHub, Apple 等）。
    *   **OnlineMind2Web**：包含 291 个真实世界的在线交互任务，分为简单、中等、困难三个等级。
*   **对比方法**：
    *   **Vanilla (Normal)**：全上下文拼接基准。
    *   **消融变体**：仅使用 In-Mem、仅使用 Ex-Mem。
    *   **SOTA 对比**：与 UI-TARS、OpenAI CUA 等经过大量微调的模型进行性能指标对比。
*   **评估模型**：闭源模型 Claude-3.7-Sonnet、Claude-Sonnet-4；开源模型 Qwen3-VL-32B。

## 4. 资源与算力
*   **训练算力**：论文明确指出该方法是 **Training-free（无需训练）** 的，因此没有模型训练阶段的算力消耗。
*   **推理/实验环境**：
    *   使用高并发 **Kubernetes (K8s) 集群**。
    *   部署了 **64 个并发节点**，每个节点托管一个虚拟计算机环境。
    *   **检索开销**：单次语义检索的延迟仅为 **6 毫秒**，在平均 122 秒的任务时长中几乎可以忽略不计。

## 5. 实验数量与充分性
*   **实验规模**：在两个主流基准上进行了全面测试，涵盖了从闭源顶尖模型到开源中型模型的跨度。
*   **消融实验**：针对历史窗口大小（k）、相似度函数（TF-IDF vs. Embedding）、检索见解数量（i）以及摘要组件进行了详细的消融研究。
*   **客观性**：使用了 GPT-4o 作为第三方公正裁判（Judge），结合最终响应和最后 5 张截图进行成功率判定，避免了自评偏差。实验设计覆盖了不同难度和领域，具有较强的说服力。

## 6. 主要结论与发现
*   **性能显著提升**：在 OnlineMind2Web 上，Qwen3-VL-32B 的成功率提升了 **19.6%**；在 WebVoyager 上，Claude 系列模型也有 5.5%~12.5% 的增益。
*   **大幅降本增效**：Qwen3-VL-32B 的 Token 消耗减少了 **58.7%**，Claude 系列减少了 **30%~55%**。
*   **开源逆袭闭源**：经过 M$^2$ 增强的开源 Qwen3-VL-32B（74.0%）在 WebVoyager 上的表现甚至超过了未经增强的 Claude-3.7-Sonnet（72.0%）。
*   **长程鲁棒性**：内部记忆有效解决了“迷失在中间”的问题，而外部记忆则通过提供“避坑指南”减少了智能体陷入死循环或地理歧义等错误的概率。

## 7. 优点
*   **即插即用**：无需对模型进行昂贵的微调，通过 Prompt 工程和 RAG 即可实现。
*   **双重优化**：同时解决了“效率（Token 成本）”和“效果（成功率）”两个核心矛盾。
*   **通用性强**：见解库中的规则具有跨网站、跨任务的迁移价值，且适用于不同架构的 MLLM。
*   **逻辑清晰**：将复杂的网页交互抽象为“状态-动作”的文本链，符合人类操作逻辑。

## 8. 不足与局限
*   **见解库依赖**：外部记忆的效果高度依赖于离线见解库的质量和覆盖面。如果遇到全新的 UI 范式，检索可能失效。
*   **冷启动成本**：第一步注入见解时会带来约 1.7k Token 的额外开销（虽然长程任务后期会通过摘要节省回来）。
*   **动态性挑战**：对于极度动态、实时变化的网页内容，静态的离线见解可能存在滞后性。
*   **视觉信息丢失风险**：虽然摘要能保留关键逻辑，但完全丢弃历史截图可能在某些极度依赖视觉对比（如找茬类任务）的特殊场景下导致信息缺失。

（完）
