---
title: "OpenSage: Self-programming Agent Generation Engine"
title_zh: OpenSage：自编程智能体生成引擎
authors: "Hongwei Li, Zhun Wang, Qinrun Dai, Yuzhou Nie, Jinjun Peng, Ruitong Liu, Jingyang Zhang, Kaijie Zhu, Jingxuan He, Lun Wang, Yangruibo Ding, Yueqi Chen, Wenbo Guo, Dawn Song"
date: 2026-02-18
pdf: "https://arxiv.org/pdf/2602.16891v1"
tags: ["query:agimem"]
score: 9.0
evidence: 用于智能体生成的层次化图结构记忆系统
tldr: OpenSage是首个支持大模型自动生成拓扑结构和工具集的智能体开发框架（ADK）。针对现有框架依赖人工设计、泛化性差的问题，它引入了自编程机制，允许智能体自主创建子智能体和工具，并结合分层图谱内存系统。实验证明其在多个基准测试中优于现有ADK，推动了从以人为中心向以AI为中心的智能体开发范式转变。
selection_source: fresh_fetch
motivation: 现有智能体开发框架过度依赖人工设计组件，限制了智能体的泛化能力和整体性能。
method: 提出OpenSage框架，通过自编程引擎实现智能体拓扑与工具集的自动生成，并构建了分层图谱内存系统。
result: 在三个主流基准测试上的实验表明，OpenSage在多种大模型底座下均显著优于现有的智能体开发框架。
conclusion: OpenSage通过实现智能体的自我进化与管理，为下一代以AI为核心的智能体开发模式奠定了基础。
---

## 摘要
智能体开发工具包（ADK）为构建智能体提供了有效的平台和工具，其设计对于所构建智能体的性能至关重要，特别是关于智能体拓扑、工具和内存的功能。然而，目前的 ADK 要么缺乏足够的功能支持，要么依赖人工手动设计这些组件，从而限制了智能体的泛化能力和整体性能。我们提出了 OpenSage，这是首个能够让大语言模型（LLM）自动创建具有自生成拓扑和工具集的智能体，并提供全面且结构化内存支持的 ADK。OpenSage 为智能体创建和管理其自身的子智能体及工具包提供了有效的功能。它还具有一个分层、基于图的内存系统以实现高效管理，以及一个专门针对软件工程任务定制的工具包。在三个最先进的基准测试上使用各种骨干模型进行的广泛实验证明了 OpenSage 相比现有 ADK 的优势。我们还进行了严格的消融研究，以证明我们各组件设计的有效性。我们相信 OpenSage 可以为下一代智能体开发铺平道路，将重点从以人为中心转向以人工智能为中心的范式。

## Abstract
Agent development kits (ADKs) provide effective platforms and tooling for constructing agents, and their designs are critical to the constructed agents' performance, especially the functionality for agent topology, tools, and memory. However, current ADKs either lack sufficient functional support or rely on humans to manually design these components, limiting agents' generalizability and overall performance. We propose OpenSage, the first ADK that enables LLMs to automatically create agents with self-generated topology and toolsets while providing comprehensive and structured memory support. OpenSage offers effective functionality for agents to create and manage their own sub-agents and toolkits. It also features a hierarchical, graph-based memory system for efficient management and a specialized toolkit tailored to software engineering tasks. Extensive experiments across three state-of-the-art benchmarks with various backbone models demonstrate the advantages of OpenSage over existing ADKs. We also conduct rigorous ablation studies to demonstrate the effectiveness of our design for each component. We believe OpenSage can pave the way for the next generation of agent development, shifting the focus from human-centered to AI-centered paradigms.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《OpenSage: Self-programming Agent Generation Engine》** 的深度结构化总结：

---

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的智能体开发工具包（ADK，如 LangChain、OpenHands、OpenAI SDK 等）主要依赖人类开发者手动设计智能体的**拓扑结构**（如何分工）、**工具集**（能做什么）和**记忆系统**（如何存储信息）。这种“以人为中心”的范式限制了智能体的泛化能力和在大规模复杂任务中的扩展性。
*   **核心问题**：如何构建一个能够让 AI 自主设计、创建和管理自身架构及功能的开发引擎，实现从“人工特征工程”向“AI 自主进化”的范式转变？
*   **整体含义**：OpenSage 被定义为首个支持“自编程”的智能体生成引擎，允许大模型（LLM）在运行期间动态生成子智能体、编写新工具并管理复杂的图结构记忆。

### 2. 方法论：核心思想与关键技术
OpenSage 的核心思想是提供一个最小化的“脚手架”，让 LLM 能够自主探索最优的系统结构。其关键技术包含三个维度：
*   **自生成智能体拓扑 (Self-generating Topology)**：
    *   **动态创建**：父智能体通过 `create_agent` 工具生成子智能体，定义其系统提示词、工具权限和初始状态。
    *   **拓扑模式**：支持**垂直拓扑**（任务分解为顺序子任务）和**水平拓扑**（多个子智能体并行尝试不同方案并进行集成/Ensemble）。
    *   **生命周期管理**：提供统一的智能体池，支持子智能体的搜索、复用、通信（通过共享留言板）和终止。
*   **动态工具合成 (Dynamic Tool Synthesis)**：
    *   **元工具机制**：提供基础接口（如 Bash/Python 环境），允许智能体编写、测试并注册新工具。
    *   **分层组织**：采用类似文件系统的结构管理成百上千个工具，避免上下文溢出。
    *   **运行时管理**：支持 Docker 容器隔离、状态保存（快照）以及长耗时任务的异步执行。
*   **分层记忆管理 (Hierarchical Memory)**：
    *   **图结构存储**：基于 Neo4j 数据库，将短期执行历史和长期系统知识存储为节点和边，而非简单的线性列表。
    *   **记忆智能体 (Memory Agent)**：引入专门的智能体负责记忆的读写、总结和去重，用户智能体只需通过自然语言与其交互。

### 3. 实验设计
论文在三个代表性的前沿基准测试上验证了 OpenSage 的性能：
*   **CyberGym**：包含 1507 个真实世界的 C/C++ 漏洞，要求智能体编写 PoC（漏洞验证代码）。侧重考察复杂推理和领域工具使用。
*   **Terminal-Bench 2.0**：包含 89 个专家策划的终端任务（涵盖软件工程、科学计算、机器学习等）。侧重考察通用任务处理能力。
*   **SWE-Bench Pro**：针对软件工程的长程任务（266 个 Python 任务）。侧重考察长时记忆和代码库级别的操作。
*   **对比方法**：包括 OpenHands、Claude Code、SWE-agent、Agentless、OpenAI Codex CLI 等当前最强的智能体框架。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件训练算力（如 GPU 型号或数量），因为该研究主要基于现有的闭源/开源大模型（如 GPT-5、Gemini 3 Pro/Flash、Claude 4.5 等）通过 API 进行推理和评估。
*   **成本分析**：文中提到了推理成本，例如在 Terminal-Bench 2.0 上，通过“大模型规划+小模型执行”的协作模式，在保持高性能的同时显著降低了 API 调用成本。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模测试，包括 CyberGym 的全量测试、SWE-Bench Pro 的 Python 子集测试，以及针对 300 个实例的深度消融实验。
*   **充分性与公平性**：
    *   **消融实验**：分别针对水平拓扑、垂直拓扑、工具系统和记忆系统进行了详尽的消融研究，验证了每个组件的增益。
    *   **跨模型验证**：使用了 GPT、Gemini、Claude 等多个家族的模型作为底座，证明了框架的普适性。
    *   **客观性**：对比了多个榜单排名第一的基线方法，实验设计较为严谨。

### 6. 主要结论与发现
*   **性能领先**：OpenSage 在 CyberGym 和 Terminal-Bench 2.0 上均取得了**榜单第一**的成绩，显著优于 OpenHands 等现有 ADK。
*   **自发行为**：观察到模型能够主动创建专门的“调试智能体”和“模糊测试工具”，这种自发生成的专用结构比人工设计的通用结构更有效。
*   **记忆优势**：图结构记忆配合专门的记忆智能体，能有效减少上下文冗余，防止长程任务中的信息丢失。
*   **模型依赖**：虽然 OpenSage 提供了强大的功能，但实验也发现当前的 SOTA 模型（如 GPT-5）仍会出现幻觉（如调用不存在的工具），说明该框架的潜力受限于底层模型的逻辑能力。

### 7. 优点与亮点
*   **范式创新**：率先实现了“AI 驱动的智能体构建”，减少了对人类专家经验的依赖。
*   **系统性强**：不仅提出了概念，还解决了工具隔离、异步执行、图记忆检索等工程化难题。
*   **灵活性高**：支持异构模型协作（如强模型带弱模型），在成本和性能之间取得了平衡。
*   **领域深度**：内置了针对软件工程和安全领域的专业工具包（如静态分析、GDB 调试等）。

### 8. 不足与局限
*   **模型门槛**：该框架高度依赖底层模型的指令遵循和逻辑推理能力，较弱的模型可能无法有效利用其自编程功能。
*   **幻觉风险**：智能体在自主创建工具或子智能体时可能产生幻觉，导致执行失败或陷入死循环。
*   **评估成本**：由于涉及动态创建多个子智能体，其推理成本和时间开销可能高于传统的单智能体结构。
*   **安全边界**：虽然使用了 Docker 隔离，但允许 AI 自主编写并执行代码（尤其是安全相关任务）仍存在潜在的安全风险，需更严格的审计机制。

---
（完）
