---
title: "MemoryArena: Benchmarking Agent Memory in Interdependent Multi-Session Agentic Tasks"
title_zh: MemoryArena：相互依赖的多会话智能体任务中的智能体记忆基准测试
authors: "Zexue He, Yu Wang, Churan Zhi, Yuanzhe Hu, Tzu-Ping Chen, Lang Yin, Ze Chen, Tong Arthur Wu, Siru Ouyang, Zihan Wang, Jiaxin Pei, Julian McAuley, Yejin Choi, Alex Pentland"
date: 2026-02-18
pdf: "https://arxiv.org/pdf/2602.16313v1"
tags: ["query:agimem"]
score: 10.0
evidence: 用于在多会话循环中基准测试智能体记忆的统一评估环境
tldr: MemoryArena是一个针对多会话智能体任务的评估基准，旨在解决现有评估中记忆与行动脱节的问题。它通过设计相互依赖的子任务，要求智能体在与环境交互中积累记忆并指导后续决策。该基准涵盖网页导航、规划、搜索和推理等领域，揭示了在长文本记忆基准上表现优异的智能体在实际复杂任务中仍面临巨大挑战。
selection_source: fresh_fetch
motivation: 现有评估往往将记忆召回与实际行动孤立开来，无法体现智能体在多轮交互中利用记忆指导未来决策的能力。
method: 引入了MemoryArena评估框架，通过人类设计的具有显式依赖关系的子任务，在多会话循环中测试智能体的记忆提炼与应用能力。
result: 实验显示，在传统长文本记忆基准上表现接近满分的智能体，在MemoryArena的智能体任务中表现较差。
conclusion: 该研究揭示了当前智能体记忆评估的空白，强调了记忆与行动在真实应用场景中紧密耦合的必要性。
---

## 摘要
现有的对具有记忆能力的智能体的评估通常将记忆与行动孤立开来。一类基准测试通过测试对过去对话或文本的回忆来评估记忆，但未能捕捉到记忆如何用于指导未来的决策。另一类则侧重于智能体在不需要长期记忆的单会话任务中的表现。然而，在现实场景中，记忆与行动是紧密耦合的：智能体在与环境交互时获取记忆，并随后依靠这些记忆来解决未来的任务。为了捕捉这一设定，我们推出了 MemoryArena，这是一个统一的评估平台，用于在多会话的“记忆-智能体-环境”循环中对智能体记忆进行基准测试。该基准测试由人工设计的智能体任务组成，具有显式相互依赖的子任务，智能体必须通过将经验提炼为记忆，从早期的行动和反馈中学习，并随后利用该记忆指导后续行动以完成整体任务。MemoryArena 支持网页导航、偏好约束规划、渐进式信息搜索和顺序形式推理等领域的评估。研究表明，在 LoCoMo 等现有长上下文记忆基准测试中表现接近饱和的智能体，在我们的智能体设定中表现不佳，这揭示了当前对具有记忆能力的智能体评估存在的差距。

## Abstract
Existing evaluations of agents with memory typically assess memorization and action in isolation. One class of benchmarks evaluates memorization by testing recall of past conversations or text but fails to capture how memory is used to guide future decisions. Another class focuses on agents acting in single-session tasks without the need for long-term memory. However, in realistic settings, memorization and action are tightly coupled: agents acquire memory while interacting with the environment, and subsequently rely on that memory to solve future tasks. To capture this setting, we introduce MemoryArena, a unified evaluation gym for benchmarking agent memory in multi-session Memory-Agent-Environment loops. The benchmark consists of human-crafted agentic tasks with explicitly interdependent subtasks, where agents must learn from earlier actions and feedback by distilling experiences into memory, and subsequently use that memory to guide later actions to solve the overall task. MemoryArena supports evaluation across web navigation, preference-constrained planning, progressive information search, and sequential formal reasoning, and reveals that agents with near-saturated performance on existing long-context memory benchmarks like LoCoMo perform poorly in our agentic setting, exposing a gap in current evaluations for agents with memory.

---

## 论文详细总结（自动生成）

# MemoryArena: 相互依赖的多会话智能体任务中的智能体记忆基准测试

## 1. 核心问题与整体含义
论文指出当前大语言模型（LLM）智能体的评估存在**记忆与行动脱节**的问题。现有的基准测试通常分为两类：一类侧重于静态长文本的记忆召回（如 QA 或摘要），缺乏环境交互和决策过程；另一类侧重于动态环境中的行动（如网页导航），但通常局限于单次会话，不要求跨会话的长期记忆。

在现实应用中，记忆与行动是紧密耦合的。论文提出了 **MemoryArena**，旨在评估智能体在**多会话、相互依赖**的任务中，如何通过“记忆-智能体-环境”循环（Memory-Agent-Environment Loop）来提炼过去经验并指导未来决策。

## 2. 方法论
MemoryArena 的核心思想是将任务分解为具有**显式因果依赖**的子任务序列。

*   **核心流程（Memory-Agent-Environment Loop）**：
    1.  **检索（RETRIEVE）**：在每个子任务开始或行动步骤中，智能体根据当前指令和历史，从持久化存储 $M$ 中检索相关记忆。
    2.  **行动（ACTION）**：智能体基于当前观察和检索到的记忆执行操作。
    3.  **更新（UPDATE）**：子任务完成后，记忆系统将该会话的交互轨迹（动作与反馈）进行抽象、提炼并更新至 $M$ 中。
*   **关键技术细节**：
    *   **多会话结构**：任务被设计为 $[s_1 \to s_2 \to \dots \to s_n]$ 的序列，后续子任务的约束条件在当前提示中是缺失的，必须依赖前序会话中存储的记忆（如之前买过的相机型号、队友的偏好等）才能成功。
    *   **记忆分级**：论文将记忆方法分为 0D（原始上下文）、1D（扁平化提炼记忆）和 2D（结构化/图谱记忆）。

## 3. 实验设计
论文在四个具有挑战性的领域构建了数据集：
1.  **捆绑网页购物（Bundled Web Shopping）**：150个任务。要求购买一系列具有技术兼容性要求的商品（如相机机身与镜头）。
2.  **偏好约束组团旅行规划（Group Travel Planning）**：270个任务。多名成员依次加入，后续成员的计划必须参考并满足前序成员的偏好和已定行程。
3.  **渐进式网页搜索（Progressive Web Search）**：256个任务。通过多步搜索积累约束条件，最终回答一个复杂的组合问题。
4.  **顺序形式推理（Sequential Formal Reasoning）**：60个任务（数学与物理）。基于科研论文的推导链，后续引理的证明依赖于前序步骤的结论。

**对比方法**：
*   **长上下文智能体**：GPT-5.1-mini, GPT-4.1-mini, Gemini-3-Flash, Claude-Sonnet-4.5。
*   **外部记忆系统**：MemGPT (Letta), Mem0, Mem0-g, ReasoningBank。
*   **RAG 系统**：BM25, 嵌入式 RAG, MemoRAG, GraphRAG。

## 4. 资源与算力
*   **模型接口**：实验主要通过官方 API（OpenAI, Anthropic, Vertex AI）运行。
*   **硬件资源**：对于需要本地运行的记忆系统或推理任务，论文明确提到使用了 **NVIDIA H100 GPU**。
*   **训练时长**：文中未详细说明具体的训练时长，因为主要评估的是推理阶段的记忆机制集成，而非从头训练模型。

## 5. 实验数量与充分性
*   **实验规模**：总计涵盖了 766 个复杂任务，平均每个任务包含约 7 个子任务，总计产生超过 40k token 的推理轨迹。
*   **深度分析**：通过 `SR@k`（第 k 步子任务的成功率）分析了性能随依赖深度增加的衰减情况。
*   **客观性与公平性**：实验对比了当前最先进的闭源模型和多种主流的开源记忆/RAG 框架，并使用了统一的 Task Agent（GPT-5.1-mini）作为基准，确保了记忆机制评估的公平性。

## 6. 主要结论与发现
*   **性能鸿沟**：在传统长文本记忆基准上表现接近满分的模型，在 MemoryArena 中的成功率（SR）极低，甚至在某些复杂规划任务中接近 0。
*   **外部记忆并非万能**：增加外部记忆或 RAG 系统并不总是优于直接使用长上下文（Long-Context）。这归因于“表示不匹配”（压缩记忆丢失细节）和“训练不匹配”（智能体未针对检索内容进行优化）。
*   **性能衰减**：随着子任务深度的增加，所有方法的成功率均显著下降，显示出智能体在维护长期、多步骤状态时的脆弱性。
*   **延迟代价**：外部记忆系统虽然在某些长序列任务中有帮助，但显著增加了端到端的执行延迟。

## 7. 优点
*   **因果依赖设计**：不同于以往简单的 QA 召回，MemoryArena 强制要求记忆参与决策，更符合真实智能体场景。
*   **多领域覆盖**：从日常购物到高难度的科研推理，覆盖面广，难度梯度明显。
*   **POMDP 视角**：将多会话记忆问题建模为部分可观测马尔可夫决策过程（POMDP），为未来研究提供了理论框架。

## 8. 不足与局限
*   **任务规模限制**：虽然任务设计精巧，但总样本量（几百个）相对于大规模预训练而言仍较小。
*   **模型依赖**：高度依赖闭源高性能模型的 API，对于开源小模型在这些复杂任务上的表现探索不足。
*   **评估成本**：由于涉及多步交互和复杂的环境反馈，运行完整基准测试的时间和金钱成本较高。
*   **状态漂移**：实验显示智能体极易受到早期错误的影响（信念漂移），目前尚无有效的记忆纠错机制。

（完）
